# BiliMT 开发进度

最后更新：2026-08-27

## 目录

- [更新规则](#更新规则)
- [当前状态](#当前状态)
- [P0 项目决策与规则](#p0-项目决策与规则)
- [P1 基础工程与首页/搜索](#p1-基础工程与首页搜索)
- [P1.5 设置、低配置模式与图片治理](#p15-设置低配置模式与图片治理)
- [P2 账号与登录](#p2-账号与登录)
- [P3 登录态页面](#p3-登录态页面)
- [P4 播放器](#p4-播放器)
- [P5 弹幕与空降助手](#p5-弹幕与空降助手)
- [P6 收尾与发布](#p6-收尾与发布)
- [P7 主页主题与视觉效果](#p7-主页主题与视觉效果)
- [P8 UI/设置/文档收尾](#p8-ui设置文档收尾)
- [P9 重构拆分收尾](#p9-重构拆分收尾)
- [P10 应用内更新](#p10-应用内更新)
- [移动端 UI 与直播（v2.0.x 里程碑）](#移动端-ui-与直播v20x-里程碑)
- [P11 YouTube 内容集成（InnerTube）](#p11-youtube-内容集成innertube)

## 更新规则

- 每完成一个可验证任务，都要更新本文件。
- 状态只使用：`Done`、`In Progress`、`Pending`、`Blocked`、`Superseded`。
- 新任务必须插入到合理阶段，不要只追加到末尾。
- 完成任务时补充验收结果，例如编译、安装、接口验证或用户确认。
- UI/UX 交互体验由用户手动测试，开发侧只负责编译、安装和必要的日志验证。

## 当前状态

当前阶段：真实二维码登录、首页、搜索、动态、历史、设置、点播播放器、字节跳动弹幕叠加层、空降助手、发布构建、TV 图标/横幅、主页主题、液态玻璃、设置重分组、搜索返回缓存、迷你进度条开关和关于展示面板均已接入。**多语言支持（v3.0.5-alpha.3，EN 全量 + ES/PT 骨架）**：语言设置从仅简体/港繁/台繁扩展为 6 档,新增 **English / Español / Português**。①`ChineseTextVariant` 枚举泛化——`forVariant` 对拉丁语系返回 identity(简繁转换只对中文变体生效);②`localizedContext()` locale 映射 English→EN/西→es/葡→pt,`stringResource` 自动命中对应 `values-<locale>`;③两套设置页(TV+移动)各加 3 个语言自名选项;④**移动端三个入口(`MobileApp`/`SettingsActivity`/`LoginActivity`)补 `localizedContext()` + `LocalChineseTextConverter` 包裹**——此前移动端 `stringResource` 走系统 locale,语言设置不生效,是本次关键缺口;⑤**数字本地化 `CountFormatter`**——统一 4 处分散紧凑计数格式化为一个 locale 感知函数,中文变体 `万/亿`(除 1e4/1e8)、拉丁变体 `K/M/B`(除 1e3/1e6/1e9),除数差异是代码逻辑;⑥新建 `values-en`/`values-es`/`values-pt` **三语全量翻译**(各 664 key 严格对齐默认,key 集合零缺失零多余、占位符逐一比对零错位、无裸 `&`/`<`/悬空 `%`,aapt 安全);⑦硬编码 UI 中文收口(~671 处 CJK 甄别,UI 标签抽 `stringResource`、日志/来源数据保留中文)。**语言真正切换生效的真因 = `build.gradle.kts` `androidResources.localeFilters`**(原 `zh/zh-rHK/zh-rTW` 三值把 `values-en/es/pt` 从 APK 里整个剪掉——APK `resources.arsc` 字节搜索证实只有默认+zh-rTW/zh-HK 的串,英文设置永远回落默认中文;补 `en/es/pt` 进 filter 后 en 真机切换生效)。曾试错四入口 `LocalResources provides`(Compose 新版本 `LocalResources` 默认从 `LocalContext` 算,en 进包后已冗余但无害、保留作兜底)。云编译经 3 轮修(裸 `&` 实体、撇号 `'` 需 `\'` 转义、`configuration.locales` 是 `LocaleList` 非 Kotlin List)后绿。**YouTube 内容集成（P11）搜索/热门/动态/频道管理/播放/多播放列表+播放器◀▶ 已实施，SABR 播放 alpha.59 已切合成 DASH MPD（SegmentTemplate + 每段 DataSource，DashMediaSource 按需逐段拉跨 60s + 原生 seek，彻底移除轮换）；alpha.59 方案A 网络栈根因修复——/player 走真实浏览器会话 WebView（真实页上下文 + 真实 cookie/TLS），用其真实 visitorData/cookie，对齐 FreeTubeAndroid 主 WebView，修 LOGIN_REQUIRED bot 拦截；alpha.61 SABR n-decrypt/harvest 修复——harvest 用长期存活 WebView（首次懒建+加载真实首页建立浏览上下文，跨 harvest 复用），对齐 YoutubeBrowserSession 成功模式，避免 fresh WebView 被风控成空白页致 SABR POST 采不到**；alpha.62 SABR DASH A/V 同步修复——真机 alpha.61 暴露音频先出/视频卡顿需暂停续播/快 1 分钟重播，研究 FreeTube 源码定位根因：DASH 每段 playerTimeMs 用了 `(段号-1)*声明段长`（video 声明 6000 > 实际 ~4360 → playerTimeMs 超前 → 服务端跳段 realSeq>N → 视频内容有洞抽干，progressive 兜底路径早已用实际累计所以正常），改回**每段实际 MediaHeader.durationMs 累加**作 playerTimeMs（对齐 FreeTube getTotalDownloadedDuration），并把 video MPD 段时长 6000→4300 对齐时间线（seek/EOS）；alpha.63 真机坐实 A/V 已同步但 60s 断崖仍在（own range 报真实增长仍被服务端在 playerTimeMs≈61604 软拒不给 MEDIA_HEADER → 6 backoff → evict → 重播），锁定根因=**双流架构**（video/audio 两个独立 SABR POST 互标对方格式假满缓冲 Int.MAX），服务端 60s-per-session 窗口在双流模型边界软拒；alpha.64 完整移植 LibreTube 自定义 SABR MediaSource（**单流**——一个 POST 同时带 A+V 两 itag、bitfield=0、两 selectedFormatIds、两真实 bufferedRanges，一次 UMP 拉多段缓存续喂，A/V 在 MediaPeriod 层拆成两个 ChunkSampleStream 共享一个 SabrMediaFetcher），替换 DASH 双流，根治 60s 断崖 + A/V 异步 + 退出后台音频残留；新建 `sabr/media/` 包 11 文件（Manifest/AdaptationSet/Representation/MediaSource/MediaPeriod/ChunkSource/DefaultChunkSource/DataSource/Segment/SegmentRequest/SabrMediaFetcher），resolver/PlaybackModels/PlayerScreen/MobilePlayerScreen 接 `isSabrSingle` 分支，MaxBufferMs 恢复 50s**；直播播放暂缓，后续单独评估。**alpha.67**：TV 搜索源切换由双 pill 改成**单 pill 按确认(OK)循环**(B站↔YouTube)，消除未选中 YouTube pill 像「禁用」的歧义（见 P11-02）；TV WebDAV 编辑弹窗自绘 D-pad 键盘换成 `OutlinedTextField` 走系统输入法（密码字段掩码 + imePadding 防 IME 遮挡 Save）。**alpha.71 path C**：引入 LibreTube 的 NewPipeExtractor fork(`com.github.libre-tube:NewPipeExtractor` jitpack `738c3d4`)作为 SABR 取流唯一数据源——`StreamInfo.getInfo` 用 visionOS 客户端发干净 /player 拿未绑定浏览器会话的 `serverAbrStreamingUrl`+`ustreamerConfig`(网关 URL 无 n-decrypt)，`BiliTvPoTokenProvider` 在 getInfo 期间铸造并缓存 128B poToken 供 init 复用(单一 minter)，退役 `YoutubeSabrHarvester` + harvest 分支；`SabrMediaFetcher.processPart` 加 itag 白名单广告过滤。根除跨 minter / 浏览器会话绑定(status=3 60s 重启)。**v3.0.0-alpha.1**：TV WebDAV 编辑弹窗内容加 `verticalScroll`——系统键盘弹出后保存/取消按钮可滚动到可见区(此前被 IME 顶出屏幕外看不到)，第二按钮 关闭→取消 对齐移动端；TV 搜索源切换由单 pill ⇄ 循环**还原成双 pill**(BILIBILI+YOUTUBE 同时显示、选中高亮、去箭头)；移动端 WebDAV 区块展开后 `delay(300)` 等动画完成再 `bringIntoView()` 自动上移，备份/还原按钮滚进可视区(此前在折叠线以下需手动下滑)。**多语言配音修复(P11-16)**：修复「中文视频播英文配音」——根因是 YouTube multi-audio 下同一 itag 按语言重复出现、`pickAudio` 盲取第一条可能拿到配音轨；`pickAudio` 优先 `audioTrack.audioIsDefault=true`(原声轨)、SABR 路径优先 `AudioTrackType.ORIGINAL`；播放器控制栏加音轨切换按钮(仅 YouTube 多音轨视频显示)，`availableAudioTracks` 暴露全部音轨、选中即 `preferredAudioTrackId` 重解析(经典+SABR 统一)；设置加 YouTube 默认画质(`YoutubeDefaultQuality` 按 `maxHeight` 上限选档)。**YouTube 播放历史 + 断电续播(P11-17)**：根因——`PlaybackProgressStore.saveProgress` 有 `cid <= 0L` 守卫，而 YouTube 请求 `cid` 恒为 0，进度从未落盘；放宽守卫为 `cid < 0L` 后 TV/移动端播放器既有 `saveProgress`/`getSavedProgress` 即恢复续播(播完的视频经 `isWatchCompleted` 阈值从 0 起播)。新建 `core/youtube/YoutubeHistoryStore.kt`(DataStore 列表存储，cap 50 按 `lastPlayedAtMs` 倒序)，TV 与移动端播放器 Ready 时 `recordPlay`、暂停/退出时 `updatePosition`；`PlaybackRequest` 加 `channelId` 供开频道。移动端「动态」底栏「历史」子 tab 并入 YouTube 历史(`MobileHistoryPage` 本地 YouTube 历史与 B 站历史**混合、按播放时间倒序**，YouTube 绿框，未登录也显示本地 YouTube 历史 + 登录提示，原独立「YouTube 历史」子 tab 移除)，点击续播、点头像开频道。**听视频模式(音频-only)**：移动端播放器顶栏右上角新增耳机按钮，开启后禁用视频轨只播音频——`toggleAudioOnly` 用 `player.setTrackSelectionParameters(...setTrackTypeDisabled(C.TRACK_TYPE_VIDEO, audioOnly))`，不重新解析、不重建 MediaSource；对三种源均成立(B站 DASH / PGC 合并流走标准 Media3 轨道选择，YouTube SABR 单流由 `SabrMediaPeriod` 释放被禁用的视频流 + `SabrMediaFetcher` 退化为纯音频拉取)；`loadRequest` 重载后重新应用视频轨禁用(覆盖切集/切画质/自动连播)，播放列表自动连播保留；开启时视频画面叠黑底 + "听视频模式"指示、弹幕隐藏；新增 `ic_player_audio` 耳机图标。**v3.0.0-alpha.4**：后台播放自动连播修复——列表/播放列表后台播放完自动播下一个不再崩溃断链，`MobilePlayerScreen` `STATE_ENDED` 不再无条件 `stopService`，改安全 `startPlaybackService`(catch `IllegalStateException`)保持服务跨自动连播存活，仅列表播完(下一集为空)才停。**历史 tab 混合按播放时间倒序**：`MobileHistoryPage` 本地 YouTube 历史(viewAt=lastPlayedAtMs/1000)与 B 站历史(viewAt)合并 `sortedByDescending` 渲染、YouTube 绿框，未登录仍只显本地历史+登录提示。**动态 feed 卡片 B站动态样式+发布日期**：`MobileVideoCard` 加 `feedLayout` 参数(默认紧凑布局，首页/搜索/空间不变)，动态页置 true 用新布局——顶行作者块(头像40dp跨两行 + UP名一行 + 发布时间·播放量一行，发布时间用相对时间复用 `video_relative_*` 字符串)→ 缩略图独占整行 → 标题。**v3.0.0-alpha.5**：动态 feed 卡片改**单列**——`MobileDynamicScreen` 的 `LazyVerticalGrid` 用 `GridCells.Fixed(1)`,卡片占满整行,配 feedLayout B站动态样式全宽展示(不再分两列)。**动态 tab 点击刷新修复**：此前只有下拉才能同时刷新 B 站 + YouTube 关注,点击底栏「动态」只刷 B 站——根因是底栏点击仅对「推荐」tab 有 `recommendRefreshKey++`,「动态」tab 点击只 `selected = dest` 无刷新触发,YouTube 关注靠 10min 缓存 + 后台协程(切走即取消)易丢;新增 `dynamicRefreshKey`(每次点击「动态」含重复点击自增,镜像 `recommendRefreshKey` 与 TV `dynamicManualRefreshKey`),经 `MobileFeedScreen` 传入 `MobileDynamicScreen`,`LaunchedEffect(isLoggedIn, dynamicRefreshKey)` 驱动 `loadFirstBody()` 同时刷 B 站 + YouTube。**UP 主页缓存机制**：从 B 站空间/YouTube 频道点进视频退出播放器回到主页时不再重载——根因是空间/频道页状态全在 composable 局部 `remember`,从空间起播时 `spacePlaybackBehind=false`/`channelPlaybackBehind=false` 使显示门控把页面整体移出组合、局部状态销毁,返回时重组合重载;照 TV `UpSpaceUiState` 范式把状态提升到 shell——新建 `MobileUpSpaceUiState`(order/profile/followed/followLoading/state + `LazyGridState` 滚动位置 + `profileLoadedMid`/`videoLoadedMid`/`videoLoadedOrder` 守卫)与 `MobileYoutubeChannelUiState`(name/followLoading/items/continuation/loading/loadingMore/endReached/failed + `LazyGridState` + `loadedChannelId` 守卫),在 `MobileApp.kt` shell 层 `remember`,`LaunchedEffect` 加 loaded 守卫跳过重载;切不同 mid/channelId 或排序切换仍正常重载。**v3.0.0-alpha.8**：对齐 LibreTube 优化 YouTube 搜索与动态加载——①首页共享关注缓存:`MobileHomeScreen` 新增 `youtubeFeedCacheStore` 参数,`loadYoutubeTrending(forceRefresh)` 非强制刷新时读缓存(10min TTL)秒出、成功写回,与动态 tab 共享同一份 `YoutubeFeedCacheStore`,避免两处重复拉全量频道(对齐 LibreTube Home/Subscriptions 共享 feed 缓存);②动态请求去重:`MobileDynamicScreen` 新增 `feedJob` 守卫,`refreshFeed` 在已有拉取进行时跳过,重复点击底栏/下拉不并发重拉;③保留旧数据后台刷新:`loadFirstBody` 有旧 Success 时不闪 Loading,由 `isRefreshing` 驱动下拉指示器,失败保留旧数据兜底;④搜索请求去重/取消:`MobileSearchScreen` 新增 `searchJob`,`loadFirstPage`/`loadNextPage` 前取消在途请求,避免快速切 source/重复提交/重试竞态写 `resultState`(对齐 LibreTube mapLatest 取消旧请求)。**v3.0.0-alpha.9**：YouTube UP 头像完整实现(对齐 LibreTube)——根因是数据层硬编码空串(`toVideoSummary` 的 `ownerFace=""`、`parseVideoDetail` 的 `channelAvatarUrl=""`),UI 层即使写 `AsyncImage` 也永远走占位圆;①数据层:`YoutubeVideo` 加 `channelAvatarUrl`,`parseVideoRenderer` 解析 `channelThumbnail`(含 `channelThumbnailSupportedRenderers.channelThumbnailWithAvatarRenderer.avatar` 回退)选最高分辨率(对齐 LibreTube `maxByOrNull { it.height }`),评论头像从 `firstOrNull()` 改选最高分辨率,`parseChannelInfo` 返回类型扩展为 `ChannelInfo`(含 `avatarUrl`,从 `c4TabbedHeaderRenderer.avatar`/`channelMetadataRenderer.avatar` 取);②频道模型:`YoutubeChannel` 加 `avatar` 字段 + `updateAvatar`(DataStore 回写,`ignoreUnknownKeys` 旧数据安全);③动态关注 tab:`getSubscriptionsFeed` 对旧频道(avatar 空)懒解析一次 `/browse` 回填并 `onChannelAvatarResolved` 回调持久化,新频道 `resolveChannel` 已带头像,卡片 `ownerFace` 空时补所属频道头像;④图片加载:`buildOwnerAvatarRequest` 识别 yt3 URL 走裸 Coil 请求(不拼 B 站 CDN `@Nw.webp` 后缀、不加 B 站请求头,否则破坏 yt3 URL/B 站 Referer 被拒),B 站头像不受影响;⑤播放器简介页:`/player` 无频道头像字段,回退卡片 `request.ownerFace`。另加临时诊断:`MobileSmsWebViewPanel` 在 `onPageFinished` 注入 JS 把 passport-h5 可见 DOM 结构(标题/按钮/勾选框/输入框/正文)以 `BILI_DOM_DUMP` dump 到 console——先拿真实布局证据再设计干净布局,拿到后可移除。**v3.0.0-alpha.10**：短信登录页两处重叠修复——①顶栏从 `Box`+`align(TopCenter)` 覆盖改 `Column` 上下排,占自己高度不再盖网页顶部;②网页内"我已阅读并同意用户协议"叠登录按钮:诊断升级 dump 关键元素 `getBoundingClientRect`+`position/z-index` 并加轮询等 SPA(Vant)渲染完再抓,确认 `.explain-tips` 是 `position:absolute` 被固定定位到 y=291 叠在登录按钮(y=301-352)上重叠 ~29px,注入 CSS 把 `.explain-tips` 改 `position:static !important` 回到文档流排在按钮下方;WebView 加 `setUseWideViewPort`+`setLoadWithOverviewMode` 按屏幕宽度渲染(对重叠 2 无效但无副作用)。**v3.0.1-alpha.1**：YouTube 频道页两处修复——①首屏去重防 key 崩溃:真机日志 `IllegalArgumentException: Key "lfvQVSXuVOQ" was already used`(11 位 YouTube 视频 ID,非 B站 `BV`+10 位),根因 `loadFirst()` 直接 `uiState.items = page.items` 不去重而 `loadNext()` 有 `distinctBy { it.bvid }` 不一致,频道第一页同一视频 ID 重复出现时 `LazyVerticalGrid` 的 `key = { it.bvid }` 撞 key 崩溃;给 `loadFirst()` 也加 `distinctBy { it.bvid }`。②频道页头像补全:频道页视频走 `parseLockupViewModel`(新格式)不携带 `channelAvatarUrl`,卡片 `ownerFace` 恒空;`LaunchedEffect` 里 `resolveChannel` 已能解析出频道头像(`parseChannelInfo.avatarUrl`)但只用了 `.name`,改为存进 `uiState.avatar` 并在 `displayItems` 注入到视频 `ownerFace`(空时补本频道头像),一处覆盖首屏+翻页。**v3.0.1-alpha.2(对齐 LibreTube:YouTube 三项设置补齐)**：①TV 端「YouTube 默认画质」——核心层 `youtubeDefaultQuality` 早已支持(移动端设置已有),但 TV 设置界面缺行、TV 播放链路恒走 Auto;`SettingsScreen` 加 `SettingsItemYoutubeDefaultQuality=17` 行(插 quality 行后、codec 前,后续 lazyIndex 全 +1),`AppShell` 接线 `onYoutubeDefaultQualityChange` + `PlayerScreen` 补传 `youtubeDefaultQuality`;②「默认播放倍速」——TV 播放器倍速硬编码 `1.0f` 无持久化,新建 `DefaultPlaybackSpeed` enum(0.5~2.0 六档对齐 `PlayerOverlay.PlayerSpeedOptions`),`AppSettings`/`AppSettingsStore` 加 key/flow/setter,`SettingsScreen` 加行(常量 23,index 3),`PlayerScreen` 起播初始化 `mutableFloatStateOf(defaultPlaybackSpeed.value)`(L606 焦点计算 `PlayerSpeedOptions.indexOf ?: 2` 兼容非 1.0 默认);③YouTube 字幕接入(路线A WebVTT URL 直拉,不走 SABR 服务端)——`PlaybackInfo` 加 `subtitleTracks` 槽位、`PlaybackTrack` 加 `languageCode`,`YoutubePlaybackResolver` 把 NewPipe `info.subtitles`(fork 实际类型 `SubtitlesStream`,语言访问器 `getLanguageTag()` 无 `getLanguageCode()`,`Stream.getUrl()` 返 `String?` 需 `orEmpty()`)映射成 WebVTT `PlaybackTrack`(mimeType text/vtt)经 `NewPipeSabrResult` 透传进 `PlaybackInfo.subtitleTracks`;`PlayerScreen` 主源构建后用 `SubtitleExtractor`(media3 1.10 默认 `DefaultExtractorsFactory` 不含字幕,须显式传 `ExtractorsFactory`+`SubtitleExtractor`,对齐 LibreTube `OnlinePlayerService`)转 MEDIA3_CUES 再 `MergingMediaSource` 合并,PlayerView 内置 SubtitleView 自动渲染;字幕轨选择/语言切换 UI 后续迭代。**v3.0.1-alpha.3(TV 搜索初始界面接通 D-pad 焦点)**：源切换按钮(B站/YouTube)在搜索界面最顶部但 D-pad 焦点路径没连到它——初始界面焦点落在「清空」按钮上,只有左键回导航栏,无上键到源切换按钮的路径,遥控器永远够不到,切不了 B站/YouTube 搜索范围;输入框只是显示文本的 `Text` 无 `focusable()`,焦点落不到。修复:`SearchSourceToggle` 加 `FocusRequester`(键盘顶部上键可达、按钮下键回输入框),`SearchInputText` 加 `focusable()`+聚焦边框(上键回源切换、下键回键盘),`SearchKeyboardButton` 加 `onMoveUp`(清空按钮上键回输入框);焦点路径打通为 源切换按钮→输入框→键盘→搜索按钮。**v3.0.1-alpha.4(搜索源 pill 点击当前源循环切换)**：真机反馈「点击 BILIBILI 没反应」——双 pill 下 `selectSource` 有 `source == newSource` 守卫,默认就在 BILIBILI 上时再点 BILIBILI 直接 return 无响应。修复:`SearchSourceToggle` 点击时算 `targetSource`,若点击的 pill 已是当前源则取另一个源(与 `SourceBili` 比较),否则取点击的 pill——点 BILIBILI 恒切到 YOUTUBE、点 YOUTUBE 恒切到 BILIBILI,从任一源点任一 pill 都落到另一源。改动仅 `onClick` 一行逻辑,不涉及布局/焦点。**v3.0.1-alpha.5(TV 视频退出焦点恢复修复 + BiliMT:Focus 诊断日志)**：真机反馈「一退出视频就切到头像设置栏」——退出时 Android TV 把默认焦点分配给侧栏第一个可聚焦项(头像),靠 `TvVideoGrid` 恢复 effect 抢回原视频卡片;原 effect 在目标卡片首帧布局完成前就盲重试 `requestFocus`,退出卡顿(实测 ExoPlayer teardown + 首页重组 + 弹幕 draw 挤主线程,734ms Davey + 33 dropped frames)时连续失败,90 帧用完后 `onRestoreFocusHandled` 清掉 `playbackFocusRestoreDestination` → `suppressAccountAutoConfirm` 关闭 → 焦点留在头像。修复:`TvVideoGrid` 恢复 effect 重构——先 `scrollRow` 到目标行,再 `while` 轮询 `listState.layoutInfo.visibleItemsInfo` 直到目标行出现(最多 `TvGridRestoreFocusWaitLayoutFrames=90` 帧),最后 `repeat(TvGridRestoreFocusRetryCount=90)` 抢 `requestFocus`,把"按帧数盲重试"改成"等布局就位再抢";`AppShell` 兜底 `PlaybackFocusRestoreCleanupFrameCount` 120→240(必须 > 等 90 + 抢 90 = 180,避免兜底在恢复 effect 途中提前清 destination 致 suppress 关闭)。新增 `BiliMT:Focus` 日志贯穿退出恢复链(`onBack` 设置 restore/suppress、恢复 start/layout[waitedFrames/rowVisible]/success[attempt]/failed、backstop 兜底清理、`AccountNavItem.onFocused`[autoConfirm/suppress/openMyPage 状态]),实时日志 `logs_live.log` grep `BiliMT:Focus` 可直接看焦点落点与失败原因。改动文件 `TvVideoGrid.kt`/`AppShell.kt`/`AppSidebar.kt`,不涉及布局/焦点路径。**v3.0.1 稳定版发布(2026-08-14)**:v3.0.1-alpha 线提升为稳定版,合 mort_debug → main,打 `v3.0.1` 稳定 tag。核心积累:IPTV 完整接入(TV+移动端双端,源配置/频道列表侧栏/断流切源/强制 IPv4/缩略图拉流截帧 ImageReader→EGL 离屏)、TV 搜索栏 5 项优化、TV 历史合并本地 YouTube + 卡片绿框、YouTube 字幕/默认画质/倍速/gl-hl 地区设置、TV 设置焦点循环导航 + WebDAV 校验 + 视频退出焦点恢复。**v3.0.2-alpha.1(TV 长视频返回丢焦点 SaveableStateHolder 根治)**：真机发现长视频播放返回后焦点偶发落侧栏头像而非原视频卡片——根因播放时首页内容被 `if (visiblePlaybackRequest == null)` 卸载、返回时网格从零重建，`TvVideoGrid` 恢复 effect 要等目标行被懒布局组合出来才能 `requestFocus()`，长视频+滚得深+设备慢时等不到目标行就放弃、焦点落头像。修复用 Compose 官方 `SaveableStateHolder`：`BiliTvApp` 层 `rememberSaveableStateHolder`(永不卸载) + `SaveableStateProvider(selectedDestination.name)` 包住目的地 `when` 分支，播放时滚动位置存进 holder、返回时精确还原(含 offset)，目标行第一帧就在视口、焦点恢复立即成功;`LaunchedEffect` 切 tab 时 `removeState(旧key)` 丢弃(保留「切 tab 回顶部」现状);兜底放宽 `TvGridRestoreFocusWaitLayoutFrames` 90→360、`PlaybackFocusRestoreCleanupFrameCount` 240→600(列表本就定位在目标行,正常首帧即出现、循环即刻退出,大帧数仅慢布局兜底)。`when` 主体缩进未重排(免巨型 diff)。改动 `AppShell.kt`/`TvVideoGrid.kt`。**v3.0.2-alpha.2(发布构建合并为单一 universal APK)**：发布阶段 CI 原按硬件架构拆成 2 个 APK(`arm64-v8a`/`armeabi-v7a`)，每个都同时含 TV+手机 UI，但 TV 盒子用户要自己挑 ABI、下错装不上。改 `.github/workflows/android-build.yml` "Build release APKs" 步骤——去掉按 ABI 的 for 循环，单次 `assembleRelease`(不带 `-PtargetAbi`，默认编 `supportedAbis` 双 ABI)产出一个 `BiliMT-<tag>.apk` universal 包，一个包同时适配 TV/手机/两种盒子架构；"Create Release" 的 `release-apks/*.apk` 通配照常命中无需改。`app/build.gradle.kts` 的 `targetAbi` property 保留(默认已是双 ABI，仍支持本地单 ABI 调试)，debug 构建本来就是 universal 不动，RELEASE_NOTES 历史条目是已发布资产记录不动。README L253 同步为「发布构建单一 universal release APK」。**v3.0.2-alpha.3(YouTube 订阅流全面修复排序与完整性 + TV 热门 tab 对齐关注动态)**：移动端「动态」合并 YouTube 关注排序不对+不全,识别 5 根因逐一修复——①`getSubscriptionsFeed` 从「RSS 优先、RSS 非空跳过 InnerTube」改成**每频道 RSS 与 InnerTube 并行拉取后按 videoId 合并**(RSS 提供精确 `publishedAt`,InnerTube 补全 `duration`/`liveNow`/`isUpcoming`/`badge`/`viewCount`/头像;任一路失败降级用另一路,修复 RSS 路径丢 Shorts/直播/首映、duration 全 0);②`perChannel` 8→15 对齐 RSS 实际返回量,不再静默丢第 9-15 条;③`toVideoSummary` `pubdate` 从 `?:0L` 兜底改为当前时间,直播/首映/解析异常视频不再沉底 1970 年;④`YoutubeRssParser` 解析失败 + `YoutubeFeed`(RSS/InnerTube 拉取失败/合并统计)+ `YoutubeRSS`(日期解析失败)日志,零可观测性打通;⑤TV 首页「YouTube 热门」tab 对齐关注动态——`VideoRepository.getHomeSectionVideos(YoutubeTrending)` 从拉 `youtubeTrending` 改为拉 `youtubeSubscriptionsFeed`(读 `YoutubeChannelStore` 关注频道,`withTimeoutOrNull(youtubeFeedTimeoutMs)` 兜底,未关注/超时返回空),与移动端首页/动态一致;移除已无调用的 `youtubeTrending` 死代码,`AppContainer` 给 `VideoRepository` 注入 `YoutubeChannelStore`。**v3.0.2-alpha.4(TV 视频退出焦点被头像抢占根治)**：真机日志 `logs_live.log` 实锤每次退出视频 `restore success` 后 ~250~300ms 又出现 `avatar focused ... openMyPage=true`——恢复 effect 成功把焦点拉回视频卡片,但 Compose 焦点系统在内容从 `SaveableStateHolder` 还原后做了一次「延迟焦点回落」把焦点落到侧栏第一个可聚焦节点(头像);根因是现有 `suppressAccountAutoConfirm` 只抑制头像 autoConfirm(不打开「我的」页)没阻止头像「接收焦点」,且抑制在 `restore success` 那一刻就被 `clearFocusRestoreRequest` 撤掉、比延迟回落早。修复两处:①`BiliFocusableSurface` 加 `enabled` 参数,false 时在 `focusable()`/`clickable()` 之后加 `focusProperties { canFocus = false }` 让节点真正不可聚焦;②`AppSidebar` 抑制时头像+导航项传 `enabled = !suppressAccountAutoConfirm`,延迟焦点回落无处可落只能留在视频卡片;③`AppShell` `clearFocusRestoreRequest` 从立即清 `playbackFocusRestoreDestination` 改成延迟 400ms 清(新增 `PlaybackFocusRestoreSuppressHoldMs`),覆盖 ~250~300ms 延迟回落,延迟清不会让恢复 effect 重跑、backstop(600 帧≈10s)也远长于 400ms 不会误清。改动 `BiliFocusableSurface.kt`/`AppSidebar.kt`/`AppShell.kt`。**v3.0.2-alpha.11(YouTube SABR NewPipe 路径 UA 与 visionOS clientInfo 不匹配 → RELOAD 死循环)**：真机 2026-08-16 两次播放，第一次 `qPR91wHXPvo` 成功(走 SABR session reuse，旧会话 UA 正确)，第二次 `jNl6YkkzKxw` 失败——NewPipe 新 harvest 会话首 fetch 即 `RELOAD_PLAYER_RESPONSE`，服务端全拒，count 1→…→8 死循环，中途 visionOS reload 重打 /player 也失败(`no serverAbrStreamingUrl/ustreamerConfig`)。根因 [YoutubePlaybackResolver.kt](app/src/main/java/com/kirin/mt/core/youtube/YoutubePlaybackResolver.kt) NewPipe 常规路径 `SabrSession.fromSabrData` 的 `userAgent` 用了 `Client.WEB.userAgent`(`Mozilla/5.0 ... Pixel 7`)，而 protobuf `ClientInfo` 与 `ustreamerConfig` 都是 visionOS(alpha.71 起 `visionOsSabrClientInfo()`)，服务端判客户端标识不一致 → RELOAD 全拒；对照 visionOS reload 路径 UA 已用对 `Client.VISION_OS.userAgent`。修复：NewPipe 路径 UA 改 `Client.VISION_OS.userAgent`(对齐 LibreTube SabrClient 全 visionOS)。改动仅 1 行。**alpha.79 补：UA 单独不够——还得摘 WEB cookie/visitor**。真机验证 alpha.78 UA 已对齐(`ua=com.google.visionos.youtube/...`)但同一视频 `jNl6YkkzKxw` **仍无限 RELOAD**——因为 HTTP 请求仍带 `Cookie`(555B)+`X-Goog-Visitor-Id`(11B) 的 WEB(Android Chrome)会话头,与 visionOS protobuf 身份冲突。彻底对齐 LibreTube:resolver 两条路径(NewPipe+visionOS reload)`cookieHeader`/`visitorData` 改空串,`SabrClient`/`SabrMediaFetcher` 的 `Cookie`/`X-Goog-Visitor-Id` 头条件化(`isNotBlank` 非空才带),会话身份全靠 protobuf(poToken/ustreamerConfig/playbackCookie)。**v3.0.2-alpha.13(移植 LibreTube NewPipe 原生 PoTokenGenerator → 修 visionOS SABR RELOAD 根因)**：alpha.79 后真机 2026-08-16 同一视频 `jNl6YkkzKxw` **仍无限 RELOAD**——UA(alpha.78)+ 摘 cookie/visitor(alpha.79)都对齐 LibreTube 后,逐字段对比 HTTP 头 + 请求体(ClientInfo 6 字段/ClientAbrState/StreamerContext)+ sabrUrl 全部一致,剩余唯一差异 = **poToken**。根因定论:BiliTV 的 poToken 是 resolve-minted 的 **BotGuard WEB token**(FreeTube bgutils-js),其 `buildContentBinding` 是 **PLACEHOLDER 占位**(`b=PLACEHOLDER&hh=PLACEHOLDER`);且 NewPipe visionOS getInfo 调 `getIosClientPoToken` → 原 `BiliTvPoTokenProvider` 返回 null → 缓存恒空 → resolver 回退 resolve-minted token。同一枚 token 对 WEB /player 能过、对 visionOS SABR 被拒(服务端按 contentBinding 严格校验)→ RELOAD 全拒。LibreTube 能播是因为其 `SabrClient` 显式用 NewPipe 原生 `PoTokenGenerator`(内部 `PoTokenWebView` + `po_token.html`)铸 contentBinding 正确的 token。**修复 = 移植 LibreTube 的 NewPipe 原生 PoTokenGenerator**(两项目 fork 同 `738c3d4`,API 全可用)：①复制 `po_token.html` asset；②移植 `PoTokenWebView.kt`(WebView 加载 po_token.html 执行 BotGuard JS,OkHttp 直发 jnn Create/GenerateIT,头对齐 LibreTube 5 头、不带 cookie/visitor——实证 LibreTube 无 cookie jar 照样铸出正确 token)；③移植 `NewPipePoTokenGenerator.kt` 实现 `PoTokenProvider`(`getVisitorDataFromInnertube` + `ofWebClient` 铸 WEB token),加 `cached()`/`ensureWebToken(videoId)`,`getIosClientPoToken` 不再 null 改铸 WEB token 填缓存；④AppContainer/NewPipeInit/resolver 换用新 provider,resolver `buildSabrSessionFromNewPipe` 读 token 前先 `ensureWebToken(videoId)` 兜底。这样 visionOS getInfo 也填缓存,resolver 拿到 NewPipe 原生铸的 contentBinding 正确 token,不再回退 resolve-minted PLACEHOLDER。旧 `YoutubeBotGuard`/`BiliTvPoTokenProvider` 保留(/player 仍走 BotGuard)。**v3.0.2-alpha.14(推翻 alpha.13 定论——visionOS SABR 请求**不带 poToken**,对齐 LibreTube)**：alpha.13 移植 NewPipe 原生 PoTokenGenerator 后真机 2026-08-16 同一视频 `jNl6YkkzKxw` **仍无限 RELOAD**(日志 `poToken=120B(newpipe-native)` 坐实新 token 已用上)。逐层对比 LibreTube 与 BiliTV 的 SABR 请求,唯一实质差异 = **poToken**:LibreTube `SabrClient` 的 `getCachedWebClientPoToken()` 在 getInfo 期间**恒空**——其 `PoTokenGenerator.getIosClientPoToken` 返回 null(NewPipe fork getInfo 只在 `fetchIosClient` 调 `getIosClientPoToken`,见 yse.java:879),故首请求 `setPoToken(ByteString.empty())` **不带 poToken 且能播**;BiliTV 移植时把 `getIosClientPoToken` 改成铸 WEB token 填缓存,getInfo 期间缓存被填满,`ensureWebToken` 拿到 120B token 塞进 SABR 请求 → 服务端按会话 visitor 不匹配(WEB-visitor 铸的 poToken vs visionOS /player 会话)RELOAD 全拒。**根因不是 contentBinding(alpha.13 定论错),而是任何 poToken 都不该发**。修复:resolver 两条路径(NewPipe + visionOS reload)`SabrSession.fromSabrData` 的 poToken 改空串,不再 `ensureWebToken`/回退 resolve-minted,poToken 空也不回退;getInfo 仍走 NewPipe 铸 token(供 iOS /player 请求),但 SABR 请求不依赖它。诊断:①`SabrMediaFetcher` RELOAD 日志加 `potSent=${poTokenState.currentPoToken.size}B`(修复后若还 RELOAD,`potSent=0B` 直接证明不是 poToken 的锅);②resolver 会话日志显式 `poToken=0B(aligned-LibreTube-no-poToken)`;③复用 `SabrSession:` 日志 `poToken=0B` + fetch 日志 `pot=0B body=1728B`(比 1848B 小 ~120B)自动确认 poToken 已摘。**v3.0.2-alpha.15(推翻 alpha.14 定论——根因是 cpn 不匹配,不是 poToken)**：alpha.14 摘 poToken 后真机 2026-08-16 同一视频 `jNl6YkkzKxw`(2160p itag313)**仍无限 RELOAD**(日志 `potSent=0B` 坐实 poToken 无关),但 `3wO2mW3eylw`(1080p itag137)能播;用户确认 **LibreTube 能播 jNl6YkkzKxw**。逐层对比 LibreTube 与 BiliTV 的 SABR 请求,唯一实质差异 = **cpn**:LibreTube 直接用 NewPipe 原始 sabrUrl(保留 `&cpn=<visionOsCpn>`),而 ustreamerConfig 来自同一 visionOS /player 响应、**绑定 visionOsCpn**(fork `YoutubeStreamExtractor.java:713-714` 把 `&cpn=` 追加到 serverAbrStreamingUrl);BiliTV 把 cpn strip 掉、`fromSabrData` 生成**随机 cpn** → SABR 请求 cpn 与 ustreamerConfig 绑定不匹配 → 服务端拒会话 RELOAD。3wO2mW3eylw 能播因服务端对 1080p 不强制校验 cpn,2160p 严格校验。**修复**:resolver 两条路径(NewPipe + visionOS reload)新增 `queryParam` 提取 `visionOsCpn` 传给 `fromSabrData` 的 `cpn` 参数,不再用随机 cpn。诊断:会话日志加 `cpn=${visionOsCpn ?: "random"}` 确认修复生效。另发现唯一请求体差异 = `ClientAbrState.audioTrackId`(field69,LibreTube 设、BiliTV 未设),jNl6YkkzKxw 有 5 音轨,若 cpn 修复后仍 RELOAD 则补该字段。**alpha.92**:复活自合成 DASH 主兜底——调研 LibreTube 确认其 DASH 是 `createDashSource` 自合成 MPD(非拉 manifest),用 NewPipe 流顶层 `content`+`initStart/indexStart` 拼 SegmentBase;当初 Phase 2 放弃基于诊断误判(itagItem.toString 看不出字段,实则在 stream 顶层,同 fork 738c3d4 可读)。`buildDashFallbackFromNewPipe` 优先级改自合成 DASH(range 非空)→ dashMpdUrl → HLS,播放器零改动复用 buildDashManifest→DashMediaSource,带 range 守卫零回归(见 docs/youtube-hd-playback.md row 100)。**YouTube 评论展示(对齐 LibreTube)**：移动端已有基础版评论列表,TV 端 `CommentScreen` 只支持 B 站(aid),YouTube 视频 aid=0 无法看评论。本次补齐——①模型 `YoutubeComment` 加 LibreTube 对齐字段(verified 认证/pinned 置顶/hearted 作者点赞/replyCount 回复数/repliesPage 楼中楼 token/channelOwner 频道主/creatorReplied 作者回复,均带默认值);②`parseCommentRenderer` 按 NewPipe JSON key 提取(authorCommentBadge/pinnedCommentBadge/actionButtons.commentActionButtonsRenderer.creatorHeart/replyCount/replies.commentRepliesRenderer 内 continuation/authorIsChannelOwner/viewRepliesCreatorThumbnail);③TV `CommentRequest` 加 source+videoId,`CommentScreen` 按 source 分支——B 站走原 aid 流程,YouTube 走 `/next`+continuation 续页(焦点驱动翻页),新增 `YoutubeCommentList`/`YoutubeCommentItem` 渲染认证✓/置顶📌/作者点赞❤/回复数;④TV feed 长按菜单「查看评论」对 YouTube 视频启用(`video.aid>0 || source==Youtube`),AppShell 传 source/videoId;⑤移动端 `MobileYoutubeCommentList` 不再映射成 B 站 `Comment` 丢字段,直接渲染 `YoutubeComment` 显示增强字段。**v3.0.3-alpha.2(TV 动态长按操作菜单显式焦点陷阱)**：alpha.1 把 `BiliActionSheet` 改屏内覆盖层后,真机反馈按上下键丢焦点——覆盖层只在首/末项拦截方向键,中间项放行给 Compose 默认焦点遍历,遍历会解析到弹窗背后网格卡片,焦点逃逸到网格、高亮消失。修复:`BiliActionSheet` 改显式焦点陷阱——每项各自 `FocusRequester`、上下/左右键全消费并手动在启用项间移焦(`moveSheetFocus` 越过禁用项、到边界即停),Back 交回外层 `BackHandler`,不再依赖默认遍历,焦点锁死在菜单内。改动仅 `BiliActionSheet.kt`。**订阅流续页 token 提取修复(debug)**：TV/移动端 YouTube 主页订阅流滑到底要么卡 ~50 条(跨频道去重后首屏量)、要么死循环——根因 `YoutubeParsers.findContinuation` 全树 `collectByKey` 扫 + 取最后一个 continuationItemRenderer，频道视频 tab `/browse` 响应夹带 shorts/相关频道等其它 section 的 token，取错后续页返回首屏相同视频 → 去重零新增不推进。对齐 NewPipe `YoutubeChannelTabExtractor`(E:\GITHUB\NewPipeExtractor)改三段式：续页响应读 `onResponseReceivedActions[].appendContinuationItemsAction.continuationItems`、首屏定位视频网格 grid/richGrid、都取第一个(无网格回退全树第一个)；另给 `RecommendScreen` 与移动 `HomeScreen` 的 YoutubeTrending 续页补 `merged.size==current.videos.size` 护栏。详见 docs/youtube-api-notes.md §4.10.2。**v3.0.5-alpha.1(YouTube SABR 自动档升档修复)**：真机反馈自动模式卡最低档(360p/itag243)不升档、手动切档正常——根因 SABR 自合成 DASH 清单里 H264+VP9 混合 mime 组被 `DefaultTrackSelector` 默认策略(`allowMixedMimeTypesAdaptiveness=false`)坍缩成单轨,自适应组只剩一条轨无档可升。修复两处：①`Representation.fromTrack` 每条轨 `Format.Builder().setId(formatId.itag)`(此前 id 全 null 破坏轨身份致组构建坍缩);②`PlayerScreen`/`MobilePlayerScreen` 显式挂 `DefaultTrackSelector` 开 `setAllowVideoMixedMimeTypeAdaptiveness`+`setAllowVideoNonSeamlessAdaptiveness`+`setAllowMultipleAdaptiveSelections`。真机实测 `trackSelLen=5`、自动 144p→480p→1080p 完整升档 ~8s。详见 docs/youtube-hd-playback.md §6.19。**v3.0.5-alpha.2(移动端 YouTube 详情补发布时间,真因)**:上一版 §6.20 只补了简介 Tab 回退卡片 pubdate,但历史页进入仍无日期。深挖坐实**三处叠加**：①`parsePublishDate` 对 ISO-8601(`2023-01-15T00:00:00-08:00`)做 `split('-')` 期望纯 `yyyy-MM-dd`,`T`+时区使 `toIntOrNull()` 失败→恒 null(字段明明在,解析成 null,`getVideoDetail mf` 诊断坐实)；②历史页 `YoutubeHistoryEntry` 原本**无 pubdate 字段**且 `toVideoSummary()` 硬编码 `pubdate=0`,`recordPlay` 也没存→历史视频 `request.pubdate=0`；③简介 Tab 只读 `publishedAt`。修复:①`parsePublishDate` 先 `take(10)` 取日期部分兼容 ISO + `uploadDate` 兜底；②`YoutubeHistoryEntry` 加 `pubdate` 字段,TV/移动两处 `recordPlay` 填 `request.pubdate`,`toVideoSummary()` 改 `pubdate`；③`MobileYoutubeIntroTab` 优先 `publishedAt`、缺省回退 `request.pubdate`。曾试 NewPipe `getInfo` 兜底(历史页抛 null 异常,真机证伪),解析器修好后从已有 `/player` 直接取,不引入二次 getInfo。详见 docs/youtube-hd-playback.md §6.20。

**YouTube 搜索排序对齐 B 站 4 项（已入 v3.0.5-alpha.6）**：真机/体验上 YouTube 搜索恒「综合排序」无法按播放量/时间排——`YoutubeSearchParams` 排序参数串（Relevance/ViewCount/UploadDate/Rating）早在早期就定义但 UI 一直没接，`youtubeSearch` 门面/调用只传 query 不传 params。本次两端排序条放开到 YouTube 源：`SearchScreen`（TV）与 `MobileSearchScreen`（移动）各拆 `BiliSearchSortOptions`/`YoutubeSearchSortOptions` 两套（key 即 InnerTube search params，Relevance 空串=默认综合），加 `sortOptionsFor(source)`/`defaultOrderKey(source)`；`selectSource`/`resetResultsForQuery` 切源时把选中 key 重置为该源默认「综合」，避免 B 站 `order` 值（totalrank/click…）混进 YouTube `params`；YouTube 分支 `youtubeSearch` 补传 `params = selectedOrderKey`（此前只传 query）。4 项：综合(Relevance=``)/最多播放(ViewCount=`CAMSAhAB`)/最新发布(UploadDate=`CAISAhoA`)/最多弹幕(Rating=`CAMSBBABGAE`)，复用 `search_sort_*` 文案。TV 排序焦点 map 改 `remember(uiState.source)` 重建。云编译经 1 轮修（首轮 `remember(source)` 引用未声明的局部 `source` 编译失败，改 `uiState.source`）后绿。

**TV 备份/还原选择弹窗崩溃修复 + 日志分享面板加备份（已入 v3.0.5-alpha.6）**：真机「设置→备份」一点就崩、弹窗未弹、无本地日志。崩溃日志定位 Compose 布局异常 `Vertically scrollable component was measured with an infinity maximum height constraints`——`SettingsWebDavSelectionDialog` 的 Column 带 `verticalScroll` 却没给有限 max 高度，弹窗在无限高约束下测量滚动容器直接抛 `IllegalStateException`（此前误判 material3 Checkbox 原生渲染崩溃，实为布局约束）。修复：Column modifier 在 `verticalScroll` 前补 `.heightIn(max=680.dp)`，与正常工作的 WebDav 配置弹窗一致。随行按用户要求给 TV 日志分享面板每个日志文件行加「备份」按钮——`WebDavBackupService.backupLogFile`（先 `mkcol bilitv/logs` 再 `put`，只上传不删本地），`SettingsLogPanel`/`SettingsScreen`/`AppShell` 接线。另随行：暂停状态 5s 无操作控制栏也自动隐藏（TV 与移动全屏同步，播放结束保持常显供重播/下一集）。云编译绿。

**离线下载播放器对齐在线播放器（当前分支，未打 tag）**：`MobileOfflinePlayerScreen` 从极简裸 `ExoPlayer.Builder().build()` + 本地文件直喂（仅 play/pause/seek/back）升级为功能对齐在线播放器 `MobilePlayerScreen`——①后台播放+通知：抽公共 `startPlayback` 到 `core/player/PlaybackServiceHelper.kt`（在线/离线共用，捕获 `IllegalStateException` 防后台启动崩溃），`PlayerHolder.player/title` 绑定 + 播放时启动 `PlaybackService` + `MediaMetadata.setTitle`；②进度保存/续播：注入 `playbackRepository`，复用在线 Room `progressStore`（在线/离线互通），进入时 `getSavedProgress` seek 续播、退出/每 15s `saveProgress`（positionMs 同步读入避免 onDispose 竞态）；③倍速：`playbackSpeed` state + `ModalBottomSheet` 七档（0.5~3.0，对齐在线 `PlaybackSpeedOptions`）；④画面手势：复用同包 `detectPlayerGestures`——中央点击暂停/暂停、边缘点击切控件、横向拖拽 seek（`seekPreviewMs` 预览 + 松手 seekTo + 播放中恢复播放）；⑤全屏/非全屏两种布局：全屏 `weight(1f)` 铺满 + 沉浸（`requestedOrientation=SENSOR` + 隐藏系统栏，`DisposableEffect` 退出恢复），非全屏 16:9 顶部 + 下方"已下载视频"列表；⑥相关视频=离线缓存列表：`downloadManager.downloads` 过滤当前项 + `COMPLETED`，2 列 `MobileVideoCard`（`coverOverride` 本地封面文件），点击切 `currentDownloadId` 重建 player（`AndroidView update` 重新绑定）；⑦ExoPlayer 配置对齐：`createTvPlaybackLoadControl()` + `setHandleAudioBecomingNoisy(true)` + AudioAttributes（USAGE_MEDIA/MOVIE）。数据源仍 `DefaultDataSource` 本地文件（无网络 headers/DRM/SABR，合理差异）。改动 `MobileOfflinePlayerScreen.kt`（重写）/`MobilePlayerScreen.kt`（删私有版加 import）/`SettingsActivity.kt`（注入 playbackRepository）/新建 `PlaybackServiceHelper.kt`。**布局微调（真机反馈"视频区置顶"）**：非全屏视频区原 `aspectRatio(16f/9f)` 紧贴状态栏顶部，改视频区 `weight(1f)` 占满剩余、视频画面区 16:9 垂直居中（上下黑边，`videoFrameModifier = if(fullscreen) fillMaxSize else fillMaxSize().aspectRatio(16f/9f).align(Center)`），下方已下载视频列表占下半，对齐在线播放器非全屏播放布局。已云编译绿，待真机测试。**底栏对齐在线（alpha.6）**：去掉进度条下方独立的播放/暂停按钮行，底栏收敛为单行（时间 + 进度条 + 时长），与在线播放器一致；播放/暂停仍走画面中央点击。改动 `MobileOfflinePlayerScreen.kt`，已云编译绿。

**B站视频下载 playurl -400 修复（当前分支，未打 tag）**：真机日志 `resolveBili 失败(bvid=... cid=0 qn=116): Bilibili playurl failed with code -400`——B站视频无法下载但能播放。根因**非防盗链/非 qn，而是 cid=0**：`MobilePlayerScreen` 的 `activeRequest` 只在 `loadRequest` 开头赋一次 `activeRequest = request`（原始请求 cid 通常为 0），之后从未更新成带真实 cid 的 `resolvedRequest`。播放内部走 `resolvedRequest`（cid 已解析）所以能播；但下载对话框 `downloadManager.enqueue(activeRequest, ...)` 用 `activeRequest`，把 `cid=0` 带进 playurl → B站 `code -400 请求错误`（与 qn 无关，内部已按修复请求 qn=127 仍 -400）。**修复：** loadRequest 解析出 `resolvedRequest` 后立即 `activeRequest = resolvedRequest` 回写，让下载/相关/分享等复用 activeRequest 的地方都带真实 cid。顺带之前修的：`DownloadUrlResolver` 下载不再强制用户所选 qn 去请求 playurl（B站对 `fourk=1 + 低 qn` 组合也 -400），恒用 qn=127（Highest，与播放路径一致）取全量轨再按所选清晰度高度挑，对齐 BV 官方客户端（VideoPlayRepository 恒 `qn=127/fnval=4048/fourk=1`，从返回轨客户端挑）。改动 `MobilePlayerScreen.kt`/`DownloadUrlResolver.kt`。已推云编译待验证，待真机测试。

**在线播放器命中缓存播本地源（当前分支，未打 tag）**：在线刷到已下载视频，点进去视频区直接播本地缓存文件，简介/评论/弹幕仍走在线，与在线播放完全一致。B站 + YouTube 都合并。**方案：** `MobilePlayerScreen.loadRequest` 在 `getPlaybackInfo` 网络解析前查缓存命中（`downloadManager.downloads.first()` 按 `videoId + cid` 双匹配 + `isPlayable`），命中 → 自包含早返回块：用 `playbackFiles(id)` 建本地 `Progressive/MergingMediaSource`（镜像离线播放器），跳过网络取流；元数据/简介/评论/弹幕照常在线加载（metadata/youtubeDetail/弹幕本就在 loadRequest 独立加载，与视频源无关）。**决策（用户定）：** 只做手机端；命中即跳过网络解析；清晰度只读（命中时 HD 按钮+菜单换静态文字显示缓存 `qualityLabel`，不可切换）；已缓存即隐藏下载入口。**边界：** 仅 `isPlayable`（媒体分件全 COMPLETED）命中，部分下载回落在线；`videoId+cid` 双匹配避免多 P 视频误用其它分P缓存；进度续播复用 `saveProgress/getSavedProgress`（bvid/cid 正确，与在线互通）。改动 `MobilePlayerScreen.kt`（新增 `findCachedPlayable`/`buildCachedPlaybackInfo`/`buildLocalMediaSource` 三个私有函数 + loadRequest 缓存分支 + 清晰度只读 + 隐藏下载入口）。已云编译绿，待真机测试。

**移除播放列表 ◀▶ 按钮，根治缓存视频点清晰度误触切下一集（v3.0.6-alpha.7）**：真机对比 debug/release——release 从下载播放列表进入缓存视频时，点"清晰度"位置却切到下一集（缓存 360p 点成 480p）。**根因：** 非 debug/release 差异，而是**上下文差异**——播放列表（多集队列）在进度条行渲染「▶下一个」按钮，它紧挨缓存清晰度静态文字；用户点"清晰度"实际落在 ▶ 上，触发 `loadRequest(playQueue[cur+1])` 经 AirJump 加载下一个视频（日志证实：无「点清晰度」日志、无缓存未命中、走 AirJump loaded segments 下一个 bvid）。单视频（debug 测试场景）无 ▶ 所以点不动。**方案：** 移除播放列表 ◀▶ 按钮（`MobilePlayerScreen` 进度条行），与在线播放器对齐，只留清晰度/音轨入口；`playQueue` 自动连播/相关视频切换逻辑保留（非按钮触发）。改动 `MobilePlayerScreen.kt`。已云编译绿，待真机测试。

**离线下载列表交互对齐卡片页（当前分支，未打 tag）**：`MobileDownloadsScreen` 下载卡片与其它卡片页统一交互——卡片主体由普通布局改 `combinedClickable`：**单击播放**（`isPlayable` 可播时，未完成/失败 no-op）、**长按弹底部操作菜单**（`DownloadActionsSheet`，样式对齐 `MobileYoutubeLongPressSheet`）。长按菜单内容：标题 + YouTube 源「加入播放列表」（复用 `MobilePlaylistPickerDialog`，`DownloadWithItems.toVideoSummary()` 仿离线播放器相关视频构造）+ 全部源「删除」（`downloadManager.delete`）。右侧按钮列**瘦身只保留下载控制**：下载中/排队→暂停/取消，暂停/失败→续传，完成/取消→无按钮（播放走单击、删除走长按）。`SettingsActivity` 给 screen 注入 `appContainer.youtubePlaylistStore`。改动 `MobileDownloadsScreen.kt`/`SettingsActivity.kt`。已推云编译待验证，待真机测试。

**下载管理批量删除（当前分支，未打 tag）**：下载管理右上角加**三点 `⋮`**（`Icons.Filled.MoreVert`）进入**批量管理模式**——`SettingsTopBar` 加可选 `trailing` 槽，非批量显示三点图标、批量显示「完成」文字按钮。批量模式交互（用户确认样例）：卡片前出现 `Checkbox`，点卡片=勾选/取消（关闭播放/长按），底部固定操作栏 `DownloadBatchBar` 显示「已选 N」+「全选」（全选切换用 `onSetSelection`）+「删除所选」（enabled=N>0，红色）；点删除弹 `AlertDialog` 二次确认（`downloads_batch_confirm_message` 带 N 项）→ 逐条 `downloadManager.delete(id)`（删本地文件+DB 行）并退出模式。`SettingsActivity` 持有 `downloadsBatchMode`+`selectedDownloadIds`（`Set<Long>`），注入 `MobileDownloadsScreen`（`batchMode`/`selectedIds`/`onToggleSelection`/`onSetSelection`/`onDeleteSelected` 五新参，均带默认值兼容）。改动 `MobileDownloadsScreen.kt`/`SettingsActivity.kt`/3 份 strings.xml。已推云编译待验证，待真机测试。

**检查更新可重新检查（当前分支，未打 tag）**：真机反馈「检查到更新没下载，就一直显示那次更新，之后可能又有新版本却无法重新检查」——`Available` 状态点击恒触发下载，无重新检查入口。修复（用户确认样例：点击重新检查，仍最新则转下载）：`UpdateUiState.Status.Available` 加 `rechecked: Boolean = false` 标记；`UpdateManager.refresh()` 从 `Available` 发起（用户主动重新检查）时 `rechecked=true`，从 Idle/UpToDate/Failed 发起（首次检查）时 `rechecked=false`；下载失败回 `Available(rechecked=true)` 让按钮保持「下载更新」可重试。UI 分派（TV `SettingsScreen` + 移动 `MobileSettingsScreen` 同步）：`Available` 且 `!rechecked` → 点击重新检查（`refresh()`），`Available` 且 `rechecked` → 点击下载；`downloadOrInstallLabel` 对应显示「检查更新」/「下载更新」。改动 `UpdateUiState.kt`/`UpdateManager.kt`/`UpdateSettingsState.kt`/`SettingsScreen.kt`/`MobileSettingsScreen.kt`。待云编译验证。

**下载对话框补「音频」「480p」选项（当前分支，未打 tag）**：B站下载对话框补强制「480P」行 + 「音频(仅音频)」行；YouTube 下载对话框补低档 480p/360p + 「音频(仅音频)」行。纯音频 = 仅下音频轨(`ResolvedDownload.video=null` 只留 `audio`,`enqueueOnIo` 只插 AUDIO 分件),离线播放器补纯音频分支(videoFile==null 但 audioFile 存在时播音频)。详见下方本任务实现段。

**播放列表详情批量移除（当前分支，未打 tag）**：`PlaylistDetailScreen`（`MobileYoutubePlaylistPage.kt`）编辑模式（顶栏「编辑/完成」）升级为**批量移除**，对齐下载批量删除——每卡前置 `Checkbox`（勾选/取消，卡点也切）、底部固定操作栏 `PlaylistBatchBar`（「已选 N」+「全选」+「删除所选」，删除弹 `AlertDialog` 二次确认 → `store.removeVideos` 一次性过滤移除 + toast + 清选中）；**保留**卡片右侧单「移除」按钮（单选即时删，同时从选中集剔除）；「完成」退出编辑清空选中。`YoutubePlaylistStore` 加批量 `removeVideos(playlistName, ids)`（单次写盘）。批量字符串仅加 `values/strings.xml`（简体，playlist 现有串本来只在那份）。改动 `MobileYoutubePlaylistPage.kt`/`YoutubePlaylistStore.kt`/`values/strings.xml`。待推云编译，待真机测试。

**播放列表长按拖动排序修复（v3.0.6-alpha.13）**：补全长按拖拽排序的标准拖拽骨架两处——①**边缘 auto-scroll**：原实现无 auto-scroll，拖到视口顶/底后没有更上/下可见项，拖拽项被钉进顶/底槽「自动填入」无法拖出当前画面（真机：拖到前两行就拖不动）；`onDrag` 用拖拽带（初始 offset + 累计位移，viewport 固定坐标）对比 `viewportStartOffset/EndOffset`，越出顶/底即 `listState.scrollBy(overscroll)`（`scrollBy` 是 `ScrollableState` 扩展，须 `import androidx.compose.foundation.gestures.scrollBy`），单 `overscrollJob` 防堆叠、越界量回 0 即 cancel，`scrollBy` 自带首/末项边界 clamp。②**目标卡方向邻位判定**：原用「最近中心」选目标，顶部拖拽时手指未离原槽、最近中心恒钉回原圈，致最上面视频拖不下去（换一格就被弹回）；改方向邻位——向下只看 `from+1`、向上只看 `from-1`，拖过其中心才换一格，单调反向不弹回，顶/底都能逐格拖。改动 `MobileYoutubePlaylistPage.kt`（onDrag）。云编译绿，待真机测试。

推荐下一项：**v3.0.2-alpha.4 真机验证(TV 视频退出焦点被头像抢占根治)**：装 debug 版后退出一次视频,①焦点应稳定停在原视频卡片(不跳头像、不打开「我的」页);②实时日志 `Y:\download\bilitv\logs\logs_live.log` grep `BiliMT:Focus` 应见 `restore success` 后**不再出现** `avatar focused`(之前每次都有);③回归正常侧栏导航(头像/导航项聚焦、autoConfirm 打开「我的」页)不受影响。验证通过后回 P1 队列与稳定版短信登录页修复验证。**v3.0.1-alpha.5 真机验证（TV 视频退出焦点）**：装 debug 版后在 BRAVIA 退出一次视频,①焦点回到原视频卡片(而非头像);②实时日志 `Y:\download\bilitv\logs\logs_live.log` grep `BiliMT:Focus` 应见 `video exit` → `restore start` → `restore layout: rowVisible=true` → `restore success`,不见 `restore failed`/`backstop cleared`/`avatar focused ... openMyPage=true`。若仍失败,日志会指明是"行没布局出来(rowVisible=false)"还是"requestFocus 一直失败"再精准下一刀。随后回 v3.0.1-alpha.3/alpha.4 真机验证(TV 搜索)与 v3.0.1-alpha.2 三项设置真机验证(YouTube 默认画质/默认倍速/字幕渲染)与字幕轨选择/语言切换 UI 迭代(`PlaybackInfo.subtitleTracks` 数据已就绪),再回稳定版短信登录页修复验证与 P1 队列。不要恢复常驻播放器 HUD，性能排查优先使用 `gfxinfo`、`meminfo`、日志和可删除的临时 instrumentation。

## P0 项目决策与规则

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P0-01 | 确定原生重写方案：Kotlin + Jetpack Compose + Android TV | Done | 已写入 `DEVELOPMENT_PLAN.md` |
| P0-02 | 确定包名 `com.kirin.mt` | Done | Gradle `applicationId` 已使用 |
| P0-03 | 确定首发 ABI 策略：主发 `armeabi-v7a`，保留 `arm64-v8a` 能力 | Done | `targetAbi` Gradle 参数已支持 |
| P0-04 | 确定不做应用内更新检查 | Superseded | 由 P10-01 推翻；2026-06-15 决定改为仅手动 GitHub Releases 检查 |
| P0-05 | 确定不复刻完整插件系统，只保留内置空降助手开关 | Done | 计划中已明确 |
| P0-06 | 建立 `AGENTS.md` 开发约束 | Done | 包含 tokens、焦点、播放器、图片、低配置模式等规则 |
| P0-07 | 建立进度跟踪文件 | Done | 本文件 |

## P1 基础工程与首页/搜索

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P1-01 | 创建原生 Android 项目骨架 | Done | 单 app 模块，Gradle 可编译 |
| P1-02 | 接入 Gradle 版本目录 | Done | `gradle/libs.versions.toml` 已使用 |
| P1-03 | 接入 Compose、Material3、TV Material、DataStore、OkHttp、Coil、Brotli | Done | 依赖集中管理 |
| P1-04 | 建立 `AppContainer` | Done | Network、Repository、Storage 等核心对象集中创建 |
| P1-05 | 建立设计令牌 | Done | `BiliTokens.kt` 包含颜色、字号、间距、圆角、焦点、骨架屏常量 |
| P1-06 | 建立 D-pad 焦点基础组件 | Done | `BiliFocusableSurface` 支持粉色焦点框、低配置关闭动画 |
| P1-07 | 接入推荐、热门、分区接口 | Done | 首页可加载卡片 |
| P1-08 | 接入 WBI 签名与 Brotli | Done | 推荐和搜索 API 可用 |
| P1-09 | 实现首页分区标签 | Done | 支持分区切换和确认刷新 |
| P1-10 | 实现首页卡片网格 | Done | 16:9 封面、UP 主头像、固定底部作者行 |
| P1-11 | 修复首页焦点滚动与卡片完整显示 | Done | 选中卡片保持完整可见 |
| P1-12 | 实现首页分页加载 | Done | 接近末尾自动加载更多 |
| P1-13 | 实现搜索键盘 | Done | 参考 Flutter 版布局 |
| P1-14 | 实现搜索建议与搜索历史 | Done | 支持历史记录和清除历史 |
| P1-15 | 实现搜索结果列表 | Done | 支持卡片、分页、焦点回退 |
| P1-16 | 实现搜索排序 | Done | 综合排序、最多播放、最新发布、最多弹幕 |
| P1-17 | 修复搜索结果头像加载 | Done | 头像 URL 解析已处理 |
| P1-18 | 修复启动初始焦点 | Done | 启动后焦点进入首页首个卡片 |
| P1-19 | 调整侧边栏导航顺序 | Done | 搜索、主页、动态、历史、设置 |
| P1-20 | 添加动态入口图标 | Done | 使用风车样式图标 |
| P1-21 | 修复首页标签左键回导航栏焦点错误 | Done | 第一个标签按左回当前激活导航项 |

## P1.5 设置、低配置模式与图片治理

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P1.5-01 | 实现设置页基础布局 | Done | 性能、交互、首页分区 |
| P1.5-02 | 实现低配置模式开关 | Done | 开关使用 BiliPink，操作时不放大 |
| P1.5-03 | 实现切换时自动确认开关 | Done | 设置页交互区显示“切换时自动确认”；默认关闭；关闭后焦点移到导航栏或首页分区标签不会自动进入，首次访问或无内容时仍会自动进入加载 |
| P1.5-04 | 实现切换时自动刷新开关 | Done | 设置页交互区新增独立“切换时自动刷新”；依赖“切换时自动确认”，自动确认关闭时不可打开；关闭后自动切回已有内容会保留原内容，按确认键切换/确认时刷新；主页加载请求完成后清空触发器，避免重复消费旧请求，同时保留冷启动首次加载；`assembleDebug` 通过 |
| P1.5-05 | 实现首页分区显示开关 | Done | 至少保留一个分区 |
| P1.5-06 | 低配置模式关闭焦点动画和平滑滚动 | Done | 已接入 `AppPerformancePolicy` |
| P1.5-07 | 低配置模式降低封面和头像请求尺寸 | Done | 图片请求按策略传入尺寸 |
| P1.5-08 | 低配置模式减少骨架屏数量 | Done | 标准 12，低配置 8 |
| P1.5-09 | 低配置模式预留强制 H.264 | Done | 字段已存在，播放器阶段接入 |
| P1.5-10 | 封面使用 CDN 尺寸参数 | Done | `@widthw_heighth_1c.webp` |
| P1.5-11 | 头像使用 CDN 尺寸参数 | Done | 小头像不拉原图 |
| P1.5-12 | Coil 全局缓存限制 | Done | 全局内存上限 20%，磁盘 128MB；低配置模式通过请求级策略禁用图片内存缓存并清空已热缓存 |
| P1.5-13 | `RGB_565` 不再全局强制 | Done | 低配置策略控制 |
| P1.5-14 | 清理局部视觉硬编码 | Done | 搜索输入字号、设置列数、骨架屏参数移入 tokens |

## P2 账号与登录

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P2-00 | 独立登录入口到侧边栏顶部 | Done | 未登录显示账号图标，设置页账号卡片已移除 |
| P2-00A | 建立用户登录态结构 | Done | `UserSession` 支持 `sessData`、`biliJct`、`mid`、`face`、`uname`、`isVip` |
| P2-00B | 侧边栏头像和大会员角标结构 | Done | 登录后显示头像，大会员显示右下角“大”；登录后头像只展示不参与焦点 |
| P2-01 | 实现 TV 二维码生成 | Done | 使用 TV 登录接口生成 `auth_code`，ZXing 本地渲染二维码，`assembleDebug` 通过 |
| P2-02 | 实现二维码状态轮询 | Done | 每 2 秒轮询，使用 `repeatOnLifecycle(RESUMED)` 暂停后台轮询 |
| P2-03 | 处理二维码过期和刷新 | Done | 过期/失败显示刷新二维码按钮 |
| P2-04 | 登录成功保存 Cookie | Done | 从 `cookie_info.cookies` 保存 `SESSDATA`、`bili_jct` |
| P2-05 | 登录成功拉取用户信息 | Done | 调用 `/x/web-interface/nav` 保存头像、昵称、UID、VIP 状态；已补完整 Cookie 和登录后资料补刷 |
| P2-06 | 登录成功刷新侧边栏头像 | Done | `SessionStore.session` 驱动 Compose 自动刷新；Shell 全局补刷用户资料，头像使用限定尺寸请求、HTTPS 规范化和 B 站图片请求头 |
| P2-07 | 账号页显示登录态 | Done | 退出登录入口暂时移除，后续整体完成后再决定位置；`assembleDebug` 通过 |

## P3 登录态页面

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P3-00 | 补齐动态/历史接入前的视频卡片数据与显示规则 | Done | `VideoSummary` 已加入历史进度、观看时间、多 P、角标、直播标记等字段；`VideoCard` 支持普通、动态、历史模式；发布时间格式为分钟/小时/昨天/2-6 天前/同年 MM-DD/跨年 YY-MM；`assembleDebug` 通过，并使用外部 build 目录 APK 安装到 `127.0.0.1:16384` |
| P3-01 | 实现动态页接口 | Done | 使用 `/x/polymer/web-dynamic/v1/feed/all`，解析视频动态、offset 和 has_more；依赖登录 Cookie |
| P3-02 | 实现动态页卡片列表 | Done | 复用视频卡片与分页焦点逻辑，使用 `VideoCardMode.Dynamic`；接近末尾自动加载更多 |
| P3-03 | 实现历史页接口 | Done | 使用 `/x/web-interface/history/cursor`，解析 view_at/max 游标、观看进度、多 P、直播标记和角标 |
| P3-04 | 实现历史页卡片列表 | Done | 使用 `VideoCardMode.History` 显示已看时长/总时长、进度条、最后观看时间和多 P 提示 |
| P3-05 | 未登录页面提示与跳转 | Done | 动态/历史未登录时显示居中登录提示；暂不自动跳转账号入口，避免焦点链路不确定 |
| P3-06 | 视频卡片播放量图标 | Done | 封面左下角播放数前新增播放 icon；`assembleDebug` 通过并安装到 `127.0.0.1:16384` |
| P3-07 | 动态分页增加收藏 tab | Done | 参考 BV 源码接入 `/x/v3/fav/folder/created/list-all` 和 `/x/v3/fav/resource/list`；`UserFeedTab` 新增 `Favorite`，`FavoriteFeedUiState` 管理收藏夹列表/当前文件夹/分页状态；`FavoriteFeedContent` 提供收藏夹 pill 行切换 + 视频网格复用 `UserFeedGrid`；加载函数 `loadFavoriteFolders`/`loadFavoriteFirstPage`/`loadFavoriteNextPage` 对齐历史页模式；strings.xml 新增 `nav_favorite` 和 `favorite_*` 文案；`assembleDebug` 云编译通过 |

## P4 播放器

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P4-01 | 接入 Media3 ExoPlayer | Done | `PlayerScreen` 使用 Media3 `ExoPlayer` + `PlayerView`，保持默认 SurfaceView；`assembleDebug` 通过；`127.0.0.1:16384` 烟测进入播放器 |
| P4-02 | 实现播放地址获取 | Done | `PlaybackRepository` 接入 `/x/player/wbi/playurl`，支持 BVID/CID/quality/fnval；CID 缺失时通过 `/x/web-interface/view` 解析；烟测已创建音视频解码器 |
| P4-03 | 播放请求头/Cookie 封装 | Done | `BiliMediaDataSourceFactory` 使用 Media3 OkHttp DataSource 统一注入 User-Agent/Referer/Origin/Cookie；`assembleDebug` 通过，烟测未见 403/source error |
| P4-04 | 编码探测与 H.264 回退 | Done | `CodecCapabilityProbe` 探测 H.264/H.265/AV1；低配置模式强制 H.264 并调整 `fnval`；`127.0.0.1:16384` 烟测选择 HEVC 解码器 |
| P4-05 | 点击卡片直接播放 | Done | 首页、搜索、动态、历史卡片确认键直接进入播放器，未引入详情页前置 |
| P4-06 | 播放控制栏 | Done | 遥控器确认键播放/暂停，左右键 10 秒 seek，控制层自动隐藏；画质切换面板待后续细化 |
| P4-07 | 播放进度保存 | Done | `PlaybackProgressStore` 使用 DataStore 保存进度，播放器 `onPause` 和返回时保存 |
| P4-08 | 返回后恢复原卡片焦点 | Done | 退出播放器后按当前页面 FocusRequester 重试恢复：首页/搜索/动态/历史共用入口；真机焦点待手测 |
| P4-09 | 早期临时开发诊断面板 | Done | 曾用于显示编码、分辨率、画质、缓冲、掉帧、host、time 且不显示 Cookie/token；该常驻叠加层已在 P4-16 移除，当前性能排查不保留播放器 HUD |
| P4-10 | Flutter 播放器 UI 对照细化 | Done | 播放器改为顶部标题/UP/发布时间/播放量/时钟、底部大进度条/icon 控制行/画质弹幕状态、自动隐藏小进度条、右侧设置面板、画质/弹幕/倍速子面板和 seek 预览；`assembleDebug` 通过，已安装 `127.0.0.1:16384` 并烟测无 `FATAL EXCEPTION`/`ParserException`/source error |
| P4-11 | 播放器内容面板和状态数据 | Done | 剧集、UP 主视频和相关推荐面板已接入真实 API；移除点赞控制；在线人数使用 `/x/player/online/total`；画质状态追加 H.264/H.265/AV1；`assembleDebug` 通过 |
| P4-12 | UP 主面板排序与关注操作 | Done | UP 主面板默认最新投稿，新增最新/热门排序和关注/已关注操作；取消关注显示确认弹窗；打开前先解析元数据 owner mid，并忽略过期面板请求，降低首次空列表风险；画质状态现在格式化为 `高清 1080P(H.265)`；`assembleDebug` 通过 |
| P4-13 | UP 主面板加载和滚动打磨 | Done | UP 主面板现在匹配 Flutter 布局，头部使用一个排序切换按钮加关注按钮；重复打开时保留已缓存视频并刷新，WBI 失败时回退到未签名空间接口；列表只在焦点视频离开可见范围时滚动；`assembleDebug` 通过 |
| P4-14 | UP 主面板行元数据与 Cookie 补充 | Done | UP 主视频行隐藏重复 owner 名称，使用首页卡片播放图标显示播放数，头部焦点可在排序和关注按钮之间移动，空间投稿请求同时携带 `SESSDATA` 和 `bili_jct` Cookie；`assembleDebug` 通过 |
| P4-15 | 播放器面板焦点和行样式打磨 | Done | 播放默认隐藏控制层，控制栏默认进入剧集；UP 主/相关推荐视频面板在数据加载后聚焦第一个视频；UP 主列表过滤当前视频；视频列表滚动会露出被遮挡的焦点行；行播放数移到缩略图左下角；`assembleDebug` 通过 |
| P4-16 | 播放器按键处理和调试清理 | Done | 移除画质/倍速待确认提示，控制层激活改为 Menu 键，OK/Enter 切换播放/暂停，新增居中暂停指示，并移除播放诊断叠加层；`assembleDebug` 通过 |
| P4-17 | 播放器按键修正和 UP 主重试诊断 | Done | OK/Enter 仅在控制层隐藏时切换播放/暂停，控制层可见时激活当前聚焦控件/面板；Menu 只显示控制层；隐藏状态 seek 不再显示控制层；UP 主投稿请求在签名失败后刷新 WBI key 并重试，同时记录脱敏失败码；`assembleDebug` 通过 |
| P4-18 | 快进预览雪碧图和选中设置颜色 | Done | 画质/倍速当前行使用 BiliPink 显示选中文本；设置页新增快进预览雪碧图开关；播放器加载 `/x/player/videoshot` 和 pvdata，并在 seek 预览时裁剪雪碧图帧，关闭或不可用时回退到纯时间预览；`assembleDebug` 通过 |
| P4-19 | 快进预览雪碧图确认模式 | Done | 快进预览雪碧图默认开启，雪碧图模式等待 OK/Enter 后再 seek，Back 取消预览；雪碧图渲染改用 Canvas source-rect 裁剪，不再用超大偏移图片渲染，避免黑色预览帧；`assembleDebug` 通过 |
| P4-20 | 快进预览雪碧图加载路径 | Done | 雪碧图预览图片现在通过播放仓库下载并携带 Bilibili 请求头，在 `PlayerScreen` 解码为 `ImageBitmap` 后传入叠加层直接进行 Canvas 裁剪；缺失图片时回退到纯时间预览，不再无限加载；`assembleDebug` 通过 |
| P4-21 | 快进预览时间戳对齐 | Done | 原生快进预览模式现在对齐 Flutter 的 `getClosestTimestamp` 行为：左右预览目标在渲染前和 OK 确认 seek 前都会吸附到 pvdata 时间戳，减少预览图、显示时间和最终跳转目标不一致；`assembleDebug` 通过 |
| P4-22 | 播放和应用退出确认 | Done | 设置页新增默认开启的播放退出确认开关；开启后播放器 Back 显示 `再按一次返回键退出播放`，并且只在 3 秒内第二次 Back 时退出；应用级 Back 始终使用 `再按一次返回键退出APP` 双确认；`assembleDebug` 通过 |
| P4-23 | 播放页防息屏 | Done | 播放页通过多层兼容方式防止 TV 息屏：`FLAG_KEEP_SCREEN_ON`、根 View 和 `PlayerView.keepScreenOn`，以及在 pause/dispose 时释放的 `SCREEN_BRIGHT_WAKE_LOCK` 回退方案；manifest 声明 `WAKE_LOCK`；`assembleDebug` 通过 |
| P4-24 | 设置页溢出修复 | Done | 设置行为列改为由焦点驱动的 `LazyColumn`，新增播放开关后底部交互设置会滚入视图，不再被裁剪；`assembleDebug` 通过 |
| P4-25 | 设置页双列焦点返回 | Done | 设置页记住上次聚焦的行为开关，并给每个 lazy row 提供稳定 `FocusRequester`；从首页分区网格向左移动时会滚回并聚焦之前的设置行，不再落到被复用的顶部项；`assembleDebug` 通过 |
| P4-26 | 播放心跳和历史续播打磨 | Done | 在暂停、完成、退出、后台和切换当前视频时上报 `/x/click-interface/web/heartbeat`；完成时上报 `played_time=-1`；历史播放强制使用卡片 cid/progress，不再被本地缓存覆盖；历史卡片用粉色 `P1/P2` 角标显示多 P，用粉色 `已看完` 角标显示已完成；`assembleDebug` 通过 |
| P4-27 | 播放器顶部元数据图标 | Done | 播放器顶部元数据现在使用图标显示 owner、发布时间和播放数，不再使用纯分隔符字符串；`assembleDebug` 通过 |
| P4-28 | 播放编码偏好设置 | Done | 设置页新增解码器选项，默认 Auto，优先级为 AV1 > H.265 > H.264；手动选项只显示 `MediaCodecList` 报告的硬件加速编码；不支持的已保存选项回退到 Auto；播放 URL 请求和 DASH 轨道选择尊重 Auto/H.264/H.265/AV1 偏好，同时保留对受支持轨道的自动回退；低配置模式不再强制覆盖编码偏好；`assembleDebug` 通过，`192.168.1.131:5555` 探测结果为支持 H.264/H.265，不支持 AV1 |
| P4-29 | 默认播放画质设置 | Done | 设置页新增默认画质选项，包含最高/1080P/720P/480P，默认最高。播放 URL 请求优先使用播放器已选画质，仅在初始播放时回退到全局默认；播放器画质面板仍显示 Bilibili 返回的全部画质；`assembleDebug` 通过 |
| P4-30 | 播放完成动作 | Done | 设置页新增默认关闭的完成后自动播下一集、自动播相关推荐和自动退出播放。播放完成时会上报进度，显示可取消 toast 并标出下一个目标，然后执行已启用动作中优先级最高的一项；自动退出复用手动退出播放器路径，确保焦点回到原视频；AirJump 直接跳到结尾附近时会抑制已跳过 toast，避免与完成提示冲突；`assembleDebug` 通过 |
| P4-31 | 缓存清理操作 | Done | 设置页新增清理缓存操作，当前归入 `系统设置`，并在行内显示当前磁盘/临时缓存大小。当前缓存策略为 Coil 内存缓存占可用内存 20%，图片磁盘缓存位于 `cacheDir/image_cache` 且上限 128MB；OkHttp 不使用磁盘缓存，推荐/播放器侧缓存仅在内存中。清理缓存会删除 Coil 图片缓存和 app cache 临时文件，同时保留登录、设置、搜索历史、WBI key 和播放进度；`assembleDebug` 通过 |
| P4-32 | 繁体中文语言支持 | Done | 新增 OpenCC4J 和简体/香港繁体/台湾繁体语言设置循环。静态字符串已有 `values-zh-rHK` 和 `values-zh-rTW`；动态标题、角标、弹幕文本、UP 主名称、分集标题、播放器侧边面板、账号名称和完成提示目标名称在显示时转换，请求和缓存 key 保留原文；`assembleDebug` 通过，并已在 `192.168.1.131:5555` 安装/启动 |

## P5 弹幕与空降助手

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P5-00 | 弹幕获取/渲染首版 | Done | 新增 `x/v1/dm/list.so?type=1` 和 `comment.bilibili.com/{cid}.xml` 回退路径，在 UI 线程外完成 gzip/zlib/raw-deflate XML 解码，补充播放器弹幕加载日志，打磨暂停/播放控制层可见性，并为弹幕数值设置加入适合 TV 的左右键调节；`assembleDebug` 通过 |
| P5-01 | 弹幕 XML 获取与解析 | Done | `PlaybackRepository.getDanmaku()` 使用 `x/v1/dm/list.so?type=1` 和 `comment.bilibili.com/{cid}.xml` 回退路径，gzip/zlib/raw-deflate 解码和 XML 解析放在 `Dispatchers.IO` |
| P5-02 | 弹幕排布与碰撞处理 | Done | 当前决策为使用字节跳动 `danmaku-render-engine`，轨道分配和碰撞由 `DanmakuView` 引擎处理；不再要求首版自研 Kotlin 轨道预计算 |
| P5-03 | 弹幕原生叠加层渲染 | Done | `PlayerDanmakuLayer` 通过 Compose `AndroidView` 承载字节跳动 `DanmakuView`，不把每条弹幕渲染为 Compose 节点；应用层不使用 `delay()` 驱动弹幕重绘 |
| P5-04 | 弹幕显示开关与样式设置 | Done | 弹幕开关、透明度、字号、占屏比、速度、顶部/底部悬停全部接入独立 DataStore 持久化；透明度按 0.1 调节，字号最小 16 且按 2 调节；`assembleDebug` 通过 |
| P5-05 | 空降助手内置开关 | Done | 设置页新增默认开启的空降助手开关，使用 AppSettings/DataStore 持久化；不做插件标签页；`assembleDebug` 通过 |
| P5-06 | 空降助手播放跳转逻辑 | Done | 播放器按 BVID 请求 `bsbsb.top/api/skipSegments` 的 sponsor/intro/outro/interaction/selfpromo 片段，进度条和迷你进度条用绿色标出跳过范围；跳过前 3.5 秒显示 `Toast.LENGTH_LONG` 的即将跳过提示，跳过后保持 `Toast.LENGTH_SHORT` 的已跳过提示；回退到片段前会重置触发状态；`assembleDebug` 通过，已安装并启动到 `192.168.1.131:5555` |
| P5-07 | 空降助手首击拉段可靠性 | Done | 修「很多视频要二次进入才识别广告段」：`AirJumpRepository.getAirJumpSegments` 原为每次进入拉一次，冷连接首击到 `bsbsb.top` 慢/失败被 `runCatching` 静默吞成空。现在加 ①失败短退避重试(最多 3 次共 2 次重试)②按 bvid 内存缓存(仅成功/404 才缓存,二次进入秒回)③加载/命中/失败/重试分级日志(LogTag `BiliMT:AirJump`)。TV `PlayerScreen` 与移动 `MobilePlayerScreen` 的 `LaunchedEffect` 接口不变、零改动,自动获得重试+缓存;冷连接首击靠重试兜住,等效预取+热连接 |

## P6 收尾与发布

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P6-00 | 卡片焦点移动流畅度优化 | Done | 视频网格覆盖默认 TV pivot bring-into-view 策略，左右切换焦点时不再触发纵向支点滚动；上下切换焦点改为目标行稳定定位，避免第三行后焦点继续移动但画面不滚；视频卡片关闭焦点阴影动画，保留边框/底色反馈以降低重绘成本；视频行声明稳定 `contentType` 便于 LazyColumn 复用；`assembleDebug` 通过 |
| P6-00A | 设置页焦点和导航反馈修正 | Done | 设置页上下键改为显式计算目标设置项，目标项已完整可见时不滚动，贴边/半遮挡时只做最小像素补偿，并覆盖默认 TV pivot bring-into-view，避免每移动一项整列大幅跳动；侧边栏图标获得焦点立即变粉，降低导航切换慢一拍的体感；`assembleDebug` 通过，已安装 `192.168.1.195:5555` 并验证设置项上下移动 |
| P6-00B | 画质轨道与主页焦点恢复修正 | Done | playurl 返回后只向 ExoPlayer 暴露接口实际 `quality` 对应的视频轨道，避免 480P 设置被高分辨率轨道覆盖；播放日志输出 requested/returned qn 和实际轨道分辨率；侧栏右键进入首页/动态/历史时读取 `requestFocus()` 真实返回值，失败则触发网格滚动恢复焦点，修复低配置模式切回主页后必须确认刷新才能进内容；封面预取降为标准 12/低配 6，降低导航切页时的解码压力；`assembleDebug` 通过，已安装 `192.168.1.195:5555` 并验证侧栏右键进首页内容 |
| P6-00C | 卡片聚焦特效恢复 | Done | 视频卡片标准模式恢复聚焦 `scale` 放大；低配置模式通过 `motionEnabled=false` 关闭卡片放大和标题跑马灯，保留无动画边框/底色焦点反馈；焦点阴影仍关闭以避免额外合成负担；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555` |
| P6-00D | 卡片轻量焦点视觉增强 | Done | 标准模式下新增封面轻微提亮、标题颜色过渡、UP 主/日期元信息颜色过渡；边框颜色/粗细继续复用 `BiliFocusableSurface` 的轻量动画；低配置模式关闭这些动画效果，仅保留静态焦点可见性；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555` |
| P6-00E | 首页标签栏与时钟避让 | Done | 首页分类栏改为轻量文字标签/选中小胶囊样式，移除整条深色胶囊背景；右侧预留 `176dp` 时钟避让区，保持时钟位置不动；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555` 并截图确认顶部不冲突 |
| P6-00F | 启动图标与繁体切换崩溃修复 | Done | 复用 Flutter 版 `BiliTV` 的启动器图标和 TV 横幅资源；manifest 声明 `android:icon` 和 application/activity `android:banner`，`aapt dump badging` 确认手机图标与 leanback 横幅均存在；发布构建 R8 保留 OpenCC4J 反射构造器，修复切换香港/台湾繁体后 `FastForwardSegment` 初始化崩溃；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已保留数据安装到 `192.168.1.195:5555` 并启动无 BiliTV 崩溃 |
| P6-01 | Baseline Profile 配置 | Done | 新增保守 baseline profile；发布构建已产出 `assets/dexopt/baseline.prof` 和 `assets/dexopt/baseline.profm` |
| P6-02 | R8/资源裁剪检查 | Done | `assembleRelease -PtargetAbi=armeabi-v7a` 通过；发布构建已执行 R8 代码压缩和资源裁剪，输出 `mapping.txt`、`usage.txt`、`resources.txt`、`seeds.txt`、`configuration.txt` |
| P6-03 | v7a 包体检查 | Done | 电视 `192.168.1.195:5555` ABI 为 `armeabi-v7a,armeabi`；当前 v7a 发布 APK `5,719,171` bytes，仅包含 `lib/armeabi-v7a/*` native 库 |
| P6-04 | 内存检查 | Done | 电视首页空闲 PSS 约 40.7 MB；导航/焦点操作后 PSS 约 55.0 MB，Native heap 约 20.1 MB，Java heap 约 12.7 MB |
| P6-05 | 帧耗时检查 | Done | 电视端焦点导航后 `dumpsys gfxinfo`：20721 frames，jank 123（0.59%），P50 9ms，P90 10ms，P95 11ms，P99 14ms，GPU memory 约 26 MB |
| P6-06 | 真机/模拟器安装验证 | Done | v7a 发布包已安装并启动到电视 `192.168.1.195:5555` 和模拟器 `192.168.1.131:5555`；本轮文档/字符串清理后重新通过 `assembleRelease -PtargetAbi=armeabi-v7a`，如需重新真机手测再安装最新 APK |
| P6-07 | 清理临时代码和调试页面 | Done | 删除未使用的 `NetworkProbeScreen` 和 `network_probe_*`、`home_shell_title` 字符串；`rg` 确认无 `NetworkProbeScreen`、`network_probe`、`home_shell_title`、`ui.debug` 残留引用 |
| P6-08 | 文档一致性与视频卡片本地化清理 | Done | `AGENTS.md`/`DEVELOPMENT_PLAN.md`/本文已记录字节跳动弹幕引擎、直播暂缓和 DataStore/Room 取舍；视频卡片相对时间、历史“看过”和播放/弹幕数量单位已移入 strings 资源；`assembleRelease -PtargetAbi=armeabi-v7a` 通过 |
| P6-09 | 内存和缓存收口复查 | Done | 推荐、动态、历史和搜索结果恢复按接口无限分页，不再用高低配置条数上限提前停止；封面预取记录随列表和图片尺寸重置；UP 主投稿缓存限制为最近 4 个 key、每个 50 条；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555`，图片列表实测 PSS 约 44 MB、CPU 0%、jank 0.73%、P99 13ms |
| P6-10 | 高低配置策略分层 | Done | 标准模式启用卡片放大、阴影、封面提亮、焦点封面模糊、标题/元信息颜色动画、平滑滚动、16 张封面预取和图片内存缓存；低配置模式关闭这些动画/模糊/阴影/平滑滚动/封面预取，封面 320x180 RGB_565，头像低尺寸 RGB_565，请求级禁用图片内存缓存并切换时清空 Coil 内存缓存；列表分页不按模式限条，避免影响连续浏览体验；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555` |
| P6-11 | 导航栏进入网格焦点一致性 | Done | `TvVideoGrid` 将导航入口焦点和播放返回恢复焦点拆分：侧边栏右键进入首页/动态/历史时请求第一个卡片；播放返回或显式内容恢复仍请求上次记录的卡片；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555` |
| P6-12 | 首页/搜索标签栏紧凑化 | Done | 首页分类和搜索排序同步改为轻量文字标签，选中项改为粉色文字且无背景色；遥控焦点落到标签时使用与导航栏/卡片一致的粉色边框反馈；视频卡片未改动；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555` 并启动无崩溃 |
| P6-13 | 低配置播放策略与首次启动内存检测 | Done | 低配置模式下播放器有效解码偏好强制为 H.264，设置页解码器显示同步为 H.264，并新增 playurl codec 日志输出 requested/effective/fnval；首次读取设置时若设备总内存低于 1GB 且用户未手动设置过低配置开关，则默认启用低配置模式；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555` 并启动无崩溃，电视 `MemTotal` 约 2.26GB 因此不会自动默认低配 |
| P6-14 | 网络权限兼容性补齐 | Done | Manifest 原本已声明 `android.permission.INTERNET`；为兼容国产系统和连通性判断补充 `android.permission.ACCESS_NETWORK_STATE`；`aapt2 dump permissions` 和电视 `dumpsys package` 均确认 `INTERNET`、`ACCESS_NETWORK_STATE`、`WAKE_LOCK` 存在且已授予；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555` 并启动无崩溃 |
| P6-15 | 发布包体编译压缩 | Done | 发布构建增加 `androidResources.localeFilters`，仅保留默认、简中、香港繁中和台湾繁中资源，并排除依赖嵌套 LICENSE 文本；APK 从 `6,186,139` bytes 降至 `5,719,171` bytes，主要减少 `resources.arsc`；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555` 并启动无崩溃 |
| P6-16 | 根目录文档英文残留中文化 | Done | 已将 `AGENTS.md`、`DEVELOPMENT_PLAN.md`、`DEVELOPMENT_PROGRESS.md` 中面向读者的英文说明翻译为中文；保留命令、API、类名、构建类型和状态值；`rg` 检查后剩余英文主要为技术标识 |
| P6-17 | GitHub 上传前无用文件清理 | Done | 删除本机生成目录 `.gradle/`、`.kotlin/`、`build/`、`app/build/` 和本机 SDK 配置 `local.properties`；补充 `.gitignore` 忽略 `.kotlin/`、NDK/外部构建目录和 APK/AAB 产物；删除 0 引用资源 `ic_player_like.xml`、`ic_banner.png`；`rg --files --hidden` 确认剩余文件为源码、资源、Gradle wrapper 和文档 |

## P7 主页主题与视觉效果

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P7-01 | 将视觉性能模式扩展为流畅/均衡/精致三档 | Done | 已新增流畅/均衡/精致三档策略并通过 DataStore 持久化；低于 1GB 首次启动默认流畅，其余默认均衡；精致档必须用户手动开启；`assembleDebug` 通过 |
| P7-02 | 新增 4 种主页主题设置和持久化 | Done | 已新增默认粉、深黑、高级灰、蓝灰 4 种主题，设置页可切换并通过 DataStore 持久化；播放器暂不跟随主页主题 |
| P7-03 | 建立主页专用主题色系统 | Done | 已新增 `HomeColorScheme` 和 `LocalHomeColors`；首页、搜索、动态、历史、设置、侧边栏和标签栏读取主页主题 |
| P7-04 | 实现主页玻璃背景 | Done | 已接入主题渐变背景；精致模式额外启用环境高光，流畅模式不启用额外动态视觉 |
| P7-05 | 重做侧边导航玻璃样式 | Done | 侧边栏改为半透明玻璃竖栏、轻边框和主题色焦点反馈，头像、图标、选中态跟随主题色 |
| P7-06 | 重做主页/搜索标签栏玻璃样式 | Done | 首页分类和搜索排序标签使用主题色文字与焦点边框，继续保留无实心背景并避让顶部时钟 |
| P7-07 | 重做视频卡片玻璃材质 | Done | 卡片信息区使用半透明玻璃层；获焦保留细边框、轻提亮、文字颜色过渡和克制缩放；流畅模式关闭动画和阴影 |
| P7-08 | 增加精致模式主题色斜向流光效果 | Done | 精致模式下焦点卡片使用单个跟随焦点的小尺寸 overlay 绘制斜向流光；切到卡片后先等 2 秒再扫，后续约每 5 秒扫一次，颜色跟随当前主页主题 |
| P7-09 | 主页视觉性能回归测试 | Done | `assembleRelease -PtargetAbi=armeabi-v7a` 通过并已安装 `192.168.1.131:5555`；基础 D-pad smoke test 无 `FATAL EXCEPTION`；`gfxinfo` 52 帧 jank 0.00%，P50 11ms、P90 18ms、P95 31ms、P99 42ms；PSS 约 85.6MB |
| P7-10 | 静态规则复查修正 | Done | 播放器打开时不再组合主页层，主页背景动画、卡片阴影/流光和封面预取随主页 Composable 一起释放；均衡档关闭封面实时模糊；主页主题颜色收口到 `BiliTokens.kt`；`assembleDebug` 和 `assembleRelease -PtargetAbi=armeabi-v7a` 通过 |
| P7-11 | 卡片跨行滚动裁切回退 | Done | 撤回安全区触发、目标舒适区、额外视觉余量、行留白和 `zIndex` 试验，恢复 `TvVideoGrid` 稳定顶齐滚动与换行前等帧，避免上下焦点卡片被裁切；`assembleDebug` 和 `assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.131:5555` |
| P7-12 | 对齐 Flutter 版跨行滚动手感 | Done | 参考 Flutter 版 `ScrollController.animateTo` 的 `500ms + easeOutCubic`，Native 仅调整 `animateScrollBy` 的滚动时长和滚动专用曲线，不改可视边界、行留白、缩放或目标行定位；`assembleDebug` 和 `assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.131:5555` |
| P7-13 | MT9655 电视 UI 性能限制策略 | Done | 针对 MT9655 或 `MiTV-MFFU1` 小米电视启用受限 TV UI 策略：保留平滑滚动和基础焦点动效，但关闭焦点阴影、精致/电影视觉、焦点封面模糊和大规模封面预取，封面降为 480x270 RGB_565、预取降为 8；`assembleDebug` 和 `assembleRelease -PtargetAbi=armeabi-v7a` 通过；已安装 `192.168.1.195:5555`，Sony Android 9 约 2.26GB 内存，模拟方向键后 P50 53ms/P95 69ms，仍比 210 旧数据 P50 93ms/P95 150ms 轻；210 设备在线后再安装实测 |
| P7-14 | 全局焦点特效层试验 | Done | `TvVideoGrid` 将方向键网格内移动的父级焦点状态写回改为离开网格/点击播放时提交，降低父级重组；卡片本体关闭真实焦点阴影，精致模式流光改为单个跟随焦点卡片的小尺寸 overlay 绘制，避免每张卡片各自跑流光；`assembleDebug` 和 `assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装 `192.168.1.195:5555`；195 实测 P50 53ms/P95 约 77-81ms，较 69ms 基线未明显改善，下一步应准备 RecyclerView/DpadRecyclerView 网格对照方案 |
| P7-15 | 播放器进出场黑屏过渡与静态约束复查 | Done | 黑屏遮罩转场改为 `BiliMotion` token，时长收短为进入 90ms、保持 10ms、退出 90ms；静态检查确认未恢复 `AnimatedVisibility` 缩放/淡入淡出，未对视频 `SurfaceView` 本体做变换，主页与播放器按 `visiblePlaybackRequest` 互斥组合，播放器显示后主页背景动画、卡片流光/阴影和封面预取会随主页 Composable 释放；本轮按要求未编译安装 |
| P7-16 | 精致模式卡片悬浮感增强 | Done | 精致模式视频卡片获焦时启用 `1.072` 轻微放大、`8dp` 上浮、斜向流光和液态玻璃感边缘；玻璃边缘使用整圈外层折射边和内层细高光模拟，不启用真实背景模糊；已移除卡片背景发光、左上额外高光线、右下暗边与非焦点卡片黑色遮罩，避免焦点移动时闪烁或边角不对称；`TvVideoGrid` 同步使用精致档缩放参与滚动可视区计算，均衡/流畅档保持原策略；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装并启动 `192.168.1.131:5555` |
| P7-17 | AndroidLiquidGlass 实验开关 | Done | 变更前已备份到 `C:\Users\Kirin\OneDrive\Code\BiliTVNative_backup_before_liquidglass_20260509_212240`；升级 Gradle Wrapper `9.4.1`、AGP `9.2.1`、Kotlin `2.3.21`、Compose BOM `2026.05.00`，并接入 `io.github.kyant0:backdrop:2.0.0-alpha03`；设置页新增“液态玻璃控件”独立开关，仅在 Android 13+ 且精致档可开启，Android 13 以下持久化读取和写入都会强制关闭；开启后主页背景作为 `LayerBackdrop` 采样层，卡片、导航按钮、设置/搜索等共享焦点控件以及播放器覆盖层控件统一切换为单层 AndroidLiquidGlass 表面；播放器视频 `SurfaceView` 本体不参与变换或采样，避免影响播放兼容性；`assembleDebug`、`assembleRelease -PtargetAbi=armeabi-v7a` 和 `git diff --check` 通过，release APK SHA256 `5D321AEC299CCA34E0A928E9B547E4AE5A2675F37A7AD2147C0F999B660BE6B2`，已安装并启动 `192.168.1.210:5555`，该设备版本 `1.0.0` / `versionCode=100` |
| P7-18 | 液态玻璃视觉微调 | Done | 单层 LiquidGlass 启用库内置 `Highlight.Ambient`、`Shadow.Default` 和 `InnerShadow.Default`，将 blur/refraction 提高到 `6dp / 14dp / 24dp`；主页视频卡片在 LiquidGlass 开启时关闭前景描边，避免获焦后底部出现一条硬横线；共享焦点控件和播放器小控件继续保留轻边缘以维持可读性；`assembleDebug`、`assembleRelease -PtargetAbi=armeabi-v7a` 和 `git diff --check` 通过，release APK SHA256 `19E7CCCE984769AD702A089D69C5A0E3DA7BB90E20695D41F77609C638F3B75D`，已安装并启动 `192.168.1.210:5555`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |
| P7-19 | 液态玻璃方向性与仿玻璃边框修复 | Done | 去掉 AndroidLiquidGlass 默认 `Highlight/Shadow/InnerShadow` 方向光，保留无方向 `vibrancy + blur + lens`，避免左上角和右下角质感不一致；液态玻璃关闭时，卡片仿玻璃边框从内容子层改为卡片根层前景绘制，并向内安全缩进 `5dp`，同时 `TvVideoGrid` 提升焦点所在行 `zIndex`，避免卡片放大后被相邻行覆盖到只剩底部一条线；`assembleDebug`、`assembleRelease -PtargetAbi=armeabi-v7a` 和 `git diff --check` 通过，release APK SHA256 `C4C37B0077FD6AD521CA4D57D2CA9473A4735FF81B417622F8232C9CC450C5AB`，已安装并启动 `192.168.1.210:5555`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |
| P7-20 | 玻璃焦点框去重与轻光源恢复 | Done | `BiliFocusableSurface` 新增单一前景绘制入口，主页卡片关闭 LiquidGlass 时的仿玻璃边框改为同一个焦点 Surface 内绘制，避免根层和内容层多套边框叠加；高级卡片在非 LiquidGlass 状态下同步关闭 Surface 自带描边，只保留一套仿玻璃边；LiquidGlass 恢复 `Highlight.Plain` 和 `InnerShadow.Default`，保留外部 `Shadow` 关闭以降低方向光过重；`assembleDebug`、`assembleRelease -PtargetAbi=armeabi-v7a` 和 `git diff --check` 通过，release APK SHA256 `EB324BB443278345ACAC541C5136109D16163384975D319558A8C9055B2EDDA7`，已安装并启动 `192.168.1.210:5555`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |
| P7-21 | 卡片底线与液态玻璃强度调整 | Done | 根据电视实拍，关闭 LiquidGlass 时仍有多边框/底线不一致，已撤掉主页卡片仿玻璃多层边框，避免上下左右不一致；LiquidGlass 底线来自 `InnerShadow.Default`，已关闭内阴影，改为 `Highlight.Ambient + Shadow.Default`，并将 blur/refraction 提高到 `8dp / 20dp / 34dp` 增强液态感；`assembleDebug`、`assembleRelease -PtargetAbi=armeabi-v7a` 和 `git diff --check` 通过，release APK SHA256 `C9E08F38090AB7B9F807C198FC26B92B62595324B589DA50961CA6E77DAA4281`，已安装并启动 `192.168.1.210:5555`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |
| P7-22 | 卡片焦点色与侧栏玻璃面板修复 | Done | 主页视频卡片获焦背景从 `cardFocusedSurface` 改回 `cardSurface`，封面 polish 的中段高光从主题 accent 改为中性 `textPrimary`，避免液态玻璃开关开/关时卡片整体都泛粉；关闭 LiquidGlass 时恢复备份前的白色仿玻璃焦点边框，并只在 LiquidGlass 不可用时绘制，避免和真实玻璃叠层；侧边导航栏 panel 本体接入 `biliLiquidGlassSurface`，开启液态玻璃后不再只有按钮是玻璃；`assembleDebug`、`assembleRelease -PtargetAbi=armeabi-v7a` 和 `git diff --check` 通过，release APK SHA256 `5115C0BF8CD7620ECD9C3B47A7C5A0CE68D9C7146E28DEDB3C1C231A6C06282B`，`192.168.1.210:5555` 上版本 `1.0.0` / `versionCode=100`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |
| P7-23 | 液态玻璃边缘裁剪与侧栏圆角调整 | Done | `BiliFocusableSurface` 改为只裁剪内容层，不再裁剪外层 LiquidGlass/前景边框，避免获焦缩放后玻璃折射边缘被自身 clip 吃掉；主页卡片在 LiquidGlass 开启时增加 `4dp` 内缩留白，给 `1.072` 焦点缩放预留边缘显示空间；关闭 LiquidGlass 时，仿玻璃卡片边框改回焦点 Surface 前景绘制，避免内容层覆盖后只剩底线；侧边导航栏四角统一改为 `30dp` 圆角；`assembleDebug` 和 `git diff --check` 通过，debug APK SHA256 `DACED13656D4A064325A97759B4A87163A9153CC091EA6D5AF411E9DEC595975`，已安装并启动 `192.168.1.210:5555`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |
| P7-24 | 主页网格与侧栏尺寸微调 | Done | 针对关闭 LiquidGlass 时第一行卡片获焦顶部被裁剪的问题，将网格顶部安全区 `ScrollInset` 从 `20dp` 提高到 `32dp`；侧边导航栏从 `88dp` 收窄到 `76dp`，导航按钮高度 `56dp -> 48dp`、图标 `28dp -> 24dp`、头像容器和 VIP 标记同步缩小，侧栏内部横向 padding 和按钮间距从 `Lg` 收紧到 `Md`；卡片网格间距 `12dp -> 10dp`，配合侧栏释放的宽度让 4 列卡片略微放大并缩短间隔；`assembleDebug` 和 `git diff --check` 通过，debug APK SHA256 `C2C7D170FCA2FAC0A9BA602A98D3415A7F72AE24FD537134AD7426D7A03092D1`，已安装并启动 `192.168.1.131:5555`，未见 `FATAL EXCEPTION` |
| P7-25 | 首页分区居中胶囊导航 | Done | 首页分区栏从左对齐 `LazyRow` 改为内容自适应的居中大胶囊，支持设置页动态启用的 `1-12` 个分区；胶囊内分区按钮保持原有焦点、确认和向左回侧栏逻辑，选中/焦点文字仍跟随主题色；LiquidGlass 开启时胶囊外层走 `biliLiquidGlassSurface` 真实玻璃材质，关闭时自动回落到主题半透明玻璃底和细边框；`assembleDebug` 和 `git diff --check` 通过，debug APK SHA256 `3FBBF2C80AA0EE193586D839F4F41B43DE179EDC25C7DA08BC556C5B3DBEF45A`，已安装并启动 `192.168.1.131:5555`，未见 `FATAL EXCEPTION` |
| P7-26 | 首页分区胶囊定位微调 | Done | `RecommendHeader` 改为 `BoxWithConstraints`，胶囊按启用分区内容宽度自适应并以可用宽度为上限居中，避免 1-12 个分区切换时位置被内容长度顶偏；胶囊上移量先试 `10dp` 后按实机截图回收为 `2dp`；`assembleDebug` 和 `git diff --check` 通过，debug APK SHA256 `F1B12FCC1D82F4B5A9DDC527BFE3EB7DA0E6A8EB7934120515D523A3C4AED846`，已安装并启动 `192.168.1.131:5555`，未见 `FATAL EXCEPTION` |
| P7-27 | 首页分区文字视觉居中校正 | Done | `HomeSectionTab` 原本通过 `Box(contentAlignment = Alignment.Center)` 做布局居中，但中文字体在 `includeFontPadding=false` 下视觉重心略偏上；新增 `HomeSectionTabTextVerticalOffset = 1dp` 只下移文字本身，不改变胶囊、按钮和焦点边框尺寸；`assembleDebug` 和 `git diff --check` 通过，debug APK SHA256 `119FBB584273A42CB17824058186CBB3CF81B1F187F29C33798F0660111128CE`，已安装并启动 `192.168.1.131:5555`，未见 `FATAL EXCEPTION` |
| P7-28 | 首页分区胶囊字号放大 | Done | 面向 12 个分区全开状态，将分区文字从 `15sp/18sp` 放大到 `17sp/20sp`，tab 高度从 `32dp` 增至 `36dp`，胶囊高度从 `48dp` 增至 `52dp`，垂直 padding 从 `5dp` 增至 `6dp`，让文字和选中态边框比例更接近电视端远距离观看；`assembleDebug` 和 `git diff --check` 通过，debug APK SHA256 `83BAAB631AA72AD1D11D6AA51E13D28F5A464D80ADF0067CCF8982FBC1BA49F6`，已安装并启动 `192.168.1.131:5555`，未见 `FATAL EXCEPTION` |
| P7-29 | 首页分区胶囊字号二次放大 | Done | 根据 12 个分区全开截图继续利用横向余量，将分区文字从 `17sp/20sp` 放大到 `19sp/23sp`，tab 高度从 `36dp` 增至 `40dp`，胶囊高度从 `52dp` 增至 `58dp`，垂直 padding 从 `6dp` 增至 `7dp`，横向间距保持不变以避免全开时过宽；`assembleDebug` 和 `git diff --check` 通过，debug APK SHA256 `252796EA165F92264D6B692DAB83477B87311A8ED027183C2E3EB3A016280555`，已安装并启动 `192.168.1.131:5555`，未见 `FATAL EXCEPTION` |
| P7-30 | 撤回分区文字下移校正 | Done | 按实机观感验证需求，删除 `HomeSectionTabTextVerticalOffset` 和 `Text` 上的 `Modifier.offset`，分区文字重新完全依赖 `Box(contentAlignment = Alignment.Center)` 与字体原始 metrics 居中；`assembleDebug` 和 `git diff --check` 通过，debug APK SHA256 `1A53B6C3EF6797EF1B206CA4099E099932731B64108C3994299DB95AE8F63A82`，已安装并启动 `192.168.1.131:5555`，未见 `FATAL EXCEPTION` |
| P7-31 | 分区少时胶囊自适应拉宽与首页卡片上移 | Done | 首页分区胶囊新增按启用数量计算的最小宽度：`1-6` 个分区逐级拉宽并使用 `Arrangement.SpaceEvenly` 均匀分布，`7-12` 个维持内容自适应与固定间距；`TvVideoGrid` 新增 `topPadding` 参数，首页单独使用 `HomeVideoGridTopPadding = 24dp`，让第一行卡片较原 `32dp` 顶部留白上移 `8dp`，不影响搜索/动态/历史网格默认安全留白；`assembleDebug` 和 `git diff --check` 通过，debug APK SHA256 `58BF1448C2BADA3DB313BC9E5F009537B21DF66BA8AD8D2FC60315B7176BF0A0`，已安装并启动 `192.168.1.131:5555`，未见 `FATAL EXCEPTION` |
| P7-32 | 首页卡片顶部放大裁剪修复 | Done | `TvVideoGrid` 新增 `topBleed` 视口扩展参数，首页使用 `HomeVideoGridTopBleed = 16dp` 将 LazyColumn 实际测量/绘制区域向上扩出，同时把首页内容 top padding 增加同等 bleed，保持卡片平时位置不变但允许焦点放大和上浮时盖过上方空白区域；默认 `topBleed = 0dp`，搜索/动态/历史不受影响；`assembleDebug` 和 `git diff --check` 通过，debug APK SHA256 `74B6569B8394DD94902D0D7D26FC1F22DE967A20D3473C26461961BB63693C71`，已安装并启动 `192.168.1.131:5555`，未见 `FATAL EXCEPTION` |
| P7-33 | 液态玻璃卡片选中外扩边缘 | Done | 液态玻璃卡片保持原有 `4dp` 安全内缩，但选中态新增 `drawLiquidGlassCardFocusedOutline()`，在焦点前景层向外扩出同等 `4dp` 并绘制 `2dp` 半透明玻璃高光边缘，使选中外沿尺寸接近非液态玻璃卡片；普通/关闭液态玻璃时仍使用原有仿玻璃边框逻辑；`assembleRelease -PtargetAbi=armeabi-v7a` 和 `git diff --check` 通过，release APK SHA256 `793987AAD8DC3E27BE1D5C62D41F0893611BBC7BCD4FCE4F4470B0299754E53C`，已安装并启动 `192.168.1.210:5555`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |
| P7-34 | 液态玻璃卡片选中边框强化 | Done | 针对实拍中液态玻璃边框不明显的问题，将液态玻璃选中外扩轮廓从 `2dp` 增强到 `3dp`，新增内侧 `1dp` 高光线，并把卡片专用白色玻璃高光透明度提升到 `0.76/0.36/0.52`，比 P7-33 更接近非液态玻璃卡片的可见边框；`assembleRelease -PtargetAbi=armeabi-v7a` 和 `git diff --check` 通过，release APK SHA256 `129602DD8CB8D3743D03CF3B8D09202340C019ACA1DA74EEB29B097F00FBC01D`，已安装并启动 `192.168.1.210:5555`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |
| P7-35 | 液态玻璃清透度调整 | Done | 按实机观感将液态玻璃从磨砂感转向更清透：全局 LiquidGlass blur 从 `8dp` 降到 `3dp`，卡片主体在液态玻璃开启时单独使用 `0.18` 透明度，底部信息区使用 `0.46` 透明度以保留文字可读性，折射参数保持 `20dp/34dp` 以保留液态边缘；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，release APK SHA256 `087A0EC6AA0119459D1398ED206BD14721DC326E9AD9BD67094E0E681D397159`，已安装并启动 `192.168.1.210:5555`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |
| P7-36 | 液态玻璃焦点边框去磨砂化 | Done | 根据实拍中焦点边框仍显厚重磨砂的问题，移除液态玻璃卡片选中态的中间 `3dp` 厚内描边，仅保留外扩折射轮廓和 `1dp` 内侧淡高光；外扩轮廓从 `3dp` 降到 `2dp`，高光透明度从 `0.76/0.36/0.52` 降到 `0.58/0.12/0.22`，让边缘更像透明玻璃折射而不是乳白磨砂边；`assembleRelease -PtargetAbi=armeabi-v7a` 和 `git diff --check` 通过，release APK SHA256 `CBF59A1431C2403723081F3627FA9D87FE455C5713BB43E08BA73A8EF30FF1C4`，已安装并启动 `192.168.1.210:5555`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |
| P7-37 | 液态玻璃焦点边框连续环带 | Done | 针对 P7-36 实机出现外圈与卡片本体边缘之间透明空隙、视觉变成双层的问题，将液态玻璃卡片选中态从单条外扩描边改为覆盖 `4dp` 外扩安全区的连续渐变玻璃环带，环带内边贴合卡片本体、外边保留外扩尺寸；同时清理已不生效的内侧描边 token，并将环带透明度调整为 `0.38/0.08`，避免填满后重新变成厚重磨砂白边；`assembleRelease -PtargetAbi=armeabi-v7a` 和 `git diff --check` 通过，release APK SHA256 `E2F9434699A5C83139F7839824C19D72A8CE44DE29C75E997EDE36E546E0D2E3`，已安装并启动 `192.168.1.210:5555`，未见 `AndroidRuntime` / `FATAL EXCEPTION` |

## P8 UI/设置/文档收尾

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P8-01 | 搜索播放返回状态修复 | Done | `AppShell` 持有 `SearchUiState`，从搜索结果进入播放器再返回时保留结果和封面加载状态；从搜索切换到其他主页面时清空搜索态，避免回到搜索仍显示旧结果 |
| P8-02 | 播放器侧栏列表视觉修正 | Done | 选集当前播放项移除粉色竖线；UP 主更多视频和相关推荐卡片放大并收紧间距，封面上的播放数、弹幕数和时长改为接近主页卡片的覆盖样式，不再挤在面板内 |
| P8-03 | 播放器设置列表滚动 | Done | 弹幕设置、分辨率/画质等播放器侧栏列表支持滚动，保留标题可读性，不再通过压缩标题字号解决底部选项被裁剪 |
| P8-04 | 迷你进度条设置开关 | Done | 设置页新增默认开启的“显示迷你进度条”，归入 `播放设置`；播放器按该设置决定是否显示自动隐藏的小进度条 |
| P8-05 | 播放器周期任务收口 | Done | 播放器保留一个 `BiliMotion.PlayerProgressUpdateMs = 500ms` 的生命周期绑定状态循环，用于播放进度、缓冲、时钟分钟变化、在线人数节流和迷你进度条状态；未保留常驻性能 HUD |
| P8-06 | 设置分类重排 | Done | 设置页分为 `播放设置`、`UI/UX`、`系统设置`：播放画质/编码、快进预览、空降助手、退出确认、播放完成动作、时钟和迷你进度条归入播放；视觉性能、液态玻璃、主页主题、切换自动确认和切换自动刷新归入 UI/UX；缓存清理、语言和关于归入系统 |
| P8-07 | 关于展示面板 | Done | 系统设置末尾新增“关于”；焦点移到该行时右侧自动展示项目简介、项目地址二维码、MIT License 和第三方开源库名称/URL/说明，右侧内容纯展示不可聚焦；面板支持液态玻璃开启/关闭两种状态 |
| P8-08 | README 重写 | Done | `README.md` 已重写为当前项目介绍、功能说明、构建方式和 UI/液态玻璃说明；截图位置保留给用户后续补充 |
| P8-09 | 根目录 3 个 MD 一致性修正 | Done | 本轮只更新 `AGENTS.md`、`DEVELOPMENT_PLAN.md` 和 `DEVELOPMENT_PROGRESS.md`，删除或改写过期的 `feature/*`、Compose Navigation、Coil 3、Room/Koin 默认推荐和常驻播放器诊断 HUD 说法；`README.md` 未改动 |
| P8-10 | UP 主面板头部按钮焦点修正 | Done | “最新发布/最热门”和“关注/已关注”不再使用整块粉色实心底作为选中态，改为粉色文字/细边表达选中；焦点态使用白色高亮边框和播放器面板焦点底，避免粉底下焦点不可见；`assembleRelease -PtargetAbi=armeabi-v7a` 通过 |
| P8-11 | UP 主更多视频间歇加载失败诊断 | Done | 在 `PlayerScreen` 和 `VideoRepository` 增加脱敏日志，记录打开 UP 主面板、mid 解析、缓存命中、space 接口签名/刷新/回退、空 vlist、网络失败和过期 token 丢弃；不打印 Cookie/token；`assembleRelease -PtargetAbi=armeabi-v7a` 通过 |
| P8-13 | UP 主更多视频 412 加载体验优化 | Done | 将空间投稿加载拆成 `Interactive` 和 `Recovery` 两种重试模式：前台面板只做一次 600ms 短重试，失败后立即结束 loading 并保留缓存/空态；后台 1.2s 后再做恢复重试，成功且仍停留在同一 UP 面板时再刷新列表，避免 412 退避把侧栏卡住数秒；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装并启动 `192.168.1.131:5555` |
| P8-14 | UP 投稿接口对齐网页抓包 | Done | 根据网页端 `x/space/wbi/arc/search` 抓包同步最新发布/最多播放请求：参数改为 `ps=25,index=1,order_avoided=true,platform=web,web_location=333.1387`，请求头使用 `space.bilibili.com/{mid}` Referer/Origin、Chrome 147 UA、sec-ch-ua/priority/fetch 头，并在 Cookie 中补充登录 mid 对应的 `DedeUserID`；`assembleRelease -PtargetAbi=armeabi-v7a` 通过，已安装并启动 `192.168.1.131:5555` |

## P9 重构拆分收尾

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P9-01 | 播放仓库职责拆分 | Done | 将弹幕 XML 获取/解压/解析拆到 `DanmakuRepository`，雪碧图元数据和图片字节获取拆到 `VideoshotRepository`，空降助手 SponsorBlock 请求拆到 `AirJumpRepository`；`PlaybackRepository` 保留播放地址、元数据、在线人数和播放进度职责；`assembleDebug` 通过 |
| P9-02 | 播放器页面逻辑拆分 | Done | 将播放器侧栏视频加载、UP 主缓存和恢复重试拆到 `PlayerSidePanelLoader`；将播放完成后的下一集/相关视频选择规则拆到 `PlayerCompletionPlanner`；将快进预览雪碧图时间对齐、预加载 URL、解码和缓存裁剪拆到 `PlayerVideoshotPreview`；保留 ExoPlayer 生命周期、D-pad 主循环和弹幕/Surface 叠层不动；`assembleDebug` 通过 |
| P9-03 | 视频数据仓库按业务域拆分 | Done | `VideoRepository` 保留对外门面和关注/取消关注，首页/热门/分区/相关推荐拆到 `HomeVideoRepository`，搜索和搜索建议拆到 `SearchVideoRepository`，UP 空间投稿/WBI/buvid/412 恢复策略拆到 `SpaceVideoRepository`，动态和历史拆到 `UserFeedRepository`，JSON 到 `VideoSummary` 的映射集中到 `VideoSummaryMappers`；`assembleDebug` 通过 |
| P9-04 | 设置页和应用壳 UI 拆分 | Done | 设置页右侧主页栏目/About 面板拆到 `SettingsRightPanels`，设置行和显示文案拆到 `SettingsRows`，设置焦点入口/方向键边界/滚动辅助拆到 `SettingsFocus`；应用左侧导航栏、账号头像和导航按钮拆到 `AppSidebar`；`SettingsScreen` 降到约 646 行，`AppShell` 保留应用级状态、页面切换、播放入口和播放器叠层；`assembleDebug` 通过 |

## P10 应用内更新

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P10-01 | 接入程序更新（仅手动） | Done | 推翻 P0-04；新增 `core/update/` 包（`AppInfo`、`UpdateInfo`、`UpdateRepository`、`UpdateDownloader`、`ApkInstaller`、`UpdateManager`、`UpdateUiState`），设置页 → 系统设置 → 程序更新提供"当前/最新版本展示、检查更新、下载/安装、查看发布说明"四项；`AndroidManifest.xml` 加 `REQUEST_INSTALL_PACKAGES` + `FileProvider` 节点，`res/xml/file_paths.xml` 新建；版本对比走 `tag` → `versionCode`（major×10000+minor×100+patch），按 `Build.SUPPORTED_ABIS` 选 asset；APK 下载到 `cacheDir/updates/`，安装走 `ACTION_VIEW + FileProvider`；不接入自动/后台检查；`assembleDebug` 通过 |
| P10-02 | 修复 CDN 焦点跳到 LazyList 末尾的 bug | Done | `SettingsItemPlaybackCdn = 21` 与 LazyList 真实索引 3 不匹配，`scrollItemIntoComfortableView(index = 21)` 被 clamp 到 `totalItems - 1`，导致按一次下方向键焦点跳到"关于"。新增 `settingsItemToLazyIndex()` 映射函数统一 `SettingsItem*` → LazyList index。已知 follow-up：当 `update-download-or-install` / `update-release-notes` 条件性 item 渲染/隐藏时，ClearCache/ChineseTextVariant/About 的真实索引会变 1-2；当前仅在"无可用更新"状态下做了对齐。`assembleDebug` 通过 |
| P10-03 | v1.0.7 发布与合入 main | Done | 整理 v1.0.6 至 v1.0.7 间的应用内更新、CI 和 About 页面变更；更新 `README.md`、`RELEASE_NOTES.md` 和本文件；打 tag `v1.0.7` 并推送；从 `mort_debug` 发起 PR 合到 `main` |
| P10-04 | v1.0.9 发布与合入 main | Done | 整理 v1.0.8 稳定版后的 1.0.8-alpha 周期变更（设置内网络测速、CdnSpeedTester 探测策略优化、搜索/动态/历史 onOwnerSelected 回调补全、UP 主取消关注对话框焦点修复、CI 发布说明改用 gh release create）；更新 `README.md`、`RELEASE_NOTES.md` 和本文件；打 tag `v1.0.9` 并推送；从 `mort_debug` 合到 `main` |
| P10-05 | 检查更新不保存状态,手动点击才检查 | In Progress | 用户反馈:检查一次未下载后一直显示旧更新、且需点两次才到下载。改为**不保存检查结果**、**仅手动点击「检查更新」触发**(不做进页自动检查),发现更新直接显示「下载更新」(不再有中间「重新检查」态),废弃 `Available.rechecked` 两击流;`downloadOrInstallLabel`/onClick 回退到 `Available → 下载更新` |

## 移动端 UI 与直播（v2.0.x 里程碑）

> 此阶段从 `mobile` 分支把触屏移动端移植进单 APK（运行时 `isTvUi()` 选 TV/手机），并新增直播播放。详细逐条见 `RELEASE_NOTES.md`。

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| v2.0.0 | 移动端 UI 移植完成 | Done | 单 APK 双入口；移动端外壳（NavigationSuiteScaffold）、内容页（首页/动态/搜索/设置/短信登录）、触屏播放器（手势/画质倍速弹幕/分P/自动连播/UP空间/空降/后台播放/全屏）、PGC MergingMediaSource；合并 mobile → mort_debug → main 打 v2.0.0 稳定 tag |
| v2.0.1 | 视频详情上下分栏 + 评论 + 动态四子 tab + 季详情 | Done | 播放器上半+评论下半；动态/历史/收藏/追番四子 tab（PrimaryScrollableTabRow + Pager）；MobilePgcSeasonScreen 季详情选集续播高亮 |
| v2.0.2 | 移动端播放器重构 + 互动 + 检查更新 | Done | 简介/评论双 Tab；点赞/投币/收藏/分享（真实计数与已操作状态）；顶/底栏不遮视频；全屏按视频比例自适应；检查更新内联下载 |
| v2.0.3 | 移动端播放器打磨 + 弹幕发送 | Done | 竖屏自动全屏居中；画质入口下移；发弹幕（本地插入+粉色描边）；弹幕数量档位；播放卡死自愈（stall 重载）；后台播放音频焦点/耳机断开自动暂停 |
| v2.0.4 | 看直播（TV + 移动） | Done | 直播入口 + 独立直播播放器（HLS/FLV 取流、画质切换）；过 -352 风控（WBI 签名 + buvid + live Referer）；直播 UI 对齐视频界面；直播画质跨房间持久化（LiveQualityPreferenceStore） |
| v2.0.5 | 直播分区浏览 | Done | 移动端 tab 行 + Pager，TV capsule tab + 网格；分区树 `getWebAreaList`（data.data 双层）；直播长播报错重试（LiveLoadErrorHandlingPolicy 指数退避 + stall 重载） |
| v2.0.6 | 移动端体验 + 首页分区同步 + 设置折叠 | Done | 正直播 UP 头像；缓冲期加载图标+控制栏；全屏跟随设备方向（SENSOR）；移动端首页分区配置与 TV 双向同步；设置面板折叠 |
| v2.0.7 | 非全屏三态布局 + 控制栏修复 + 直播兜底 | Done | 移动端播放器非全屏三态统一（顶栏贴顶+视频底部对齐中线+简介/评论 Tab 占下半）；全屏/暂停控制栏修复；直播间无播放地址 durl 兜底；TV 焦点/选中态打磨 |
| v2.0.9-alpha.4 | 移动端设置页补日志查看/导出 | Done（编译绿，运行时待真机） | 新增 `MobileLogsScreen`（触屏版日志列表+内容查看+分享导出+手动开始/停止录制），`MobileSettingsScreen` 加「系统设置/日志」入口，`SettingsActivity` 接 `showLogs` 导航；复用共享 `LogCatcherUtil`，镜像 TV `SettingsLogsColumn` 但去掉 D-pad 焦点逻辑。**运行时待真机**：列表显示实时/崩溃/手动文件、点开查看、分享、录制开关 |
| 稍后再看（移动端闭环） | 动态分页加列表 tab + 播放器添加入口 | Done（编译绿，运行时待真机） | 对照 BV ToView 仅读列表范式：①**列表 tab**：`MobileFeedScreen` 动态分页新增第 2 位"稍后再看"（动态/历史/稍后再看/收藏/追番/播放列表），新建 `MobileToViewPage`（仿 `MobileFavoritePage`：下拉刷新 + viewAt/max 双游标分页 + 空/错态），`UserFeedRepository.getToViewPage` GET `/x/v2/history/toview`；②**添加入口**：B站播放器互动行（赞/投币/分享后）加"稍后再看"按钮 `addToView(aid)`；③**修 addToView URL bug**：原 POST 指向列表接口 `/x/v2/history/toview`，改为 `/add` 并补 buvid3/4 风控指纹；④新增 `ic_player_toview.xml` 时钟图标与 `nav_toview`/`toview_*`/`player_control_toview` 文案。云编译 `d4242dc` 绿（首轮 `fromToViewItem` 漏 `VideoSummary` 必填 view/danmaku/pubdate/badge 编译错已修）。**真机待验证**：播放器加"稍后再看"→ 动态 tab 稍后再看列表 → 点击续播 |
| 移动端主题（4套暗色 + 明/暗模式） | 移动端接入 homeThemeVariant；再叠加独立明/暗模式 | 完成（编译绿，运行时待真机） | ①此前移动端不读 `LocalHomeColors`、`BiliTvTheme` 是 no-op，切主题仅存值不生效——MobileApp/SettingsActivity 包 `BiliMobileTheme`，把 TV 同源 HomeThemes 映射成 Material 深色 scheme；②加 `AppAppearanceMode`（Dark/Light/Auto）独立于 4 套主题，`HomeThemes.lightFromVariant` 亮色底（中性浅底 + 各 variant 加深一档 accent 保证对比 ≥4.5:1，见 `tmp/light-theme-palette.html`），`homeToMaterialScheme(colors,isDark)` 分支 dark/lightColorScheme；③设置页「界面与交互」加「外观」枚举行；6 语言补 `settings_appearance_*`。TV 端走 LocalHomeColors 不消费 appearance。**真机待验证**：移动端切「浅色」整页变亮、切「跟随系统」随系统主题切换 |

## P11 YouTube 内容集成（InnerTube）

> 背景：把 YouTube 内容加进原生 搜索/首页热门/动态 三个入口。数据来自 FreeTube / YouTube.js（MIT）的 InnerTube 私有 API，biliMT 用 Kotlin 独立重写协议（不复用 AGPL 代码）。关键 API 实测发现记录在 `docs/youtube-api-notes.md`。

| ID | 任务 | 状态 | 验收/备注 |
| --- | --- | --- | --- |
| P11-01 | InnerTube 数据层 | Done | 新增 `core/youtube/` 包：`YoutubeConstants` / `InnerTubeClient` / `YoutubeModels` / `YoutubeParsers` / `YoutubeRepository` / `YoutubeChannelStore`；guest 认证（visitorData protobuf 编码）无需 key；`VideoSummary` 加 `source` 字段（bili/youtube）；`AppContainer` 注册 `youtubeRepository`；`VideoRepository` 转发 youtube 方法 |
| P11-02 | 搜索 B站/YouTube 来源切换 | Done | TV `SearchScreen` + 移动 `MobileSearchScreen` 顶部加来源切换；YouTube 走 continuation 分页；B站排序只在 B站来源显示。**alpha.67**：TV toggle 由双 pill 改成**单 pill 按确认(OK)循环切换**（B站↔YouTube），消除未选中态像「禁用」的歧义；移动端不变 |
| P11-03 | 首页 YouTube 热门分区 | Done | `HomeSection` 加 `YoutubeTrending`；`VideoRepository.getHomeSectionVideos` 特判调 `youtubeTrending`；移动/设置面板通用渲染自动生效 |
| P11-04 | 动态页 YouTube 关注 tab | Done | `UserFeedTab` 加 `YoutubeSubscriptions`（TV 8 处 switch 补齐）；移动 `MobileFeedScreen` 加第 5 个 tab；免登录；空频道回退热门 |
| P11-05 | 首页迁移自动启用 YouTube 分区 | Done | 新增 `HomeSectionsYoutubeMigrationV1` 一次性把 `YoutubeTrending` 加入启用集，修复已装设备不显示 |
| P11-06 | 反爬与 renderer 解析修复 | Done | 实测：`zh-CN/CN` locale 触发反爬（搜索返回「出了点问题」拦截页），改 `en/US` 正常；通用热门 `FEtrending` 已被 YouTube 废弃（400），改用 topic 热门（游戏/体育/播客）返回 `gridVideoRenderer`；parser 统一收集 `videoRenderer`/`gridVideoRenderer`/`compactVideoRenderer`/`lockupViewModel` 四种格式，gridVideoRenderer 时长在 `thumbnailOverlayTimeStatusRenderer`；用户确认搜索/热门可用 |
| P11-07 | YouTube API 笔记文档 | Done | 新建 `docs/youtube-api-notes.md`，记录 InnerTube 请求形态、反爬规避、renderer 解析、废弃端点、播放难点 |
| P11-08 | 设置页 YouTube 频道管理 | Done | 新增 `YoutubeParsers.parseChannelInfo`（解析频道页 c4TabbedHeaderRenderer/channelMetadataRenderer/microformat 取 channelId+名称）和 `YoutubeRepository.resolveChannel`（归一化输入，`/browse` 支持 @handle / UC... 频道 ID）；TV `SettingsScreen` 新增「YouTube 内容」区 + `SettingsYoutubeChannelsColumn` 右面板（内嵌轻量 D-pad 键盘、添加/清空、频道行删除），懒索引重编号；移动 `MobileSettingsScreen` 新增 `MobileYoutubeChannelsPanel`（TextField 输入+删除列表）；`AppShell`/`SettingsActivity` 接线 store+repo；`assembleDebug` 云编译通过；打测试 tag 验证 |
| P11-09 | Phase 2 YouTube 播放 | Done（编译绿，运行时待真机） | `POST /player`（WEB→ANDROID 回退）解析 `adaptiveFormats`，按 codec 偏好挑视频 + 最佳音频，产出 progressive `PlaybackInfo`（segmentBase=null）；TV/移动播放器走 progressive `MergingMediaSource`（镜像 PGC）+ 门控 B 站副作用（heartbeat/元数据/弹幕）；`YoutubeJsExecutor` 隐藏 WebView JS 引擎（eval 桥、懒建、会话级复用）；`YoutubeNDecryptor` 拉 base.js + 隐藏 WebView 整体 eval + 正则识别 transform 解密 `n`；`YoutubeBotGuard` jnn PO token 结构占位、失败降级直连。**运行时依赖真机**：`n` 解密（正则法，base.js 常变）与 PO token（jnn WASM 完整性校验）无法云编译验证，需真机手测迭代 |
| P11-10 | 移动播放器 YouTube 简介/评论 Tab | Done（编译绿，运行时待真机） | 修复 YouTube 进视频后简介 Tab 永转圈、评论 Tab 空白：`YoutubeModels` 加 `YoutubeVideoDetail`/`YoutubeComment`/`YoutubeCommentPage`；`YoutubeParsers` 加 `parseVideoDetail`（`/player` videoDetails.shortDescription + microformat.publishDate）与 `parseCommentPage`/`parseCommentRenderer`（`/next` 递归收 `commentRenderer` + section 内 continuation，防御回退全根）；`YoutubeRepository` 加 `getVideoDetail`/`getComments`，`VideoRepository` 转发；移动播放器 `MobilePlayerIntroCommentTabs` 按 `isYoutube` 分流到新增 `MobileYoutubeIntroTab`（标题/频道行/观看·发布时间/简介，无 B 站互动行，详情失败显示「简介暂不可用」）与 `MobileYoutubeCommentList`（`/next` continuation 续页 + 去重，映射成 `Comment` 复用渲染，无计数/无排序）；相关视频 effect 对 YouTube 加守卫。**运行时依赖真机**：`/next` 评论结构与 `/player` videoDetails 字段随 YouTube 改版可能漂移，需真机手测迭代 |
| P11-11 | 移动端 YouTube UP主页关注/加入播放列表/动态播放列表tab | Done（v2.0.8-alpha.11，编译绿，运行时待真机） | `VideoSummary` 加 `channelId` 并补 `toVideoSummary`、订阅流给空 channelId 视频注入所属频道；新建 `YoutubePlaylistStore`（DataStore 本地播放列表，免登录）+ `MobileYoutubeChannelScreen`（频道名+关注按钮+continuation 分页网格，镜像 MobileUserSpaceScreen）；`MobileVideoCard` 放开 owner 点击（YouTube 按 channelId）+ 长按 `combinedClickable` 加/移除播放列表；`MobileApp` 新增 `youtubeChannelRequest` 覆盖层 + `openOwner` 按 source 分流 + `playQueue` 连播队列；`MobilePlayerScreen` YouTube 简介 tab 加"加入播放列表"按钮 + 播放列表播完自动连播下一项；`MobileFeedScreen` 加播放列表 tab，`MobileYoutubePlaylistPage` 含编辑删减模式。**运行时依赖真机**：进频道解析、关注持久化、长按/按钮 toggle、连播切换需真机手测 |
| P11-12 | 多播放列表 + 播放器◀▶/去弹幕/相关视频=列表后续 | 实施中（编译绿待云编译，运行时待真机） | `YoutubePlaylistStore` 重写为多命名列表（`YoutubePlaylist(name,videos)`，新 key `youtube_playlists`，旧扁平列表迁移进「默认」；`playlists`/`addVideo`/`removeVideo`/`createPlaylist`/`replaceVideos`/`moveVideo`/`migrateLegacyIfNeeded`）；长按不再自动 toggle——新增 `MobileYoutubeLongPressSheet`（底部菜单「加入播放列表」）+ `MobilePlaylistPickerDialog`（列列表勾选加入/移除 + 新建列表），`MobileApp.onLongPress` 改弹菜单；`MobileYoutubePlaylistPage` 改两层（第一层列表选择+新建，第二层单列 + `detectDragGesturesAfterLongPress` 长按拖动排序 + 编辑删减）；`MobilePlayerScreen` 播放列表内加◀▶上/下一个按钮（`playQueue` 定位 + 就地切 `activeRequest`）、所有 YouTube 隐藏发送弹幕按钮/输入栏、相关视频改 `playQueue.drop(curIndex+1)`、简介 tab「加入播放列表」改弹选列表 + 新增相关视频单列区。**运行时依赖真机**：多列表增删/拖动排序、◀▶连播、相关视频、弹幕隐藏需真机手测 |
| P11-13 | 动态页统一流:B站动态 + YouTube 关注合并(TV+移动) | 实施中(代码改完待云编译) | `VideoRepository` 加顶层 `mergeByPubdate`(concat+按 pubdate 倒序) + `YoutubeFeedTimeoutMs=5000`;TV `UserVideoFeedScreen` 移除 `UserFeedTab.YoutubeSubscriptions` 与 `feedState.youtube`,`loadDynamicFirstPage` 增 `coroutineScope`/`youtubeChannels` 参数,`type=="video"` 且有频道时后台 `mergeYoutubeIntoDynamic`(withTimeoutOrNull 5s 兜底,就绪后与 B 站动态合并重排;Empty→Success(yt)),登录门槛改为「合并 tab 有频道也可免登录」,删 `loadYoutubeSubscriptions`,`DynamicFeedContent` 增 `youtubeChannels`;移动 `MobileFeedScreen` 删第 4 个「YouTube关注」tab(播放列表前移到下标 4),外层登录 gate 放行「动态+有频道」,`MobileDynamicScreen` 增 `youtubeChannels` 参数并在 `loadFirstBody` 后台合并 YouTube;删 `MobileYoutubeSubscriptions`/`YoutubeFeedState`。分页不变(load-more 只拉 B 站 offset 页追加尾部)。**运行时待真机**:交错合并、5s 兜底、免登录 YouTube-only、独立 tab 消失 |
| P11-14 | YouTube 高清播放(Tier 1:`s` 解密 + adaptive 首选 + DASH 播放 + 硬件过滤 + 多档清晰度;Tier 2:PO token) | 实施中(2.0.10-alpha.50:PO token+SABR 高清全链路打通,n-decrypt 经 watch 页 harvest 打破;alpha.49 起 watch 优先+加载步骤 UI,初始加载 15s→6s;60s 断崖 alpha.48 主动轮换曾不生效——evict 误杀共享 sid + 触发按 cumulative 被 audio 领先虚高提前轮换,alpha.50 真正修:EOF 交棒/evict 守卫 + 按 playhead 触发 + 阈值 45s;alpha.51 真机仍 0 轮换触发(playhead 触发键够不到死亡点:会话死在 playhead~43s 时 cumulative 已到 60s 服务端上限)→ 改走 FreeTube 式「移动播放点」Track A 诊断:段请求 URL 加 `startTimeMs`+`sq`(每请求自锚定绕开会话窗口上限,FreeTube 无轮换即靠此),body playerTimeMs 仍 playhead(alpha.44 防 60s 断崖),待真机验服务端是否按 URL 锚点回段、播放能否越 60s;embed 页采集回退已删(Error 153 不可解,从未成功捕获一次,row 77);alpha.52 统一 60s 窗口会话模型(详见 §6.7 row 78):后台多会话预取解决连续性(maybeTriggerRotation 触发键 playhead→cumulative,距锚点≥40s 后台 harvest 下个窗口、≥60s 耗尽无缝切换,播放可越过 60s)+ 手动选播放位置 seek(目标 X 锚定会话 floor(X/60)*60 + startMs 透传,PlayerScreen/MobilePlayerScreen routeSeek 走重新起播免双 init 崩,窗口内复用会话/跨窗口新 harvest,resetForSeek 代际丢弃 seek 后过期预取);详见 `docs/youtube-hd-playback.md` §6.7 row 76/77/78;alpha.83:itag248 被暴露成视频轨——alpha.82 mimeType 方案真机证伪(NewPipe ItagItem 把 itag248 标 VIDEO_ONLY/WEBM → mimeType `video/webm` → `MimeTypes.isVideo` 过滤不掉),改按 codec 过滤(可靠信号=原始 /player codecs 参数,itag248=opus),详见 `docs/youtube-hd-playback.md` alpha.83 段);⚠️ **alpha.83 更正(已推翻)**:alpha.83 自身推的 codec dump 真机打印 `248=vp9`,itag248 是 **720p VP9 视频**而非 Opus 音频,整套 itag 归因被推翻;RELOAD 真因是 NewPipe visionOS 拿到**未 attested 的 ustreamerConfig**(LibreTube 默认走 Piped 拿已 attested 的 WEB-bound config 故能播),与 itag 选择/分类/visionOS ClientInfo 均无关;修复方向=接 Piped 后端,详见 `docs/youtube-hd-playback.md` 「alpha.83 更正」段 | 方案落盘 `docs/youtube-hd-playback.md`。**实测结论**:无 PO token 时 YouTube 在所有客户端剥掉 adaptive 高清 url(只剩 progressive 360p),ANDROID 客户端也剥——高清唯一前置是 PO token。Tier 1 已落地:`s` 解密(`YoutubeSDecryptor`)+ adaptive 首选 + 合并 WEB+WEB_EMBEDDED+ANDROID 候选 + `pickVideo` 最大化分辨率 + `CodecCapabilityProbe` 硬件过滤 + `initRange`/`indexRange` 填 segmentBase 走 DASH + 多档清晰度。Tier 2 已落地:打包 bgutils-js(MIT)进 `assets/youtube/bgutils.js` + `YoutubeBotGuard` 完整流程(challenge `/att/get` → descramble → interpreter 加载 → snapshot → GenerateIT → mint → 视频 ID 绑定 PO token)+ resolver 注入 `/player` `serviceIntegrityDimensions.poToken`。**PO token 真机调试已修掉 4 个障碍**(详见 `docs/youtube-hd-playback.md` §6.7):① WebView 重建导致 `__runSnapshot` 丢失 → eval 抛错检测重建 + 验证重载;② WebView shell 未就绪就 eval → `onPageFinished` 置位 `shellReady` + `awaitShellReady`;③ pollState 双重 JSON 编码解析;④ GenerateIT 响应 token 在 index 3。**已跑通**:challenge → interpreter → snapshot(`botguardResponse`)→ GenerateIT → mint → `PO token minted`(128B)。**alpha.4 诊断**:带/不带 token 的 WEB /player 响应**完全一样**(`rawAdaptive=30 firstUrl=EMPTY`)→ token 在 body 里但被 YouTube 判无效;对照 youtubei.js `#buildContext` 补浏览器指纹字段。**alpha.5 诊断**:challenge context 补指纹**生效但没解决**(`challenge context: os=Windows/10.0 browser=Chrome/126.0.0.0 device=/ mem=8000000 tz=Asia/Shanghai`),adaptive 仍 0;token 铸造管线正常(`webPoSignalOutput diag={"length":1,"isFunc":"function"}`、GenerateIT `["<token>",43200,100]`)。**本次修改**:按 FreeTubeAndroid 加 **WEB_EMBEDDED 客户端**回退(常量 + Client enum + buildContext 共享 WEB 指纹 + buildWebViewHeaders clientNameId=56;resolver 循环 WEB→WEB_EMBEDDED→ANDROID,viaWebView 同 WEB)。**待真机验证**:WEB_EMBEDDED 是否返回带 url 的 adaptive(>0);若仍 0,加 visitorData 绑定诊断(dump /att/get 与 /player 的 visitorData 是否一致)。**其余待真机**:DASH 出帧、4K 无硬解回退 | **2.0.10-alpha.6 + 根因定位**:WEB_EMBEDDED 死路(每个视频 Error code 152-18,客户端版本过期+缺 embed 授权,排除);三客户端 adaptive 全剥空、带/不带 token 响应一致。**根因(核对 FreeTube botGuardScript.js + youtubei.js HTTPClient 源码)**:botguard 铸 token 全链路与 FreeTube 逐字节一致,token 有效;但 **/player 必须带与 visitorData 配对的 `VISITOR_INFO1_LIVE` cookie**(youtubei.js 每请求设 Cookie,值==visitorData proto base64)才能把 token 绑定真实会话。我们 sw.js_data 抓取时**随机注入** `VISITOR_INFO1_LIVE=randomId()`、/player 却只带 `X-Goog-Visitor-Id` → 无法绑定 → 剥空 adaptive。**本次修复**:①buildWebViewHeaders 显式 `Cookie: VISITOR_INFO1_LIVE=currentVisitorData(); PREF=tz=Asia.Shanghai` 覆盖 WebView 不配对 cookie;②OkHttp 分支加同款 Cookie 头;③sw.js_data 去随机 VISITOR_INFO1_LIVE 注入;④/player 诊断加 `cookieV1L` 真机确认配对。**待真机验证**:adaptive firstUrl 是否 EMPTY→present | **2.0.10-alpha.7 真机诊断**:cookie 配对**确认生效**但 adaptive **仍剥空**(`cookieV1L`==visitorData 三客户端都带,token 铸对注入,`rawAdaptive=30/31 firstUrl=EMPTY`);**VPN 下重测也剥空**(只播 360p progressive)→ **IP 网络限制排除**,cookie 配对理论证伪。**剩余差异**:youtubei.js 会话 cookie 是抓真实首页拿的完整一套(CONSENT/SOCS/VISITOR_INFO1_LIVE/PREF),我们只发 VISITOR_INFO1_LIVE+PREF;YouTube 对无 consent cookie 会话判"未同意浏览"常只回 progressive。**本次修改**:①`captureSessionCookies` 抓真实首页捕获全套 Set-Cookie 存 `sessionCookies`;②buildWebViewHeaders/OkHttp/att-get 改用完整会话 cookie;③决定性诊断:dump 第一条 adaptive 完整 JSON + 全表扫描 url/cipher/sabr/pot 字段。待真机验证:adaptive 是否解锁 | **2.0.10-alpha.8 决定性诊断**:raw dump 证明 YouTube 对 guest+token 会话**拒签所有 DASH 流**——40 条 adaptive 全是纯元数据(initRange/indexRange/contentLength/qualityLabel 齐全),**无 url/cipher/sabr/pot**;ANDROID 仍给 progressive(360p 能播),WEB 连 progressive 都不给。token 结构在 body(128B serviceIntegrityDimensions)、visitorData 配对正确、session cookies 捕获(YSC+VISITOR_INFO1_LIVE,**无 CONSENT**)。结论:token 结构性有效但**信任度不足以解锁签名流**。**根因锁定**:botguard VM 环境指纹与 context 自相矛盾——context 声称 `os=Windows/10.0 browser=Chrome/126`(桌面),但 VM 跑在安卓 WebView:UA 虽覆盖为桌面 Chrome,`navigator.platform=Linux aarch64`、`userAgentData.mobile=true`、`plugins` 空、`screen` 安卓物理像素、`maxTouchPoints=5`。FreeTubeAndroid 用 CDP `setDeviceMetricsOverride` 让 VM 呈现一致桌面指纹。**alpha.9 修复**:js_shell.html 头部加内联 polyfill,在 document 解析时(早于任何 eval 的 interpreter)覆盖 `navigator.platform/oscpu/vendor/appVersion/deviceMemory/hardwareConcurrency/maxTouchPoints/language/languages/webdriver/userAgentData/plugins/mimeTypes/connection` + `screen.*` + `window.devicePixelRatio/outer*/inner*/screenX/Y` 为桌面 Chrome 值;YoutubeBotGuard.runSnapshot 加 `VM fingerprint=` 诊断 dump 确认真机生效。**待真机验证**:VM fingerprint 是否桌面值 + adaptive 是否解锁 | **2.0.10-alpha.9 真机诊断**:VM 指纹 polyfill **完全生效**(`VM fingerprint={"platform":"Win32","uaMobile":false,"uaPlatform":"Windows","plugins":5,"screenW":1920,"screenH":1080,"dpr":1,"touch":0,"webdriver":false,"mem":8,"cores":8}`),但 adaptive **仍全剥空**(`rawAdaptive=40 firstUrl=EMPTY`,带/不带 token 响应逐字节同构)→ **VM 指纹假设被证伪**。用户确认 FreeTubeAndroid 同机无登录能拿 1080p(1080p 只能走 adaptive DASH,progressive 最高 720p)→ 前提成立,必还有差异。**核对 FreeTube botGuardScript.js 发现关键差异**:FreeTube 的 **GenerateIT 在 WebView 同源发,自动携带完整浏览器 cookie(含 VISITOR_INFO1_LIVE)+ visitorData**;我们 OkHttp 直发 GenerateIT **只带 Content-Type/x-goog-api-key/x-user-agent/User-Agent,无 Cookie 无 X-Goog-Visitor-Id** → integrityToken 未绑定到会话 → 最终 PO token 无效 → /player 拒签 DASH。**alpha.10 修复**:`InnerTubeClient.currentSessionCookies()`/`currentVisitorData()` 改 public;`YoutubeBotGuard.generateIntegrityToken` 的 GenerateIT 请求加 `.header("Cookie", currentSessionCookies())` + `.header("X-Goog-Visitor-Id", currentVisitorData())` + 诊断 log dump cookie/visitor。**待真机验证**:GenerateIT cookie 是否带上 + adaptive 是否解锁 | **2.0.10-alpha.10 真机诊断**:GenerateIT cookie 已带上(`GenerateIT cookie=YSC=...; VISITOR_INFO1_LIVE=CgtONTJpa3U1bjFpOCik2tXT visitor=CgtONTJpa3U1bjFpOCik2tXT`),但 adaptive **仍全剥空** → **GenerateIT cookie 假设被证伪**。真机日志确认 token 全链路正常:minter 产生(`webPoSignalOutput={"length":1,"isFunc":"function"}`)、GenerateIT 返回 Shape B(`["<token>",43200,100]`)、token 注入 /player(`bodySID=present bodySIDToken=124B`)、signatureTimestamp 解析(20670)——但带/不带 token 响应仍逐字节同构 → **token 被 YouTube 完全忽略,即密码学无效**。**核对 FreeTube poTokenGenerator.js 发现两个新差异**:(1) FreeTube 的 token 生成跑在 **Electron 独立 cookie-less session**(`session.fromPartition('potoken',{cache:false})`),GenerateIT 也**不带 Cookie/X-Goog-Visitor-Id**——alpha.10 加的 cookie 是多余且非根因;(2) FreeTube 的 **/player 走主 Electron session 带真实浏览器 cookie**,而我们的 WEB /player 走 WebView `fetch`,**`Cookie` 是 fetch 的 forbidden header 被浏览器静默忽略** → WebView 原生网络栈不带会话 cookie → token 无法绑定会话。**alpha.11 修复**:`YoutubeJsExecutor.fetchViaWebView` 把 `Cookie` 头改写入 `CookieManager.setCookie("https://www.youtube.com", ...)` 让原生网络栈自动携带;js_shell.html 补 `navigator.vendorSub/productSub/appCodeName/appName/product/doNotTrack/pdfViewerEnabled` + `window.chrome` polyfill(安卓 WebView 缺/不同的强指纹信号);`YoutubeBotGuard.runSnapshot` 的 VM fingerprint 诊断扩到实际 `navigator.userAgent`/`window.chrome`/`document.URL`/`baseURI`;`InnerTubeClient.postJson` /player 诊断加 ctxOs/ctxBrowser/ctxMem 与 challenge context 对比。**待真机验证**:WebView /player 是否带上 cookie + adaptive 是否解锁 | **2.0.10-alpha.11 真机诊断**:VM 指纹**完全桌面化**(`ua=...Chrome/126.0.0.0 platform=Win32 uaMobile=false plugins=5 chrome=object docURL=https://www.youtube.com/`)、/player context 与 challenge context **逐字段一致**(`ctxOs=Windows/10.0 ctxBrowser=Chrome/126.0.0.0 ctxMem=8000000`)、CookieManager 写入生效——但 adaptive **仍全剥空**(`rawAdaptive=30 firstUrl=EMPTY`)。**Cookie/VM 指纹/context 匹配假设全被证伪**。**用户提示 FreeTube 播放有 SABR 后退时间 → 核对 FreeTube createLocalSabrManifest 发现根因候选**:FreeTube **不用 legacy DASH 直链**,而是用 `/player` 响应的 **`streaming_data.server_abr_streaming_url`(SABR 基址)+ adaptive 元数据(itag/initRange/indexRange)** 构建 SABR manifest,把 poToken 传给 SABR 流播放。**即 YouTube 对 guest+token 会话不再在 adaptiveFormats 里给签名直链,而是期望客户端走 SABR 协议**——我们死磕 legacy DASH 的 url 空是方向错了。**alpha.12 修改**:`YoutubePlaybackResolver` 诊断加 `sabrUrl=present/ABSENT` dump(带/不带 token 都加),确认 /player 是否真有 `server_abr_streaming_url`。**核对 FreeTube Watch.js 源码修正**:FreeTube **不是"换成" SABR**,而是**优先 SABR、失败回退 legacy DASH**——决策逻辑:①有 `server_abr_streaming_url` **且** `player_config.media_common_config.media_ustreamer_request_config` 才走 SABR;②否则 adaptive[0] 有 url/cipher 走 legacy DASH;③否则 legacy 360p 兜底。SABR manifest 由 adaptive **元数据**(itag/initRange/indexRange)构建,不依赖 url,我们剥空的 adaptive 元数据齐全。**alpha.12 补**:诊断再加 **第二道闸** `ustreamerCfg=present/ABSENT` dump(带/不带 token 都加),一次真机确认 SABR 两个前置条件。**待真机验证**:sabrUrl 是否 present + ustreamerCfg 是否 present;若都 present → 实现 SABR 播放(构建 manifest + 走 SABR 流 + 传 poToken);若 sabrUrl ABSENT → FreeTubeAndroid 同机 1080p 必走 legacy DASH(adaptive 带 url),token 仍无效,SABR 不是答案 | **2.0.10-alpha.13 真机诊断(sabrUrl=ABSENT,证伪 SABR)+ 移动一致化根因**:真机日志三 case(WEB 带/不带 token、ANDROID)全部 `sabrUrl=ABSENT` → SABR 第一道闸不存在,FreeTube 决策逻辑根本不会走 SABR,**SABR 方向整个关闭**,ustreamerCfg 第二道闸无需验证。反推:FreeTubeAndroid 同机无登录能拿 1080p,其 /player 响应 adaptive 必带签名直链 url → **FreeTube 的 token 是真的,我们是密码学无效**。**系统性 diff FreeTubeAndroid 源码(已加本地参考上游 MarmadileManteater/FreeTubeAndroid development)后锁定根因**:FreeTubeAndroid 全链路**零 UA 覆盖、一致地移动**——原生移动 WebView UA、原生移动 VM 指纹、sw.js_data(移动 UA)报 Android context(osName=Android/browserName=mobile Chrome);`platform=DESKTOP`(youtubei.js device_category 默认 desktop,非差异)。**而我们 alpha.8/9 把 UA/VM 指纹/context(os=Windows)全硬成桌面**——Android 真机上跑桌面 polyfill = **假环境,与真实设备不一致** → YouTube 拒签 DASH。youtubei.js `#adjustContext` WEB 分支不覆盖任何字段(平台保持 DESKTOP、formFactor 保持 UNKNOWN),`#buildContext` 的 osName/browserName 来自 sw.js_data device_info。**alpha.13 修复(移动一致化)**:①`YoutubeConstants.MobileUserAgent`(移动 Chrome UA);②`YoutubeJsExecutor` WebView UA 桌面→移动;③`InnerTubeClient` WEB 客户端 UA、buildWebViewHeaders、challenge、sw.js_data、getText、captureSessionCookies、buildContext userAgent 全改移动;④`YoutubeBotGuard.generateIntegrityToken` UA 改移动;⑤`YoutubePlaybackResolver` watch 页+base.js UA 改移动;⑥`js_shell.html` **删除桌面指纹 polyfill**(露出原生移动指纹,与移动 context 一致)。**待真机验证**:VM fingerprint 是否移动值 + context osName 是否 Android + adaptive 是否解锁(url 从 EMPTY→present) | **2.0.10-alpha.14 诊断根因修正——camelCase 假阴性推翻 alpha.13「SABR 证伪」+ n-decrypt plasma 失配**:核对 FreeTubeAndroid `src/renderer/views/Watch/Watch.js` `createLocalSabrManifest` 与 `src/renderer/helpers/api/local.js` 发现**致命字段名错位**:FreeTube 代码里读的全是 **snake_case**(`streaming_data.server_abr_streaming_url`、`player_config.media_common_config.media_ustreamer_request_config.video_playback_ustreamer_config`、`adaptive_formats[0].signature_cipher`)——但这是 **youtubei.js 库端把 raw InnerTube 响应的 camelCase 转成 snake_case 后的形态**,不是 raw JSON 里的 key。我们 `YoutubePlaybackResolver` 读的是 **raw /player JSON(camelCase)**:`streamingData`/`adaptiveFormats`/`formats`/`signatureCipher`/`url`/`itag` 全用 camelCase 且**全部解析成功**(rawAdaptive=40、metadata 齐全)——唯独 SABR 两道闸用了 snake_case `server_abr_streaming_url`/`player_config.media_common_config...` → **raw JSON 里根本没这俩 snake_case key** → alpha.13 三 case 全报 `sabrUrl=ABSENT ustreamerCfg=ABSENT` 是**假阴性**,不是 YouTube 真没给 SABR。**这推翻 alpha.13「sabrUrl=ABSENT → SABR 方向整个关闭」的结论**——SABR 方向**重新打开**。旁证:alpha.8-13 一直困惑的「adaptive 40 条全是纯元数据(initRange/indexRange/contentLength/qualityLabel 齐全)但无 url/cipher」**正是 SABR 模式的特征形态**——YouTube 期望客户端走 SABR(serverAbrStreamingUrl + 元数据构建 manifest),adaptiveFormats 里本来就不该有 url;我们读错 key 看不到 sabrUrl,误判为「YouTube 拒签 DASH」。**alpha.14 修改**:`YoutubePlaybackResolver` 诊断把 SABR 字段读全改 camelCase(`serverAbrStreamingUrl`、`playerConfig.mediaCommonConfig.mediaUstreamerRequestConfig.videoPlaybackUstreamerConfig`),并补 `streamingData.keys`/`playerConfig.keys`/首条 adaptive 全 key 的**原始 key 全量 dump**——一次真机坐实「camelCase 假阴性」理论与 SABR 数据是否真 present。**n-decrypt 独立根因(同步定位,本次未修)**:alpha.13 progressive itag=18 回退 403,日志 `YtNDecrypt: could not locate transform name`。拉当前 base.js(`player-plasma-es6-en_US.vflset/base.js`,player id 95daa498,1480318B)发现 YouTube 已切 **plasma 播放器变体**,经典 n-transform 锚点 `.get("n"))` **彻底不存在**——`YoutubeNDecryptor` 三条正则全依赖 `.get("n")` 或 `(b=...` 形态,对 plasma 全失配 → n 原样未动 → googlevideo 403。需按 plasma 结构重写 n-transform 定位(待补)。**待真机验证(alpha.14)**:①`streamingData keys` 是否含 `serverAbrStreamingUrl`;②`playerConfig keys` 是否含 `mediaCommonConfig`→`mediaUstreamerRequestConfig`→`videoPlaybackUstreamerConfig`;③sabrUrl/ustreamerCfg 是否从 ABSENT→present。若 present → SABR 数据齐全,下一步实现 SABR 播放(Media3 无原生 SABR source,需自建 SABR media source/数据源,工程量大);若仍 ABSENT → 诊断已无歧义,转其它拿流路径(yt-dlp android 客户端直链?)。n-decrypt 不影响 SABR 主路径(SABR url 自带 n 仍需解,但先确认 SABR present 再投入) | **2.0.10-alpha.14 真机结果——SABR 确认成立(camelCase 理论坐实)+ plasma WASM n-decrypt 深挖**:真机日志(15:48,videoId=m-zCHXBbO58)直接坐实 row 38 的 camelCase 理论:`WEB streamingData keys=[expiresInSeconds, adaptiveFormats, serverAbrStreamingUrl]`、`ANDROID streamingData keys=[expiresInSeconds, formats, adaptiveFormats, serverAbrStreamingUrl]` → **`serverAbrStreamingUrl` 真的 present(WEB 918B、ANDROID 883B,带/不带 token 都 present)**。alpha.13 的 `sabrUrl=ABSENT` 确认是 snake_case 假阴性,**SABR 方向确认成立,alpha.13「SABR 证伪」彻底推翻**。adaptive 仍是 40/41 条纯元数据(无 url/cipher)——正是 SABR 模式形态(YouTube 期望客户端走 serverAbrStreamingUrl + 元数据构建 manifest)。**gate 2(ustreamerCfg)诊断需修正**:我们读了子层 `playerConfig.mediaCommonConfig.mediaUstreamerRequestConfig.videoPlaybackUstreamerConfig` 报 ABSENT,但 `mediaCommonConfig` 在 playerConfig keys 里;FreeTube Watch.js L884 的 gate 只查**父层** `media_ustreamer_request_config`(不查 videoPlaybackUstreamerConfig),createLocalSabrManifest 才用子层——需补查父层 `mediaUstreamerRequestConfig` present/ABSENT 才能定 SABR gate 2。其它:WEB 现在 `progressiveRaw=0`(连 progressive 都不给,纯 SABR);ANDROID `progressiveRaw=1`(仍给 itag=18,resolve 落 360p)。**plasma WASM n-decrypt 根因(深挖 base.js 锁定,非正则可修)**:拉当前 base.js(`player-plasma-es6-en_US.vflset/base.js`,player id 95daa498,1480318B)发现 YouTube 已切 plasma 变体,n/sig 解码**移进 WASM**,无 JS n-transform 函数可正则定位——证据:①`get("n")` 全表只 1 处,在 `yoh` 函数里做 URL 路径 `/n/X` 规范化(非签名 transform);②经典 n-transform 调用点 `.get("n"))&&(b=Name(c))`/`&&(b=Name(c)` **全 0 匹配**,`b=Name(c)` 只 1 处且无关;③`"sig"`/`sig=`/`decipher`/`set("n"` 全 0,`nsig` 只匹配无关子串;④WASM 重度:`WebAssembly`×6/`wasm`×44/`o_W`(WASM 模块)×2,`signature`/`encrypt` 跑在 WASM 实例 `c.DY` 上(`c.DY.memory.moveOut`、`x.encrypt(I,...,iv)`)。→ `YoutubeNDecryptor` 的「正则找名 + WebView eval 调用」方案对 plasma **结构性失效**,alpha.13/14 的 `could not locate transform name` 不是正则写错是 player 变体变了。已存 memory `youtube-plasma-wasm-n-decrypt`。**下一步路径(待定,非一正则可修)**:①加 IOS InnerTube 客户端(yt-dlp 用法,常给无 n-param 直链绕开 n-decrypt,中等工作量,最务实);②实现 SABR(gate 2 父层先补诊断确认,SABR url 仍可能需 decipher 但走 poToken+ustreamerConfig);③经 WebView 调 plasma WASM 入口(找混淆入口难);④移植 youtubei.js decipher(jintr,巨大) | **2.0.10-alpha.15/16 SABR 数据三要素全确认 + Phase 2a 协议往返探针**:alpha.15 真机补查 gate 2 **父层** `mediaUstreamerRequestConfig`=present(WEB 3147B/ANDROID 3504B)→ FreeTube gating 两道闸全过;alpha.16 真机定位 `videoPlaybackUstreamerConfig` 是 proto bytes→JSON base64 字符串(`ustreamerCfgStr`=present WEB 3100B/ANDROID 3352B),`.obj()` 报 ABSENT 是类型不符假阴性。SABR 数据三要素(sabrUrl+ustreamerConfig bytes+adaptive 元数据)全齐。**Phase 1(二进制基础)已提交 513d9f3**:UmpReader(YouTube 自定义 varint,逐 case 对齐 googlevideo UmpReader.ts)/CompositeBuffer/ProtoWire/SabrProto——纯逻辑。**Phase 2a(协议引擎+诊断探针,本提交)**:SabrClient(POST VideoPlaybackAbrRequest→流式 UMP 解析→匹配 MEDIA_HEADER→收集 MEDIA→MEDIA_END)+ InnerTubeClient.sabrClientInfo()(StreamerContext.ClientInfo)+ YoutubePlaybackResolver 探针(WEB+SABR 数据齐全时发一次 init 段请求验协议往返,仅诊断不接 Media3)。**proto 字段号全量核对上游**:逐个拉 `LuanRT/googlevideo` proto 源文件核对(media_header/video_playback_abr_request/streamer_context/client_abr_state/buffered_range/FormatId/stream_protection_status/sabr_error/sabr_redirect/next_request_policy/ump_part_id),全部正确;UmpReader varint 与 UmpReader.ts 逐 case 对齐;MEDIA/MEDIA_END 首字节=headerId 与 SabrUmpProcessor 一致。**待真机(alpha.17)验证探针**:Success bytes?/InvalidPoToken?/Error? 据结果定 Phase 2b(Media3 DashMediaSource+sabr://DataSource 接线)或调 PO token 绑定 | **2.0.10-alpha.17/18 真机——探针触发协议 encode 成功,googlevideo 返 HTTP 403 空体;alpha.18 加会话传输头**:alpha.17 真机探针成功 encode+POST(`SabrSession: sabrUrl=https://rr1---...googlevideo.com/videoplayback?... poToken=88B ustreamerCfg=2288B video=itag=315(2160p)` + `fetch rn=0 body=2612B`),但 googlevideo 返 HTTP 403+空响应体(非 CDN edge、非 IP)。根因候选:SABR POST 缺会话传输头(shaka 在浏览器里自动带 UA/Cookie/visitor,我们 OkHttp 裸 POST 只设 content-type/accept/accept-encoding)。alpha.18 修:`SabrSession` 加 userAgent/cookieHeader/visitorData 字段(探针从 client.userAgent+currentSessionCookies()+currentVisitorData() 注入,与 /player 同会话);`fetch` Request 加 User-Agent/Cookie/X-Goog-Visitor-Id/Origin/Referer;非 200 时 log 全量响应头。**alpha.18 真机:传输头已带仍 403**(`Server=gvs 1.0 Content-Type=application/vnd.yt-ump Content-Length=0`——服务端识别为 SABR 但应用层拒签)→ 传输头非根因。核对 googlevideo `examples/downloader/utils/sabr-stream-factory.ts`(工作参考)发现决定性差异:`serverAbrStreamingUrl = await innertube.session.player?.decipher(...)`——SABR URL 在 POST 前必须经 player decipher(n-transform+sig);我们取的是未 decipher 原始 URL,带未解 `n` → googlevideo 经典 403。另发现 `enabledTrackTypesBitfield` 误用 0(VIDEO_AND_AUDIO)应 2(VIDEO_ONLY)。**alpha.19 修**:探针在 fromSabrData 前 `decipherSabrUrl(sabrUrl, resolvePlayerJsUrl(videoId))` 复用 nDecryptor.decrypt + dump n/sig/s/pot 存在性 + applied/NO-CHANGE;SabrClient enabledTrackTypesBitfield VIDEO→2。**alpha.19 真机:n-decrypt NO-CHANGE 坐实 plasma WASM 根因**:日志 `sabrUrl params: ...n=true sig=true pot=true` + `sabrUrl n-decrypt: NO-CHANGE` + `could not locate transform name`——SABR URL 确带 n,但 plasma 把 n-transform 移进 WASM(`r.B.encrypt(B)`/`memory.copyIn/moveOut`),YoutubeNDecryptor 三正则全依赖 `.get("n")` 形态对 plasma 全 0 匹配 → resolveTransformName 返回 null → 原样返回未解 → 403。至此排除所有候选(传输头/sig/pot/enabledTrackTypesBitfield/poToken minter 真机确认产+videoId 绑定对齐示例)→ SABR 403 根因唯一=untransformed n,plasma WASM 不可在 Kotlin 侧正则/eval 逆向(社区 yt-dlp/youtubei.js 同卡)。**⚠️ 推翻 §6.9 旧论断「SABR 是唯一不需要解 n/sig 的拿流路径」——SABR URL 同样带 n 必须 decipher**(alpha.19 真机 n=true 坐实) | **2.0.10-alpha.20——WebView 嵌入采集器(plasma 兜底,用户选定方案)**:Kotlin 跑不了 plasma WASM,改让 Android WebView 浏览器引擎(原生跑 WASM+WebCrypto)替我们做 n-transform。新建 `YoutubeSabrHarvester`(独立 WebView,不复用 YoutubeJsExecutor 单例以免导航破坏 bgutils 上下文)加载 `https://www.youtube.com/embed/<id>?autoplay=1&mute=1&playsinline=1`,`onPageStarted` 注入 fetch/XHR wrapper 截获首个发往 googlevideo.com/videoplayback 的 SABR POST(含 sabr),采集 {url(浏览器已 transform 的 n),bodyB64(含 poToken/ustreamerConfig/formatIds),status},让请求照常放行(embed 自己拿 200 即证浏览器 transform 的 n 被服务端接受)。WebView 配置对齐 YoutubeJsExecutor.createWebView(MobileUserAgent/domStorage/allowUniversalAccessFromFileURLs)+mediaPlaybackRequiresUserGesture=false(允 muted autoplay 触发播放器→SABR POST);加载前 CookieManager.setCookie(Origin,currentSessionCookies())+flush() seed 会话 cookie;poll-drain window.__sabrCaptures[0] 双解码兼容(parseCapture)。`YoutubePlaybackResolver` 探针分叉:decipherSabrUrl 返回 applied(nTransformed)走原生 SabrClient(classic 播放器回归);NO-CHANGE(plasma)走 sabrHarvester.harvest(videoId)+log 采集到的 url/status/n 对比+replaySabrCapture(剥 embed 的 rn/cpn 加 rn=0,用 SabrClient 同款传输头 POST 采集到的 body,看 WE 能否驱动 SABR:200+UMP=会话接受重放,403/Error=rn/会话冲突→alpha.21 须用新鲜 rn+embed 完整上下文)。alpha.20 仅证机制+取数据形状,alpha.21 才接 SabrSession+SabrClient+Media3。**待真机验证**:harvest status=200?(浏览器 transform 的 n 被接受)→ replay 200+UMP?(WE 能驱动)or 403(rn/会话冲突,alpha.21 调) | **2.0.10-alpha.21/22 真机——采集器链路诊断透视 + detached WebView 0 尺寸根因**:alpha.20 真机 harvest 触发但 25s 无捕获(采集器对 embed 内部行为是盲的)。alpha.21 放宽 HOOK_JS 从「只截 SABR POST」到「截所有 googlevideo 请求(POST/GET)」记录 {url,method,bodyB64,status} + WebViewClient 加 onPageStarted/onPageFinished/onReceivedError/onReceivedHttpError + WebChromeClient.onConsoleMessage(捕 embed 播放器 console) + SabrCapture 加 method 字段 + resolver 探针按 method 分流。alpha.21 真机:onPageStarted/onPageFinished 触发(页加载成功,无 consent 重定向)但 **25s 零 googlevideo 请求 + 零 console** → 播放器 JS 根本没 init。根因定位:**detached WebView 默认 0 尺寸**,YouTube 播放器 JS 检测 player 元素/视口(`window.innerWidth/Height`、`getBoundingClientRect`)为 0 → 拒绝 init。alpha.22 修:createWebView 末尾 `measure(1080×1920 EXACTLY)+layout(0,0,measuredW,measuredH)` 给真实内部尺寸 + onPageFinished eval `PAGE diag title/body/player/vp` 经 onConsoleMessage 捕获。alpha.22 真机:`PAGE diag ... player=true vp=432x768`(尺寸修复生效,player 元素 present)但抛 **Error 153(video player config error)**——embed 播放器自己的 config 请求被拒,没到拉流阶段。 | **2.0.10-alpha.23 真机——🎉 突破:watch 回退捕到 SABR POST status=200 + transformed-n(n-decrypt 阻塞打破);空 body 重放得 `sabr.malformed_config`;alpha.24 修 body 采集**:alpha.23 修:①harvester UA `MobileUserAgent`→`UserAgent`(桌面 Chrome,FreeTube 桌面 Electron 同源能播);②embed 10s 无捕获自动回退 watch 页(无 embed 权限闸);③timeout 25s→30s。alpha.23 真机(20:32):①embed 桌面 UA **仍报 Error 153**(桌面 UA 不解 embed Error 153,embed client 自身被拒,与 UA 无关);②10s 无捕获→回退 watch 页;③watch 页 SPA+播放器 JS 跑起来(`LegacyDataMixin`);④**`harvest: captured POST status=200 n=7r1hnfryz8IWTQ`**——watch 页播放器发 SABR POST 且 googlevideo 回 200!`n(harvested=7r1hnfryz8IWTQ raw=LmBYoj5Aqw1JUWm7 same=false)`——浏览器 plasma WASM n-transform 做了,**服务端接受(200 非 403)→ n-decrypt 阻塞彻底打破**;⑤但 `bodyB64=0B`(fetch hook 没捕到 SABR POST body);⑥resolver 重放(空 body,strip rn/cpn +rn=0,带 cookie/visitor/桌面 UA)→ **`SABR replay: HTTP 200 Content-Type=application/vnd.yt-ump body=31B 2c1d0a...736162722e6d616c666f726d65645f636f6e666967...`**——hex 解 ASCII=`sabr.malformed_config`(UMP 错误)。**决定性结论**:重放 URL(含浏览器 transform 的 n)被服务端接受(**HTTP 200 非 403**,403 阻塞彻底打破,n-transform 真值已拿到);服务端回 `sabr.malformed_config` **只因我们 POST 了空 body**(没 VideoPlaybackAbrRequest protobuf)——**有真 body 就能拿到 SABR 媒体流**;watch 页是可用采集源(embed Error 153 不可解,watch 页无 embed 权限闸)。**alpha.24 修 body 采集**:alpha.23 fetch hook `body=init&&init.body`——播放器用 `fetch(new Request(url,{body}))` 形态,init.body 空、body 在 Request 对象里(Request.body 是 ReadableStream 不可同步读)→ b64 返空。改 hook:`bodySrc=(init.body!=null)?init.body:input`;Request 对象走 `input.clone().arrayBuffer()`(克隆 Request 读其 body,不消耗原请求);`Promise.all([bodyPromise,responsePromise])` 后再 record(拿 status)。XHR 路径不变(send body 是 ArrayBuffer/Uint8Array,b64 直读)。**待真机(alpha.24)**:重放 bodyB64 非 0 + 期望 `HTTP 200 application/vnd.yt-ump body=NKB`(真 UMP 媒体初始化段,非 malformed_config)→ 证可驱 SABR,接 alpha.25 建 SabrSession+Media3。若仍 malformed_config→body 采集仍空(回看 hook);若 rn/cpn 冲突错(非 malformed)→alpha.25 调 URL 参数(保浏览器 cpn、重置 rn) | **2.0.10-alpha.24 真机——🎉 body 采集修复成功(4632B);重放得 105B UMP 非 malformed_config 但无 MEDIA;alpha.25 保 cpn + UMP 解析定位**:alpha.24 真机(21:01:19)`harvest: captured POST status=200 n=0U_GAg0a9dxJTg bodyB64=4632B`——**body 采集修复成功**(alpha.23 是 0B,Request.clone().arrayBuffer() hook 生效)。重放(带真 body 4632B,strip rn+cpn +rn=0)→ `HTTP 200 application/vnd.yt-ump body=105B`——**非 malformed_config**(alpha.23 是 31B sabr.malformed_config),body 被接受!手动解 UMP 首 part type=57(SABR_CONTEXT_UPDATE) size=92B——响应是 context update 不是 MEDIA_HEADER,105B 无媒体段;ASCII 含 ei 值。推测:剥了 cpn → URL 失会话绑定 → 服务端只回 context。alpha.25 修:①replay 保留浏览器 cpn(只剥 rn 重置 0 做 re-init,让 cpn+body+transport 会话一致);②replay 解析 UMP 逐 part log 类型+内容(SABR_ERROR/REDIRECT/STREAM_PROTECTION_STATUS/NEXT_REQUEST_POLICY/MEDIA_HEADER)定位 105B 真身。待真机:type=20(MEDIA_HEADER)+21(MEDIA)→成功接 SabrSession+Media3;type=44(ERROR)→据错码调;仅 type=57(CONTEXT_UPDATE)→深挖 cpn+rn 对齐。另:二次 harvest watch 页 net::ERR_CONNECTION_CLOSED(服务端短时重复 harvest 限流,非致命) | **2.0.10-alpha.25 真机——🎉🎉🎉 完整 SABR 媒体响应!n-decrypt 阻塞彻底打破;alpha.26 转驱动验证**:alpha.25 replay 保留浏览器 cpn + UMP 解析,真机(21:24)逐 part 解出 `STREAM_PROTECTION_STATUS status=1`(poToken 有效,会话完全接受)+ FORMAT_INIT_METADATA + NEXT_REQUEST_POLICY(cookie=true) + **MEDIA_HEADER/MEDIA/MEDIA_END itag 251(audio opus)+396(video av01)各 init(seg0)+seg1(seq1)**——audio seg1 101540B/6041ms + video seg1 91639B/6006ms 全拿到。**完整 SABR 媒体响应**:会话三要素(cpn+body+transport cookie/visitor)一致即服务端发媒体(alpha.24 剥 cpn 只回 context 是失会话绑定)。**至此 n-decrypt 阻塞(社区 yt-dlp/youtubei.js 同卡)经 WebView harvest 彻底打破**。**alpha.26**:不 echo 浏览器 body(只给 init+seg1 不能续),改 `SabrProto.decodeVideoPlaybackAbrRequest` 解码 body 取 poToken/ustreamerConfig/formatIds(浏览器会话绑定值)→建 `SabrSession`(用浏览器 cpn,非随机;fromSabrData 加 cpn 参数)→`SabrClient` 自构 body 驱 fetch(init)+fetch(seg seq=1)。验「我们构造的 body 能否驱动」:Success→接 Media3;context-only/Error→对齐 clientInfo/clientAbrState 或回退 echo。后续 Phase 2b Media3 sabr:// DataSource | **2.0.10-alpha.26 真机——🎉🎉🎉🎉 我们构造的 body 驱动 SABR 成功!init 237B+seg1 660025B 全拿到;研究/调试阶段结束,转 Phase 2b Media3 接线**:alpha.26 真机(21:57)harvest 捕 `POST status=200 n=yc9yaWttLj0S7Q bodyB64=2296B`(transformed-n)。`SABR drive: decoded poToken=10B ustreamerCfg=1284B audio=itag=251 video=itag=248 cpn=l9ZHsAYgJSQkIOv6`——`SabrProto.decodeVideoPlaybackAbrRequest` 解码 body 取会话参数成功(poToken/ustreamerConfig/formatIds/cpn 全拿到,用浏览器 cpn 绑会话)。**`SABR drive init(video, constructed body): Success bytes=237B headerId=1 itag=248 isInit=true contentLen=237`**——**我们构造的 init body(非 echo 浏览器 body)被服务端接受,拿到 video init 段(237B,itag 248 vp9)!** **`SABR drive seg1(video, constructed body): Success bytes=660025B headerId=1 itag=248 isInit=false contentLen=660025 dur=6006ms`**——**我们构造的 segment body 拿到 video seg1(660025B=644KB,6006ms)!** **至此 SabrClient 完全可驱动 SABR**:harvest 取会话参数 → SabrSession(浏览器 cpn)→ SabrClient 自构 body 驱 init+segment,服务端全接受。clientInfo 用我们 sabrClientInfo()/clientAbrState 用 SabrClient 默认即可——服务端不严查这俩,只看 poToken/ustreamerConfig/formatIds/cpn 会话绑定。**研究/调试阶段(alpha.17→26 跨 10 alpha)结束**:n-decrypt 阻塞(plasma WASM)经 WebView harvest 打破 → body 采集(Request.clone)→ cpn 保留(会话三要素一致)→ 完整媒体响应 → 自构 body 驱动。**Phase 2b(Media3 接线)待做**:SABR=fMP4 init+段序列(seq=0 init,seq=1/2/... 每 ~6s),音频(itag 251)+视频(itag 248)分离两流。方案:合成 DASH MPD(video+audio 两 adaptation set,每段 `sabr://...?seq=N&stream=video/audio` URL)+ 自定义 `sabr://` DataSource(open 时 SabrClient.fetch(init/seg) 喂字节);DashMediaSource 处理分离音视频。段时序 ~6s/段,seq↔时间 seq=N → (N-1)*6s~N*6s | **2.0.10-alpha.27——Phase 2b 接线完成:sabr:// streaming DataSource + 全局 registry + resolver/播放器 wiring,SABR 字节流接入 Media3 progressive,待真机验出帧出音**:把已证 SabrClient 字节流接进 Media3 让画面真播。**方案选型**:不用 DASH MPD 合成(SABR 段数/时长解析时未知,MPD 需动态 timeline 复杂),改走 progressive + 自定义 `sabr://` DataSource——SABR 是 fMP4,init+seg1+seg2+…拼接成合法 fragmented MP4,ExoPlayer MP4 extractor 按 progressive 解析;音频(251)+视频(248)分离两流 → 播放器既有 `MergingMediaSource(ProgressiveMediaSource×2)` 分支(segmentBase==null 触发)直接复用,无需新 MediaSource 类型。**新组件 3 文件**:①`SabrStreamRegistry`(进程级 `object`,sessionId→Entry(session,client) 查表;选全局 object 而非 AppContainer 注入因 DataSource 只从 Media3 拿 URI 无上下文,会话参数 poToken/ustreamerConfig/cpn 太重塞不进 URL,URL 只带 opaque sid `sabr://youtube/<sid>?stream=video|audio`);②`SabrStreamingDataSource`(implements `androidx.media3.datasource.DataSource`;open 解析 sid+stream→registry 查表→`runBlocking{client.fetch(init)}`→buffer init 字节→返 `C.LENGTH_UNSET`;read 逐段 drain,buffer 空时 fetch(seg seq=nextSeq++),Success→buffer,Redirect→session.applyRedirect+refetch 同 seq,Backoff→sleep+refetch,Error/InvalidPoToken→`C.RESULT_END_OF_INPUT` EOF;runBlocking 桥接 suspend fetch:loader 线程后台阻塞网络是 progressive DataSource 常态同 OkHttpDataSource);③`SabrAwareDataSource`(scheme 路由:sabr://→SabrStreamingDataSource,其余 http/https→OkHttp delegate;addTransferListener 转发给 http 保 B 站带宽估计)。**SabrClient 改**:requestNumber var→AtomicInteger(video+audio 两 loader 并发 fetch,rn 必须唯一);SabrSession.sabrUrl val→var + cpn 字段 + applyRedirect(newBase)(Redirect 重写 sabrUrl 重加 alr+cpn)。**resolver 改**:SABR 数据齐全分支(classic nTransformed 用 /player 数据建会话;plasma NO-CHANGE 走 harvest→buildSabrSessionFromCapture 解码 body 取 poToken/ustreamerConfig/formatIds/浏览器 cpn 建 SabrSession)→SabrStreamRegistry.register(session, client)→返 buildSabrPlaybackInfo(两条 sabr:// progressive track,segmentBase=null,元数据从 /player adaptive JSON 按 itag 取);删诊断 driveSabrFromHarvest(registry 取代)。**播放器改**:两处(PlayerScreen ~1387/MobilePlayerScreen ~525)`DefaultDataSource.Factory(context, SabrAwareDataSourceFactory(BiliMediaDataSourceFactory(...).create()))` 一行包装,非 sabr 流不受影响。**media3 1.10 契约核对**(WebFetch 官方源码):DataSource.addTransferListener 是 abstract(必须 override)→两 DataSource 都加 override;getUri 是 Java getter→Kotlin 用 override val uri 属性式避 function/property 歧义;@UnstableApi 是 lint 注解非编译期,CI 跑 assembleDebug/assembleRelease 无 lint task 不阻。**已知限制(MVP 接受)**:①不可 seek(C.LENGTH_UNSET;后续合成 DASH MPD + /player lengthSeconds 做可 seek timeline);②n-param/URL ~6h 过期,长暂停可能 mid-stream 403(后续 re-harvest on 403);③harvest ~30s 解析延迟(后续 video-detail 页后台预 harvest 缓存);④video+audio 双未知长 progressive MergingMediaSource 未真机验,若 merge 失败→降级 video-only(alpha.27b,只返 video track audioTracks 空)。**待真机**:看 YtSabr log:SabrStream open→fetch init Success→fetch seg Success(seq 1,2,3…)+audio 同→画面出帧+出音=Phase 2b 成;若视频有帧无音/merge 失败→降级 alpha.27b;若 mid-stream Error/InvalidPoToken→查 Redirect/Backoff/END_OF_TRACK vs 瞬态错 | **2.0.10-alpha.28——修 alpha.27 premature-EOF 黑屏:推进 playerTimeMs+bufferedRanges 让 SABR 服务端 lookahead 窗口前移**:alpha.27 真机「可以播放,但加载很慢,偶发只有声音黑屏,不可选清晰度」。查 YtSabr log:video 流 EOF at seq=5 + audio EOF at seq=3——服务端只发初始 ~4 段(1-4,~26s lookahead from time 0)就不再发新段 → premature EOF → 黑屏(视频先 EOF,音频再 buffer 一会儿也 EOF)。**根因(关键发现)**:SABR 是**服务端驱动**——服务端按请求的 clientAbrState.playerTimeMs + bufferedRanges 决定发哪段;alpha.27 SabrClient.fetch 硬死 playerTimeMs=0L + bufferedRanges=emptyList(),每次段请求都告诉服务端「我在 time 0,啥也没缓冲」→ 服务端 lookahead 窗口始终停在 0 → 发完初始 ~4 段拒绝前推 → 要 seq5 时服务端认为还在 0s 不给 → premature EOF。不是 token 失效/重定向/瞬态错,是请求体 playerTimeMs/bufferedRanges 没推进。**alpha.28 修**:①SabrFetchRequest 加 playerTimeMs:Long + bufferedRange:BufferedRangeInput?;②SabrClient.fetch 用 clientAbrState.playerTimeMs=req.playerTimeMs(原 0L)+ input.bufferedRanges=listOfNotNull(req.bufferedRange)(原 emptyList())+ input.playerTimeMs=req.playerTimeMs(原 0L);③SabrStreamingDataSource 加 cumulativeDurationMs 字段(open 拿 init 后 +=initResult.mediaHeader?.durationMs;read 拿每段后 +=seg.mediaHeader?.durationMs)→ buildSegRequest(e) 构造下一段请求 playerTimeMs=cumulativeDurationMs + bufferedRange=BufferedRangeInput(itag/lmt/xtags,startTimeMs=0,durationMs=cumulativeDurationMs,startSegmentIndex=0,endSegmentIndex=nextSeq-1)(报已缓冲 0..cumulative 让服务端发下一段;init 后 cumulative=0 时跳过 bufferedRange 等同 alpha.27);④fetchUntilReady 返回 SabrFetchResult.Success?(原 ByteArray?)让 read 能取 seg.data+seg.mediaHeader.durationMs。**预期**:服务端 lookahead 窗口随消费前移,video/audio 流持续 fetch seq 5/6/7… 不再 premature EOF → 黑屏消失持续播放。**待真机**:看 `SabrStream ... seq=5 ... playerTimeMs=24000` 持续出现(非 seq5 EOF)= premature-EOF 修好。加载慢=harvest ~30s(已知 MVP 限制,后续 video-detail 后台预 harvest);不可选清晰度=只用 harvested formatIds(已知 MVP 限制,后续多 quality)——这俩 alpha.28 不动 | **2.0.10-alpha.29——多清晰度选择:联网核对 FreeTube 源码证 poToken 会话级不绑 itag,sabr:// 带 itag + videoId 缓存复用免重 harvest 切档**:alpha.27 真机「不可选清晰度」根因调研——联网核对 FreeTube Watch.js createLocalSabrManifest:`this.sabrData={url,poToken,ustreamerConfig}`(poToken 单一会话级值)+`formats: adaptive_formats.map(f=>({itag,...}))`(全部 itag 进 manifest)→ poToken 会话级不绑 itag,清晰度=请求体 preferredVideoFormatIds 填哪个 itag(证伪 alpha.27「poToken 可能 itag-bound」假设)。播放器切档机制(Explore agent 摸清):切档=用 preferredQualityId 重跑 getPlaybackInfo→重建整个 MediaSource(progressive 只取 videoTracks.first())。故 SABR 多清晰度形态:qualities=全部视频 itag 菜单、videoTracks=[仅选中 itag 一条]、selectedQuality=preferredQualityId 命中菜单。但切档重跑 resolve 会重 harvest(30s)→解法:videoId 缓存(poToken/ustreamerConfig/cpn 会话级可复用 ~6h)。**改**:①SabrSession 加 videoFormats:List<FormatId>(从 /player adaptiveFormats 全收)+videoFormat(itag)查表;②SabrFetchRequest 加 videoItag;③SabrClient.fetch 用 videoFmt=req.videoItag?.let{session.videoFormat(it)}?:videoFormatId(替换硬死)+matchesFormat/MEDIA_HEADER 日志同步按请求 itag;④SabrStreamingDataSource 加 requestedItag(从 URL 解析),open/buildSegRequest 带 videoItag;⑤SabrAwareDataSource.parseSabrUri 解析 &itag= 返 Triple(sid,stream,itag?);⑥SabrStreamRegistry 加 byVideoId 反查表+registerByVideoId/getByVideoId(同视频复用会话);⑦resolver SABR 块入口 getByVideoId 缓存命中→跳过 harvest/decipher 直接 buildSabrPlaybackInfo(用 preferredQualityId 选 itag);miss 时建 videoFormats 传给 fromSabrData/buildSabrSessionFromCapture+registerByVideoId;⑧buildSabrPlaybackInfo 重写:qualities=videoFormats 按 height 降序全部 itag(带 codec 标签 AV1/VP9/H264/HEVC),selectedQuality=preferredQualityId 命中菜单否则默认 videoFormatId,videoTracks=[选中 itag 一条 sabr://youtube/<sid>?stream=video&itag=N];buildSabrTrack 视频 baseUrl 带 &itag=;加 shortCodec 辅助。**预期**:清晰度面板出多档,切档不重 harvest(缓存命中秒切),poToken 同会话复用跨 itag。**待真机**:看 SABR PlaybackInfo: qualities=N selected=itagX + 切档后 SabrStream ... itag=Y 新 itag 请求成功(非 InvalidPoToken)。风险:harvest 的 poToken 理论会话级但 harvest 路径没验过,若换 itag 报 InvalidPoToken→回退换档重 harvest(后续)。加载慢(alpha.27 问题 1)本 alpha 不动,后续 alpha.30 详情页后台预 harvest | **2.0.10-alpha.30——修 alpha.28 持续 premature-EOF(黑屏只有声音):FreeTube 源码确认三因叠加——playbackCookie 未回传 + 无-media 当 EOF + 对方 itag 未标满缓冲**:alpha.28 真机日志(audio seq7 playerTimeMs=60001 在 56 分钟视频才 60s 就断)证 playerTimeMs 推进生效但 premature-EOF **延后未消失**。派研究 agent 核对 FreeTube `SabrSchemePlugin.js` 源码,确认三个叠加根因:①**playbackCookie 没回传**(主因)——服务端每次 NEXT_REQUEST_POLICY 带 `cookie=true` 要求客户端把 playbackCookie 塞进下个请求 StreamerContext.field3,我们只 log 不回传;前 ~6 段服务端宽容,seq7 切严格校验 → 只回 policy 不回 media → 「no matching MEDIA_HEADER」→ EOF。FreeTube 原话:「不回传 cookie,服务端丢失会话连续性,你会得到正是『几段后停止』症状」。②**无-media 当永久 EOF**——200+policy 无 MEDIA_HEADER,FreeTube 当 `shouldRetry`(按 backoffTimeMs 等待+回传 cookie+重发同 seq),我们当 Error→-1→黑屏。③**对方 itag 没标满缓冲**——FreeTube 两并发流各带 `selectedFormatIds=[audio,video]` 但用 `createFullBufferRange`(durationMs/seg=Int.MAX)标对方格式「别发」;我们没标 → 服务端每流试图发双格式 → 会话状态混乱(audio itag 251 header 混进 video 响应)。playerTimeMs/bufferedRange 保留 alpha.28(seq1-6 正常返 media 证明数值可用,非 seq7 触发点)。**alpha.30 修**:①`SabrSession` 加 `@Volatile var playbackCookie: ByteArray?`(会话级——cookie 含双格式 resolution,服务端对同一会话发同一值,两 loader 共享安全);`decodeNextRequestPolicy` 加 `playbackCookieBytes`(field7 原始 bytes,opaque 透传免解码/再编码);`processUmpStream` NEXT_REQUEST_POLICY 捕获 cookie 写回 `session.playbackCookie`;`fetch` 构造 `StreamerContextInput(playbackCookie=session.playbackCookie)`(field3,encodeStreamerContext L83 已支持);②`processUmpStream` 尾部重写:有 NEXT_REQUEST_POLICY 无 media 无 Error → 返回 `Backoff(backoffMs)`(fetchUntilReady 已有 backoff 重试,sleep+continue 重发同 seq 自动带 session 已更新的 cookie),不当 Error→EOF;加 `sawPolicy` 标志;Success 透传 cookie(诊断 log 用);③`fetch` 构造 bufferedRanges:own 格式报真实已缓冲段(现有 `[0..cumulative]`)+ **对方格式 `createFullBufferRange`**(Int.MAX)对齐 FreeTube fillBufferedRanges;init 请求不发 bufferedRanges(对齐 FreeTube)。新增 `createFullBufferRange` 辅助。fetch 日志补 cookie 状态。**预期**:seq7 retry 带 cookie → 服务端持续发 media → audio 过 60s → 黑屏消失。**待真机**:看 `fetch ... cookie=true` + seq7+ 持续 `SabrStream ... seq=7/8/9 bytes=NB`(非 EOF)。**风险**:若 cookie 会话级假设错(stream-specific),两 loader cookie 互相覆盖致混乱 → 需改 per-stream cookie(较大重构)。harvest ~30s 加载慢原计划 alpha.30,现挪到 alpha.31+(本 alpha 专注 EOF 修复)。汇总文档 `docs/youtube-stream-resolution.md`(top-down:方案架构/阻断点/SABR 协议/实现细节文件级/调试问题按主题/真机样例日志/alpha.30 方案/参考资料) | **2.0.10-alpha.31 修 SABR 上下文握手缺失**:alpha.30 真机 playbackCookie 回传+Backoff 重试都已生效但仍打不开视频——每个 init 只回 `type=57(SABR_CONTEXT_UPDATE)`+`NEXT_REQUEST_POLICY backoff`,从不回 MEDIA_HEADER,8 次 backoff 后 EOF。联网核对 FreeTube `SabrSchemePlugin.js` 锁根因:FreeTube 维护 SABR 上下文握手状态机,收到 type 57 存 `sabrContexts` Map,每次请求调 `prepareSabrContexts()` 把 active 上下文编进 `streamerContext.sabr_contexts`(field5);我们永发空 `sabrContexts` → 服务端判握手未完成 → 只回 context 不发 media。修:`SabrProto` 加 `decodeSabrContextUpdate`/`decodeSabrContextSendingPolicy`+`PART_SNACKBAR_MESSAGE=67`;`SabrSession` 加会话级 `sabrContexts`/`activeSabrContextTypes`+`prepareSabrContexts()`;`processUmpStream` type 57/59 handler 填状态机;`fetch` 填 `sabrContexts/unsentSabrContexts`(替换 emptyList)+日志 `contexts=A/U`。待真机:看 `contexts=1/0`+`MEDIA_HEADER`+`bytes=NB`(非 EOF)。详见 docs/youtube-hd-playback.md §6.7 row 55 | **2.0.10-alpha.32 🎉 n-decrypt URL 类方式 PoC(推翻 plasma「锁死」结论)**:联网核对 zemer-cipher 库(223 条 config 含 95daa498=Xz)证 plasma n-transform 没被锁死,只是入口换——经典「正则找函数名+外部 eval」两重失效(正则 0 匹配 + 即便命中函数名是 IIFE 内局部外部 eval 取不到)。新方案对齐 zemer `buildNJsExpression`:`window.__nTransformFunc=function(n){var u=new g.<nClass>(url,true);return u.get('n')}` 注入到 `})(_yt_player);` 之前(IIFE 闭包内捕获 `g` 局部),外部 `evaluateJavascript` 调用;`.get('n')` 内部触发 transform(即便在 WASM 也照跑)。改 `YoutubeNDecryptor.kt`:删正则方案,加 `resolvedNClass`/`resolveNClass`/`buildNTransformExport`/`injectNExport`+16 条硬编码 `nClassByPlayerHash`+`PLAYER_HASH_PATTERNS`;`loadBaseJs` 注入+eval+probe `__nTransformFunc`;`decrypt` 调 `window.__nTransformFunc(n)`。**零回退**:harvest 代码+resolver else 分支原样保留,失败→`nTransformed=false`→走 harvest 老路。**真机结果(零回退验证 ✓,优化未触发)**:`playerHash=854a788e nClass=null (no config)`——当前 player 轮换到 854a788e 不在硬编码 map(整缺 sts 20665-20668 档),按设计回退 harvest,视频照播(itag 401 2160p/399 1080p),30s harvest 未跳过。zemer config 查到 `854a788e=tL`→方案对,只是 config 没覆盖。tag v2.0.10-alpha.32(commit 758659a,CI 绿)。详见 §6.7 row 56 | **2.0.10-alpha.33 nClass config 全量打包**:把 zemer 整份 config(224 条 player+全部 alias)打包成 `assets/youtube/player_configs.json`,`YoutubeNDecryptor` 构造加 `Context`,`nClassByPlayerHash` 改 `by lazy { loadPlayerConfig() }` 运行时从 asset 加载(主 hash+aliases 全登记),失败回退空 map→harvest 兜底;删 16 条硬编码 map;`AppContainer` 接线传 appContext。当前 player 854a788e=tL 现能命中,后续轮换不易断。后续(不在本):自愈远程 fetch config(6h TTL,镜像 zemer PlayerConfigStore)。tag v2.0.10-alpha.33(commit a557219,CI 绿 run 31231452251)。**待真机**:看 `n-decrypt: ok nClass=tL`+`nTransformed=true`+无 `harvest` 行=成功(预估落地 ~5-8s,砍到 1/3);`__nTransformFunc not defined`→共享 executor 跑不通 IIFE 上专用 WebView;`transform no-op`→nClass/`.get('n')` 排查。详见 §6.7 row 57 | **2.0.10-alpha.34-39(60s 断崖攻坚)**:alpha.33 n-transform 真机证伪(plasma WASM,URL-class `.get('n')` 返回 garbage)→ 退回锁死,harvest WebView 仍唯一可用。alpha.34 续播 startMs。alpha.35 SABR 60s 断流诊断叠层。alpha.36 误把 FreeTube `playerTimeMs=startTimeMs` 读成固定 0 锚点 → 60s 反退成 5s 断崖,alpha.37 回退 cumulative。alpha.38 纯诊断叠层(解全 NextRequestPolicy readahead 字段 + 逐流逐段 cookie 指纹 + 发出 cookie 哈希)。**alpha.38 真机判读**:三规则全排除——同流 sentCookieHash 恒等于上一 PolicyDiag.cookieHash(cookie clobber 排除,是服务端轮转 cookie 非 clobber)、readahead target 15000 而 seq7 拒绝时 lead≈12000(非超前)、cookie 共享同 FreeTube 设计;断崖精确在 playerTimeMs≈60000 而 FreeTube `playerTimeMs='0'` 恒不过 60s → 主嫌锁定 playerTimeMs=cumulative。另核实 `streamerContext.backoffTimeMs`(FreeTube L424 设但 streamer_context.proto 无该 int 字段 → 死代码,排除)。**alpha.39 根修**:`playerTimeMs = cumulativeDurationMs` → `startMs`(对齐 FreeTube 固定锚点)+ `enabledTrackTypesBitfield` video `2(VIDEO_ONLY)`→`0(VIDEO_AND_AUDIO)`(对齐 FreeTube L733)。whipsaw 风险:alpha.36 单改 playerTimeMs=0 撞 5s 断崖,本版同改 bitfield 降低概率;若仍 5s,MEDIA_HEADER matched=false 日志暴露服务端发的 seq 再定位。详见 §6.7 row 63。 | **2.0.10-alpha.40 真机证伪——双改均退化,两处全回退 + SegDiag 取证缓冲膨胀假设**:alpha.39 真机(PID 19899,三视频 sid=Ubhypj14KX9jtMgJhgwmQQ/1ESha7x2SsZSjHhEHuSCsw/RpdOU-2oMIskt0ToNQhWYA)证双改各自退化:① playerTimeMs=startMs **复现 alpha.36 的 5/10s 断崖**(第三次踩同坑)——三视频全 seq=2 断,服务端对 seq=2 请求重发 seq=1 MEDIA_HEADER(matched=false)→ 循环 `no MEDIA_HEADER but NEXT_REQUEST_POLICY → Backoff 0ms` → exhausted/interrupted → EOF;audio 断 @10001/14305ms、video 断 @4372/4304ms;commit 赌「同改 bitfield 降低 5s 概率」没中。② bitfield video 2→0(VIDEO_AND_AUDIO)**致视频流被灌 audio 头 + itag399 init 彻底死**(alpha.18 退化翻版)——VIDEO 流先收 itag=251(audio)MEDIA_HEADER(matched=false wanted itag=video);itag401 靠 headerId=2 侥幸拿到 video 头,itag399 几十次 backoff=0 拿不到任何 video 头 → seq0 init 即 EOF。横跳定论:playerTimeMs 0↔cumulative 来回横跳是追红鲱鱼——FreeTube 发 '0' 跑通证明 playerTimeMs 非区分因子(真因 row 60:FreeTube shaka 逐段 ?sq=N 实时 paced vs 我们 ProgressiveMediaSource 贪婪 burst-fetch)。**根修(两处全回退)**:(1)buildSegRequest `playerTimeMs=startMs`→`cumulativeDurationMs`(alpha.37/38 原值,回 60s 断崖基线,严格优于 5-10s+视频死);(2)SabrClient `enabledTrackTypesBitfield=AUDIO?1:0`→`else 2`(VIDEO_ONLY,alpha.18→38 原值);(3)新增 **SegDiag** 逐段日志(buildSegRequest):playerTimeMs(发)/playhead(实时 1x)/cumulative(缓冲终点)/lead(=cumulative-playhead)/ownRange/segIdx,到 60s 断崖点取证「缓冲膨胀」假设——cumulative 因 burst-fetch 跑在 playhead 前(playhead ~46s 时 cumulative 已 60001),服务端按 playerTimeMs(=cumulative)判位置超 60s 软拒;若证,下版 alpha.41 试 playerTimeMs=playheadEstimateMs()(随实时 1x 播放涨,既非 0 也非 buffered 终点,不膨胀,理论上同时避开 5s 与 60s 两断崖)。详见 §6.7 row 64。 **2.0.10-alpha.41**:part 46(RELOAD_PLAYER_RESPONSE)从 unhandled 提为 terminal 变体(不再 6×backoff 死循环)+ init 失败 evict + 播放器 error-retry 走新 harvest + part 46 payload dump。**alpha.41 真机**:首播能放约一分钟(60s 断崖基线未动),但清晰度切换/续播(startMs>0)全死。**2.0.10-alpha.42 根修清晰度切换死睡**:`playheadEstimateMs` 漏 startMs——续播点 startMs=74753 但 playhead 只取 open 后 elapsed(≈0)→ lead≈74753 → pace sleep 61s → 服务端会话空闲过期 → seq=1 只回 NEXT_REQUEST_POLICY backoff 不给 media → 6 backoff EOF evict。改 `playhead=startMs+elapsed` + segIndex acc 起点=startMs + own 条件改 `segDurations.isNotEmpty()`;三改对 startMs=0 全等价,不回归首播一分钟、不动 60s 断崖。详见 §6.7 row 66。 **2.0.10-alpha.43**:harvest watch 页无 SABR POST 诊断叠层——`YoutubeSabrHarvester` HOOK_JS fetch wrapper 加 `isPlayer(url)` 分支截 watch 页 `/youtubei/v1/player` 请求 body(搜 poToken/serviceIntegrityDimensions)+ 响应(dump streamingData keys/serverAbrStreamingUrl/adaptive0Url/mediaUstreamerRequestConfig/formats),走 console.log 不污染 `__gvCaptures`;PAGE diag 抽 `PAGE_DIAG_JS` 常量加 `<video>.currentSrc/src` + captures 条数;harvest 轮询每 ~3s 周期 dump;`shouldInterceptRequest` 只读覆盖 googlevideo URL log method/itag/sabr 后 return null 放行原生。详见 §6.7 row 68。**alpha.43 真机**:row 68 场景未复现——harvest 正常建会话(sabrUrl=present),但**仍只能播一分钟**,日志定位两个独立 bug(见 alpha.44)。 **2.0.10-alpha.44 修「只能播一分钟」两个 bug**:① **60s 断崖**(主因):`buildSegRequest` `playerTimeMs=cumulativeDurationMs`(缓冲终点,alpha.37 误读)→ 播 ~60s cumulative>60000=maxTimeSinceReq → 服务端软拒只回 NEXT_REQUEST_POLICY backoff 不给 MEDIA_HEADER → 6 backoff EOF evict(证据:SegDiag seq=13 playerTimeMs=60031 playhead=48535)。改 `playerTimeMs=playheadMs`(当前播放位置,对齐 clientAbrState 语义,随 1x 涨不膨胀;5s 断崖是恒 0 致重发 seq=1,playhead 不恒 0 不复发)。② **reuse session seq 错位**(切清晰度触发):reuse 时 `open()` 设 cumulative=startMs 但 nextSeq 仍 1 → 发 seq=1/playerTimeMs=7194 → VIDEO segDur 5800ms 落 seq=2 → 服务端给 seq=2+ → `matchesFormat` 严格相等判 false → 死磕 EOF(AUDIO segDur 10000ms 落 seq=1 不错位,仅 VIDEO 卡)。改 `matchesFormat` `>=`(接受续播更高 seq,拒绝重发回退段)+ `read()` nextSeq 对齐 `realSeq+1`。不动 resolver reuse 路径/pacing。详见 §6.7 row 69。 **2.0.10-alpha.45 修 60s 断崖**:删 `read()` 内 pacer 阻塞 + MaxBufferMs 90s→50s + 封顶上报 bufferedRange 终点 55s(详见 §6.7 row 70)。**2.0.10-alpha.46 修 55s 断崖**:`fetchUntilReady` 入参改 `reqProvider: () -> SabrFetchRequest`,每次 backoff 重试重建请求刷新 playhead,服务端流控见 lead 回落恢复发段(详见 §6.7 row 71)。**2.0.10-alpha.46 修视频加载不出**:harvest 只读 `window.__gvCaptures[0]` 且要求 status>0——首条 capture 无效(status=0/url 空)时循环永远卡在 `[0]`,从不检查 `[1]`/`[2]` 里的 SABR POST → 30s 超时 → 返回 null → 无 playable URL → 加载不出。改读全数组遍历 + 新增 `parseCaptureArray` + 无有效项时 dump 全数组诊断(详见 §6.7 row 72)。 **alpha.50 修 60s 断崖**：EOF 交棒/evict 守卫 + 按 playhead 触发 + 阈值 45s(详见 §6.7 row 75)。**alpha.51 统一 60s 窗口会话模型 + 手动 seek**(详见 §6.7 row 78)：后台多会话预取解决连续性(maybeTriggerRotation 触发键 playhead→cumulative,距锚点≥40s 后台 harvest 下个窗口、≥60s 耗尽无缝切换,播放可越过 60s)+ 手动选播放位置 seek(目标 X 锚定会话 floor(X/60)*60 + startMs 透传,PlayerScreen/MobilePlayerScreen routeSeek 走重新起播免双 init 崩)。**alpha.52 真机 0→60 跑通但 120s 卡死**→**alpha.53 修轮换 120s 卡死**(详见 §6.7 row 79)：Bug B 共享窗口去重消除重复 w60 harvest + 轮换 harvest 失败重试 3 次 + 空白页 fail-fast 8s(harvest `onPageFinished not fired within 8000ms → fail fast`)。**alpha.54 修轮换后新会话立即软拒(段请求对齐 FreeTube,详见 §6.7 row 80)**：联网核对 googlevideo `SabrStreamingAdapter.ts` 锁根因——轮换会话首段 `ownRange=[null..+null]`(无 bufferedRange)下软拒触发源只剩 URL `startTimeMs=60000`(未封顶)和 body `playerTimeMs=34159`(旧时间线 playhead 对从 60s 起的新会话无效);FreeTube 只发 `rn` + `playerTimeMs=段绝对起点` + bufferedRange startTimeMs 恒 0,无窗口/重基准、无 60s 上限。修:①`buildSegRequest` body `playerTimeMs` 从 playhead 改 `cumulativeDurationMs`(段绝对起点,轮换首段即 60000);②段 URL 删 `&startTimeMs=`/`&sq=` 只发 `rn`(alpha.51 Track A 诊断参数移除)。不动 bufferedRange.own(alpha.45 55000 封顶已让初始会话工作,轮换 own=null 与本次 bug 无关改它回归风险大)/轮换机制(修复后当前会话不再软拒,cumulative-anchor≥SWITCH_AT_MS 门仍会在 60s 切到预取窗口作为兜底无害)。待真机验轮换首段 `playerTimeMs=60000` matched=true、绝对 cumulative 越 60000→120000、全程无连续 6 次 `no MEDIA_HEADER but NEXT_REQUEST_POLICY`。**alpha.54 真机(10:19 PID 14609)证伪「60s 非硬上限」**——playerTimeMs=段起点没能消除 60s 软拒,burst-fetch 让 cumulative ~1s 冲 60001,AUDIO seq7/VIDEO seq13 仍 `playerTimeMs=60001/60060` → 6 backoff → EOF;每会话 ~60s 服务量上限确为硬约束,轮换必经。**alpha.55 修轮换 init 带共享 cookie 致 VIDEO InvalidPoToken(视频卡死 ~1min 真凶,详见 §6.7 row 81)**——两会话不对称:AUDIO 轮换成功续播(新会话 init `cookie=false` 成功、播 60-120s),VIDEO 轮换 init 失败(`cookie=true sentCookieHash=119540432` → `STREAM_PROTECTION_STATUS status=3` → `InvalidPoToken` → revert → seq13 再软拒 → 视频死)。根因:`SabrSession.playbackCookie` 会话级共享,AUDIO 先轮换写进 cookie、VIDEO 后轮换 init 复用 AUDIO 派生 cookie → 全新会话 init 带旧 cookie → 服务端拒(铁证:所有成功 init 皆 cookie=false,唯一失败 init 是 cookie=true;MEDIA 请求带 cookie 全正常)。修一行:`SabrClient.fetch` `playbackCookie = if (req.isInit) null else session.playbackCookie`(init 不发,MEDIA 仍回传;window=0 初始播放 cookie 本为 null → 零回归)。待真机验 VIDEO 轮换 init cookie=false→status=2→matched=true、两流都越 1 分钟、2 分钟。**alpha.55 真机(11:12 PID 16862)播放全程播不出——卡在 init,轮换从未触发**(见 §6.7 row 82):每会话 init `cookie=false`,服务端回 `NEXT_REQUEST_POLICY backoff=8800ms` 不给 MEDIA_HEADER,流 `Backoff 8800ms (sleep + refetch)` 却从没发出重试;~7.5s 后 MobilePlayer stall 检测器(`@pos=0ms buffered=0%`)先 cancel 在途 open() → `InterruptedException` → `fetchUntilReady` null → `init fetch failed → evict+throw` → 无限 re-harvest。**根因=timing 竞态**:`fetchUntilReady` 的 Backoff 分支 `Thread.sleep(result.ms)` 在 ExoPlayer loader 线程同步阻塞服务端全部 backoff,而 init backoff 常达 **8.8s > MobilePlayer `StallThresholdMs`(8s)**——watchdog 第 8s 判死在途 init(对齐 alpha.45「勿在 loader 线程长睡」,init 长睡比段长睡更致命:pos 恒 0、watchdog 零容忍)。**alpha.56 修**:backoff sleep 封顶 `MAX_BACKOFF_SLEEP_MS=2500ms`(`minOf(result.ms,2500)`),重试总在 8s watchdog 前触发(2.5/5/7.5s 三次机会);backoff 是服务端流控建议(advisory),提前重试服务端照收(alpha.54 工作运行首 backoff 后 ~4.3s 重试即 `status=2` 恢复佐证)。待真机验:①init 不再被 watchdog cancel——`Backoff 2500ms attempt=N` 后 `RESUMED after N backoffs`/`MEDIA_HEADER matched=true`,播放能起播;②起播后复验 alpha.55/row 81 修复——VIDEO 轮换 init `cookie=false`→`status=2`(非 3)→双流越 1min/2min。若仍卡(服务端坚持完整 backoff),下一步让 stall 检测器在「首次 init 进行中」容忍 >8s(跨组件)。**alpha.56 真机(11:43 PID 25188 视频 INxlHUoPCUg)init 卡死已解、播放能起播,但轮换后仍卡死在 ~1min**(见 §6.7 row 83):VIDEO 轮换 init 依旧 `status=3`(InvalidPoToken),且这次 `cookie=false`(alpha.55 已生效)仍失败。取证:两会话轮换 init 不对称——AUDIO 轮换 init `contexts=0/0` → `status=1` → `hand off to new session` 正常续播;VIDEO 轮换 init `cookie=false` 但 `contexts=1/0` → `status=3` → `revert old session` → 旧 VIDEO seq13 `playerTimeMs=60060` 再软拒 → 6 backoff → EOF → 视频死在 ~1min。**根因**:`SabrSession.sabrContexts`/`activeSabrContextTypes`(field5/field6 SABR 上下文握手)与 `playbackCookie` 一样是**会话级共享**——AUDIO 先轮换到新会话并 fetch,`processUmpStream` 把 AUDIO 上下文写进 `session.sabrContexts`;VIDEO 后轮换到同一新会话,其 init 复用 AUDIO 派生的上下文(`contexts=1/0`)→ 全新会话 init 带上前一会话上下文 → 服务端校验失败 → status=3。**与 row 81/alpha.55 同一类 bug**:alpha.55 只修了 `playbackCookie`(field3),漏了 SABR 上下文(field5/field6)。**alpha.57 修**:`SabrClient.fetch` 构造 `streamerContext` 时 init 请求**也不带** SABR 上下文(`if (req.isInit) emptyList<SabrContext>() to emptyList<Int>() else session.prepareSabrContexts()`);MEDIA(段)请求仍回传(alpha.31 结论)。待真机验:①VIDEO 轮换 init 变 `contexts=0/0` → `status=1/2` 非 3 → `MEDIA_HEADER matched=true`;②VIDEO 不再 revert old session / seq13 软拒,而是新会话播 60-120s;③双流越 1min/2min。若仍卡,下一步查轮换新会话是否仍带其它前一会话状态(如 bufferedRange/playerTimeMs 锚点)。**alpha.58 转向 FreeTube paced 模型(Phase 1 验证)**:轮换虽修到能跨 60s,但每次轮换 ~5s 回退 + seek 仍回退 SABR。核对 FreeTube(shaka)发现根因是**取段模型**而非请求语义:ProgressiveMediaSource 贪婪 burst-fetch 让 cumulative ~1s 冲 60001 撞服务端 ~60s 每会话服务上限(硬约束,alpha.54 证伪「60s 非硬上限」),轮换是 burst 模型的兜底;FreeTube 用 paced 按需取段(每段只在播放需要时请求,playerTimeMs=段绝对起点随播放推进、与墙钟同步,bufferedRange 锚 0 小窗)→ 服务端持续发段、无 60s 上限、seek 原生、无轮换。**Phase 1(本提交)**:`TvPlaybackLoadControl` MaxBufferMs 50s→10s(50s 缓冲边沿=playhead+50s,playhead≈10s 时 cumulative 已到 60s 撞断崖;10s 让 ProgressiveMediaSource 每 ~6s 播放才拉一段、请求节奏与墙钟同步)+ `SabrStreamingDataSource` maybeTriggerRotation/switchIfRotated 临时 return 禁用轮换,观察 paced 请求能否不靠轮换跨 60s。若成立 → Phase 2 合成 DASH MPD(SegmentTemplate,每段 `sabr://...?seq=N`)+ DataSource per-segment + 播放器接线 DashMediaSource + 原生 seek + 彻底移除轮换。 |
| P11-15 | WebDAV 备份/还原 YouTube 关注频道 | 实施中(代码改完待云编译) | 新增 `core/webdav/` 包:`WebDavConfigStore`(DataStore 明文存 URL/账号/密码)、`WebDavRepository`(OkHttp PUT/GET/MKCOL + `Credentials.basic` Basic auth,复用 `downloadHttpClient` 长超时)、`WebDavBackupService`(序列化 `List<YoutubeChannel>` 成单个 JSON,上传到 `{url}/bilitv/youtube_channels.json`,还原下载解析写回 `YoutubeChannelStore`);`AppContainer` 注入三组件。**UI 调整后**:TV `SettingsScreen` 加「WebDAV 备份」行(只显示 URL)+ `SettingsWebDavDialog` 居中弹窗(内嵌 D-pad 键盘编辑 URL/账号/密码)+ 两个独立「备份到 WebDAV」「从 WebDAV 还原」action 行(成功 Toast,懒索引重编号);移动 `MobileSettingsScreen` 加 `MobileWebDavSection`(地址行只显 URL,点按/长按弹 `AlertDialog` 编辑三字段 + 备份/还原按钮行成功 Toast);`AppShell`/`SettingsActivity`/`MainActivity` 接线。**运行时待真机**:WebDAV 服务器连通、备份文件生成、还原频道恢复 |
| P11-16 | YouTube 多语言配音修复 + 音轨切换 + 默认画质 | Done(云编译绿) | 修复「中文视频播英文配音」:根因是 YouTube 多语言配音(multi-audio)下同一 itag(如 251/140)会按语言重复出现,`pickAudio` 盲取第一条可能拿到配音轨。**修复**:`pickAudio` 优先 `audioTrack.audioIsDefault=true`(原声轨),再兜底 251/140/最高码率;SABR 路径 `firstAudio` 优先 `AudioTrackType.ORIGINAL`。**音轨切换**:`PlaybackInfo.availableAudioTracks` 暴露全部音轨,播放器控制栏加音轨按钮(仅 YouTube 多音轨视频显示),选中即 `loadRequest(preferredAudioTrackId)` 重解析;经典路径与 SABR 路径统一,SABR 会话按 id 命中 `audioTracks` 表 `copy(audioFormatId)` 换轨重注册。**默认画质**:设置加 `YoutubeDefaultQuality`(自动/4K/2K/1080P/720P/480P),解析按 `maxHeight` 上限选档。单音轨视频多个 itag 折叠成一条,避免误显示多音轨菜单。详见 `docs/youtube-hd-playback.md` §6.11 |
| P11-17 | YouTube 播放历史 + 断电续播 | Done(云编译绿) | 根因:YouTube 播放请求 `bvid`=videoId、`cid` 恒为 0,而 `PlaybackProgressStore.saveProgress` 有 `cid <= 0L` 守卫 → YouTube 进度从未落盘。**修复**:守卫放宽为 `cid < 0L`(允许 cid=0),TV/移动端播放器既有 `saveProgress`/`getSavedProgress` 即恢复续播;播完的视频经 `isWatchCompleted`(进度≥时长-1s)阈值从 0 起播。**历史存储**:新建 `core/youtube/YoutubeHistoryStore.kt`(DataStore 列表存储,cap 50 按 `lastPlayedAtMs` 倒序,`recordPlay`/`updatePosition`/`remove`/`clear`),TV 与移动端播放器 Ready 时 `recordPlay`、暂停/退出时 `updatePosition`;`PlaybackRequest` 加 `channelId` 供开频道。**移动端 UI**:YouTube 历史并入「动态」底栏「历史」子 tab(`MobileHistoryPage` 本地 YouTube 历史与 B 站历史**混合、按播放时间倒序**、YouTube 绿框),未登录也显示本地 YouTube 历史 + 登录提示;原独立「YouTube 历史」子 tab 已移除(`MobileYoutubeHistoryPage.kt` 删除)。点击续播、点头像开频道。详见 `docs/youtube-hd-playback.md` §6.12 |
| P11-18 | 后台播放自动连播修复 + 历史混合按时间排序 + 动态 feed 卡片 B站动态样式+发布日期 | Done(云编译绿) | **后台播放自动连播**:`MobilePlayerScreen` `STATE_ENDED` 不再无条件 `stopService`(此前 stop+restart 触发 `ForegroundServiceStartNotAllowedException` 崩溃致后台播放完不自动播下一个),改安全 `startPlaybackService`(catch `IllegalStateException`)保持服务跨自动连播存活,仅下一集为空(列表播完)才停。**历史混合排序**:`MobileHistoryPage` 本地 YouTube 历史(viewAt=lastPlayedAtMs/1000)与 B 站历史(viewAt 秒)合并 `sortedByDescending` 渲染、YouTube 绿框(`toVideoSummary` 补 viewAt 作统一排序键),未登录仍只显本地历史+登录提示。**动态 feed 卡片**:`MobileVideoCard` 加 `feedLayout` 参数(默认紧凑布局,首页/搜索/空间不变),动态页置 true 用 B站动态样式新布局——顶行作者块(头像40dp跨两行 + UP名一行 + 发布时间·播放量一行,发布时间相对时间复用 `video_relative_*` 字符串)→ 缩略图独占整行 → 标题。**单列(v3.0.0-alpha.5)**:`MobileDynamicScreen` 的 `LazyVerticalGrid` 改 `GridCells.Fixed(1)`,卡片占满整行全宽展示。 |
| P11-20 | TV 端 YouTube 取流 TVHTML5 client 试验 | 实施中(代码改完待云编译,运行时待真机) | 背景:TV/移动端 YouTube 取流原完全共用 WEB client + SABR(NewPipe path C),无端区分;YouTube 官方 TV 端用 TVHTML5 client,对某些视频格式/清晰度可用性可能更宽容。**改动**:①`YoutubeConstants` 加 TvHtml5ClientName=TVHTML5/Version=7.20260707.07.00/NameId=7/Cobalt UA;②`InnerTubeClient.Client` enum 加 TVHTML5(buildContext/OkHttp headers/buildWebViewHeaders 加分支,viaWebView 条件含 TVHTML5——TV client OkHttp 直连大概率被拦,走 Chromium 原生网络栈);③`YoutubePlaybackResolver` clients 列表按 `PlaybackRequest.preferredYoutubeClient` 构建(TVHTML5→[TVHTML5,WEB] 失败回退 WEB,null→[WEB] 移动端不变),SABR 门控放宽含 TVHTML5(NewPipe path C 独立于外层 client,最坏退回 SABR=现状),加 `resolve clients=... preferred=...` 日志(TV 端加载显示走的 client);④`PlaybackRequest` 加 `preferredYoutubeClient` 字段;⑤TV `PlayerScreen` YouTube 请求 `.copy(preferredYoutubeClient=TVHTML5)`。移动端 `MobilePlayerScreen` 不改(默认 WEB+SABR)。**可行性风险**:§6.5 实测无 token TVHTML5 失败;visitorData 与 WEB session(2.x)不配对可能被拒;TVHTML5 经典路径 adaptive url 大概率仍剥空(整个 §6.7 证明 WEB 也剥空)→ 最可能退回 SABR 无收益,但安全可回退(失败自动回退 WEB)。**待真机**:看 logcat(tag `YtResolver`,目录 `Y:\download\bilitv\logs`)`resolve clients=[TVHTML5, WEB]` + `TVHTML5 formats:`/`TVHTML5 diag:` 行,对照 WEB 看 TVHTML5 /player 是否给带 url adaptive / 更多清晰度(结局 C=收益保留),还是被拦回退 WEB SABR(结局 A/B=无收益回退改动) |
| P11-21 | 退役 YouTube classic(Path 2)SABR n-decrypt 兜底 | Done(代码改完待云编译) | 真机观察「只有 Path C(NewPipe SABR)有效,classic(n-decrypt 兜底)会卡住」。代码坐实两坑:①**poToken 跨 minter**——classic 用 resolve() 顶部 `botGuard.generatePoToken` 铸的 poToken,SABR init minter 不一致 → status=3 60s 卡死(Path C 注释明说它用 getInfo 期间缓存的同一枚「根除」的就是这个);②**n-decrypt 结构性失效**(memory `youtube-plasma-wasm-n-decrypt`)——plasma 把 n/sig 移进 WASM,`decipherSabrUrl` 正则全 0 匹配 → `nTransformed=false` → 多数视频根本不进 classic,少数非 plasma 偶然命中又踩坑①。**改动**:`YoutubePlaybackResolver` SABR 闸门内删 classic `else if (nTransformed)` 分支及仅服务于它的局部变量(`videoFormats`/`sabrUrlDeciphered`/`nTransformed`/`vFmt`/`aFmt`)+ `YoutubeLoadProgress.emit(DecipherN)`;删死方法 `decipherSabrUrl`;ready 日志去掉 `source=NewPipe/classic`、`nTransformed`;NewPipe 返回 null 直接落 DASH 兜底(日志 `落 DASH 兜底(classic n-decrypt 已退役...)`)。`nDecryptor` 保留(经典 DASH `resolveStreamUrl` 解 legacy 直链仍用)。同步更新 `docs/youtube-hd-playback.md` harvest 退役段。**待真机**:YouTube 视频走 Path C 不变;NewPipe 失败的视频落 DASH 不再卡 60s;日志无 `source=classic`/`nTransformed` |
| P11-22 | YouTube 相关视频(/next secondaryResults,对齐 LibreTube) | 实施中(代码改完待云编译,运行时待真机) | 移动端播放器简介 tab 相关视频原用 `playQueue.drop(curIndex+1)`(播放列表后续)占位;TV 端相关视频面板对 YouTube 调 B 站 `getRelatedVideos` 会失败。**改动**:①`YoutubeParsers` 加 `parseRelatedVideos`——从 `/next` 响应 `contents.twoColumnWatchNextResults.secondaryResults.secondaryResults.results[]` 收集 `compactVideoRenderer`(复用 `parseVideoRenderer`)+ 该 section 内 continuation,无容器回退全根;②`YoutubeRepository` 加 `getRelatedVideos(videoId, continuation)`(POST `/next`,与评论同端点);③`VideoRepository` 加 `getYoutubeRelatedVideos(videoId, continuation): List<VideoSummary>`(map `toVideoSummary`);④移动 `MobilePlayerScreen` 相关视频 `LaunchedEffect` YouTube 分支改走 `getYoutubeRelatedVideos`,失败回退 `playQueue.drop`;⑤TV `PlayerScreen` `PlayerControl.Related` 面板 + 播完自动连播相关视频,YouTube 分支改走 `getYoutubeRelatedVideos`。**待真机**:YouTube 播放器相关视频区/面板出真实相关视频(非播放列表后续),点击切播正常 |
| P11-22 | SABR 主路径应用默认画质上限 | Done(云编译绿,运行时待真机) | 用户报告 YouTube 默认画质设置(自动/4K/2K/1080P/720P/480P)TV/移动端都不生效。排查:`youtubeDefaultQuality` 从 UI→DataStore→`resolve()` 形参全程接通,但 `resolve()` 内仅 DASH 兜底分支 `pickVideo` 消费 `maxHeight`;SABR 主路径(YouTube 实际取流)`buildSabrPlaybackInfo` 选档固定用会话首条 itag(`preferredQualityId` 命中优先,否则 `sabrSession.videoFormatId.itag`),完全不读 `maxHeight` → 设置存了没用。**改动**:`buildSabrPlaybackInfo` 加 `youtubeDefaultQuality` 形参;选档 `preferredQualityId` 命中优先(手动切清晰度不变),否则 `maxHeight!=null` 取 `height<=maxHeight` 最高 itag(全超上限取最低档保证可播),Auto 取 `maxBy height`(对齐 DASH `pickVideo` 最大化语义),兜底会话首条;`resolve()` 调用处透传。会话构建 `buildSabrSessionFromNewPipe` 不动(`videoFormats` 全量保留供清晰度菜单,同 sid 换 itag 即换清晰度,选非首条 itag 安全,见 alpha.29 注释)。同步更新 `docs/youtube-hd-playback.md` §6.11 + `docs/youtube-stream-resolution.md` §5.4。**待真机**:设默认画质 1080P 开 4K 视频确认起播 1080p(非会话首条);480P 起播 480p;Auto 起播最高档;TV/移动端各验;播放中手动切清晰度仍正常 |
| P11-23 | YouTube 内容地区(gl/hl)开放为用户设置 | 实施中(代码改完待云编译,运行时待真机) | 背景:`gl`(国家/内容地区)+`hl`(界面语言)原写死 `YoutubeConstants.Gl=US/Hl=en`(原作者注释明说 zh-CN/CN 触发反爬故锁 US)。需求:让挂不同地区 VPN 的用户能让 gl 跟上(避免"日本 IP+美国 context"地理不一致 bot 特征)+看对应地区热门/推荐。**架构决策(集中式 holder 非逐层透传)**:gl/hl 影响 browse/search/player/SABR 全部(共享 `InnerTubeClient`),照 `youtubeDefaultQuality` 透传需改 `YoutubeRepository` 每个 browse/search 调用点+所有 UI 调用方,blast radius 过大 → 选进程级 holder `YoutubeContentLocale`(AppShell/MobileApp 收集 settings 后写 holder,InnerTubeClient.buildContext+sabrClientInfo 读 holder)。**新增文件**:`YoutubeContentRegion`(枚举 US/JP/HK/TW/KR/GB/DE,每项 gl+hl 联动预设,默认 US,不放 CN,KDoc 注明反爬)+`YoutubeContentLocale`(object,@Volatile current,gl/hl getter)。**改动**:`AppSettings`+`AppSettingsStore`(stringPreferencesKey `youtube_content_region`+fromKey 读+setter);`InnerTubeClient.buildContext`(3 处 Hl/Gl→holder,WEB/ANDROID/TVHTML5 活路径)+`sabrClientInfo`(死代码改一致性);`visionOsSabrClientInfo` **不动**(LibreTube 逐字对齐+RELOAD_PLAYER 风险);`YoutubeConstants.Hl/Gl` 保留作 fallback。**UI**:TV `SettingsScreen` 加「YouTube 内容地区」循环行(focus id=33,settingsItemToLazyIndex 6 项 +1)+回调透传;移动 `MobileSettingsScreen` 加 `MobileEnumPickerRow`;AppShell/MobileApp 加 `LaunchedEffect` 写 holder+回调写 store;3 语言 strings.xml 加 title/description。**待真机**:切 JP 重启看 WEB /player context gl=JP hl=ja;切回 US 恢复;默认首次安装仍 US 不触发反爬;主 SABR 路径正常起播(visionOS gl/hl 由 NewPipe 决定不受控,见已知限制)。详见 `docs/youtube-hd-playback.md` §6.8.3 |
| P11-24 | TV 历史 tab 合并本地 YouTube 历史 | Done(云编译绿,运行时待真机) | 根因:TV `UserFeedScreen` 的 History tab 只 `loadHistoryFirstPage` 拉 B 站历史,从不合并本地 `YoutubeHistoryStore`(移动端 `MobileHistoryPage` 早已混合)。修复:①`UserFeedScreen` 加 `youtubeHistoryStore` 参数(由 `AppShell` 传入),`collectAsState` 收集 `youtubeHistoryStore.history` 并 `toVideoSummary`(viewAt=lastPlayedAtMs/1000,progress=positionMs/1000,source=SourceYoutube,channelId)转换成卡片;②`UserFeedContent`/`UserFeedGrid` 加 `extraVideos` 参数,`mergeExtraVideos` 把 (extra + base) 按 `viewAt` 倒序混合(extra 空时原样返回 base,Dynamic/Favorite/Bangumi 不受影响),焦点 `resolveFocusIndex` 改对混合列表解析;③新增 `HistoryFeedContent` 组合函数处理两种登录态——登录时 B 站历史(含分页)+ YouTube 历史混合;未登录时只渲染本地 YouTube 历史 + 底部登录提示(History tab 从登录门禁放行,对齐移动端),无 YouTube 历史未登录则原登录提示屏。点击续播走 `toPlaybackRequest`(progress 续播、source 路由 YouTube)。 |
| P11-25 | TV 动态/历史 YouTube 卡片标绿框识别 | Done(云编译绿,运行时待真机) | 需求:youtube 内容在 TV 端也要标绿框识别(对齐移动端 `MobileVideoCard` 的 `showYoutubeBorder`)。修复:`VideoCard`(TV)加 `YoutubeBorderColor = Color(0xFF00C853)`(与移动端同值)+ 常驻绿框——`mode == VideoCardMode.Dynamic || History` 且 `video.source == SourceYoutube` 时在卡根 `BiliFocusableSurface` modifier 加 `border(2.dp, YoutubeBorderColor, RoundedCornerShape(BiliRadius.Card))`。覆盖:动态(video/all,含合并的 YouTube 关注)与历史(含 P11-24 合并的本地 YouTube 历史)标绿框;Favorite 虽用 Dynamic 模式但只有 B 站内容(source != SourceYoutube)不误标;Standard(首页/搜索/频道)不标,避免首页 YouTube 热门整行全绿,对齐移动端(仅动态+历史标)。 |
| P11-26 | IPTV 播放黑屏——m3u8 302 重定向到 IPv6 节点,真机连不上 | 实施中(代码改完待云编译,运行时待真机) | 根因:真机日志 `LivePlayback` 播 IPTV 一直 `state=BUFFERING tracks=0 video=null audio=null`,8s 后触发 stall 自动重试,永远到不了 READY → 黑屏。实测源 `cf.19961226.xyz/iptv/` 的 m3u8(如 `ott.mobaibox.com/.../index.m3u8`)会 **302 重定向**,重定向目标**按客户端 IP 族选择**——客户端走 IPv6 就回 IPv6 字面量节点 `[2409:8087:...]`,走 IPv4(`curl -4`)回 IPv4 节点 `223.110.246.80`。真机(Sony BRAVIA)常处"IPv6 已启用但不可路由"网络,拿到 IPv6 重定向连不上 → HLS 按 `LiveLoadErrorHandlingPolicy` 内部重试 7 次(指数退避)一直 BUFFERING 不报错 → 黑屏。**改动**:新增 `IptvDataSourceFactory`(core/player)——用只返回 A 记录(过滤 AAAA)的 `Ipv4OnlyDns` 强制客户端走 IPv4,服务端即回 IPv4 节点;`OkHttpDataSource.Factory` + 通用 User-Agent(部分源对无 UA 请求 403);`LivePlayerScreen` IPTV 分支从裸 `DefaultDataSource.Factory(context)` 换成 `remember { IptvDataSourceFactory().create() }`(复用同一 factory,避免每次重载源新建 OkHttpClient)。**待真机**:IPTV 频道起播出画面;源切换/断流自动切源正常;B站直播不受影响(仍走 `BiliMediaDataSourceFactory`)。 **alpha.31 缩略图并发**:`IptvThumbnailManager` 信号量 2→3;TV `LiveScreen.onVisibleRangeChange` + 移动端 `MobileLiveScreen.captureVisibleIptv` 消费循环由串行 for 改 `async` 并发(先 `mapNotNull` 取 URL 再逐个 `await`),死源/慢源不再串行堵住整批缩略图,信号量 3 并发首次真正用满。 **alpha.32 截帧全失败根因修复(stall 看门狗误杀慢源)**:真机日志实锤截帧 3 次尝试 0 成功、全 `stall 0% reload #1/2/3`→timeout,而真机播放器同一源 1s 到 READY——根因是看门狗 3s 阈值太激进,mobaibox 这类慢源首帧要 ~8s,被 3s 判 stall 反复重挂打断启动,永远 READY 不了;`IptvThumbnailCapturer` 加 `StartupGraceMs=6s` 启动宽限期(期内不判 stall 不重挂)、ready 判断从绑死 `videoSize.width` 改 `ImageReader.setOnImageAvailableListener` 的 `AtomicBoolean frameReady` 帧可用信号、超时 15s→22s、stall 阈值 3s→4s、重挂上限 3→2。 **alpha.33 截帧换方案(ImageReader 本机不可行→EGL 离屏)**:alpha.32 真机日志实锤另一层根因,10 频道全抛 `UnsupportedOperationException: producer output buffer format 0x7fa30c06(私有/YUV) doesn't match ImageReader's configured buffer format 0x1(RGBA_8888)`——codec 输出格式与 ImageReader 配置格式必须一致,本机给私有格式直接异常,不是 stall/慢源/超时,ImageReader 方案本身在本机从根上不可行。**新增 `IptvThumbnailCapturerEgl`(方案一)**:SurfaceTexture 接解码器输出(无格式协商)→ EGL 离屏 pbuffer + OES 外部纹理 + shader 画全屏 quad → `glFinish` → `glReadPixels` 读回 RGBA 转 Bitmap(media3 播放器截帧标准真离屏路径);`IptvThumbnailManager` 切到 EGL capturer,ImageReader 版 `IptvThumbnailCapturer` 保留对照;复用 alpha.32 全部修复(启动宽限期 + 帧可用信号改 OnFrameAvailableListener + 22s 超时 + LiveLoadErrorHandlingPolicy + 静音 + 专用 HandlerThread 同线程)。 |
| P11-27 | TV 侧栏 + 设置页焦点循环导航(最上按上→最底,最底按下→最上) | 实施中(代码改完待云编译,运行时待真机) | 需求:TV 侧栏与设置页在焦点边界要能循环——最上按上跳到最底,最底按下跳到最上。**侧栏**(`AppSidebar`):新增 `focusedSidebarIndex` 状态 + `sidebarFocusOrder`(头像 index 0 + 导航项 index 1..n,按 `AppDestination` 枚举序,Settings 已排除);`AccountNavItem`/`AppNavItem` 加 `onSidebarItemFocused` 回调,`onFocusChanged` 上报各自 index;侧栏根 `Column` 加 `onPreviewKeyEvent`——仅在边界拦截(头像按上→`sidebarFocusOrder.last()` 即 Live,最底 Live 按下→头像),其余交给默认焦点遍历。**设置页**(`SettingsScreen.moveSettingFocus`):原 `while (nextOrderIndex in indices)` 越界即停,改成 `repeat(size)` + 越界取模回绕(`((nextOrderIndex % size) + size) % size`),跳过不可聚焦项(条件项 `settingsItemToLazyIndex < 0`),循环跳转时滚动方向取反(上→底要往下滚,下→顶要往上滚)。**待真机**:侧栏头像按上→Live、Live 按下→头像;设置页首项按上→末项、末项按下→首项;非边界上下移动仍正常;autoConfirm 聚焦选中行为不变。 |
| P11-28 | 移动端设置页补 Piped 后端入口(alpha.84 验证受阻根因) | Done(云编译绿,运行时待真机) | 用户报告 alpha.84 推送后真机仍"有的能播有的不行"。排查 `logs_live.log`(2026-08-17 00:07 窗口)定位**两个叠加问题**:①**装的包是 Piped 代码之前的老包**——真机 `App Version: 3.0.2-alpha.19 (3002119)` → tag `v3.0.2-alpha.19` → commit `67d60ff`(8/16 21:45),而 Piped 修复在 `03d6e11`(8/16 23:49);该包 resolve() 根本没有 Piped 分支,整份日志零条 `source=Piped`/`Piped path` 行、全部 `source=NewPipe`,jNl6YkkzKxw 94 次 RELOAD、D2kXTmSPUJo/-GZatCruA8c 各 2 次能播 = alpha.83 旧行为;②**移动端设置页没有 Piped 开关入口**——alpha.84 三行(启用 Piped / Piped 实例 / 锁定会话 itag)**只加到 TV 端 `ui/settings/SettingsScreen.kt`(L1024)**,移动端 `ui/mobile/settings/MobileSettingsScreen.kt` 漏了,用户在移动端无处开 `youtubeUsePiped` → 即便装含 Piped 代码的包也恒 false → 永远走 NewPipe 旧路径(用户原话"移动版里没有实例设置位置")。**改动**(commit `22475dc`):照移动端已有 `MobileIptvSection` 折叠段模式,在 `MobileSettingsScreen.kt` 加 `MobileYoutubeSabrSection`(「YouTube SABR 实验」折叠段)——启用 Piped 开关→`setYoutubeUsePiped`、Piped 实例行(点按 `MobilePipedEditDialog` 编辑,`normalizeIptvUrl` 补协议,空串=默认 `DEFAULT_PIPED_INSTANCE`)→`setPipedInstanceUrl`、锁定会话 itag 开关→`setSabrForceSessionVideoItag`;新增段头字符串 `settings_youtube_sabr_section`。store setter/resolve() Piped 分支/`PipedClient` 注入此前均已就位,本次仅补移动端 UI 入口(移动端直接调 `appSettingsStore` setter,无需穿 AppShell)。**待真机**:装含本修复的新 debug 包('debug' release `BiliMT-debug.apk` 已由运行 `31957948314` 更新),进移动端「设置→YouTube SABR 实验」开「启用 Piped 后端」(实例留空=默认 `kavin.rocks`),播 jNl6YkkzKxw 验证 Piped 修好 RELOAD,播 D2kXTmSPUJo 确认无回归。详见 `docs/youtube-hd-playback.md` alpha.85。 |
| P11-30 | 首页订阅流接真实 continuation 分页(翻到最早视频) | Done(云编译绿,运行时待真机) | 根因:首页 YouTube 热门 tab(实为关注频道订阅流)每频道 RSS+InnerTube `/browse` 合并后**硬截断 `take(perChannel=15)`**,更早视频结构性删除;`getChannelVideosRaw` 拉第一页时丢弃 continuation token → 永远翻不到最早。LibreTube 对订阅流同样不翻页(30 天时间窗+`cleanUpOlderThan` 清库),无可抄续页方案 → 自建聚合流 continuation。**改动**:①`YoutubeModels` 加 `YoutubeSubscriptionsPage(videos, perChannelContinuation: Map<String,String?>)`,`endReached=continuation.values.all{it==null}`;②`YoutubeRepository` 加 `getChannelVideosRawPage(channelId, continuation)`(首屏 browseId+params/续页 continuation,不再丢 token)+ `getSubscriptionsPage(channels, perChannel, previousContinuation)`(首屏 RSS+InnerTube 并行合并 `take(15)` 记录 continuation;续页只对 token 非 null 频道拉更早一页、RSS 无续页概念仅走 InnerTube;每批 onChunkReady+随机 delay 防节流+单频道 runCatching 降级,复用原骨架)+ 抽取 `fillChannelInfo`;③`VideoRepository` 加 `youtubeHomeFeedPage(previousContinuation, ...)`(读 youtubeChannelStore.channels,空→空页;`youtubeSubscriptionsFeed` 保留供动态 tab 不动);④TV `RecommendScreen`/移动 `MobileHomeScreen` `Success` 加 `youtubeContinuation: Map<String,String?>?`——首屏取 `page.perChannelContinuation`+`endReached=page.endReached`,loadNextPage 走 `youtubeHomeFeedPage(previousContinuation)` 合并 `distinctBy{bvid}.sortedByDescending{pubdate}`(替代原 `nextVideos.size<PageSize` 误判到底);⑤`YoutubeFeedCacheStore`/Room 零改动(缓存只存首屏快照不落盘 continuation,cache 命中视为单页到底)。**待真机**:首页 YouTube 热门 tab 每次触底追加一批更早视频、能逐页逼近最早;切走切回保留续页状态;10min 冷启动缓存快照单页到底回归;动态 tab YouTube 关注仍正常合并进 B 站动态。 |
| P11-29 | alpha.86 回退 alpha.85b WEB 经典路径(n-param 403 回归 ≤1080p)+ 4K 终态定论 | Done(云编译待验,运行时待真机) | alpha.85b(commit `354a979`,在 22475dc 之后另推一个 alpha.85 提交)试 attested WEB /player 经典路径(`buildSabrSessionFromWebPlayer`)作 SABR 主路径修 4K(itag313)RELOAD。前提假设 docs §6.7 row 75「sabrUrl 网关无 n-param」也适用 WEB /player sabrUrl。**真机证伪**(`logs_live.log` 2026-08-17,`App Version: dev.r1215`):RELOAD 死循环消失,但所有 SABR fetch 返回 **HTTP 403**(Server=gvs 空体,alpha.18 同症状)——WEB /player 的 `serverAbrStreamingUrl` **带 n-param**(与 fork visionOS sabrUrl 无 n 不同),plasma WASM 致 n-decrypt 结构性失效,OkHttp POST 未 transform 被拒;且**连 ≤1080p 都回归 403**(n-param 对所有 itag 一视同仁)。docs §6.7 row 75 只适用 fork visionOS,不适用 WEB /player。**git 历史补证**:查 git 发现 alpha.80(`7d0ed49`)L687 真把 poToken 接进 fromSabrData 真测过 visionOS config + WEB-visitor poToken(首请求带 token 尝试 attestation)→ RELOAD(visitor 不匹配,L767 注释正确);此前以为「L756 一直写死空、poToken 从未进 SABR 会话」是误判(alpha.14 改空→alpha.80 改回带→真测 RELOAD→又改回空)。**alpha.86 改动**(commit `e801402`):回退 alpha.85b WEB 经典主路径,恢复 alpha.84 NewPipe visionOS 主路径(n-free,≤1080p 走 status=2 刷新可播)= 回到 ≤1080p 已知可播状态;移除死代码 `buildSabrSessionFromWebPlayer` + `Base64` import。**4K 两条 OkHttp 直连路全堵且真机证伪**:visionOS+WEB-poToken→RELOAD(alpha.80)、WEB+WEB-poToken→403 n-param(alpha.85b);4K 唯一曾跑通是 alpha.20-25 WebView harvest(浏览器 WASM n-transform + 全 WEB attested body),alpha.71 退役(疑跨 minter 60s,但 alpha.68 同步刷新已修竞态,待重测)。**用户 2026-08-17 决定:接受 ≤1080p(与 LibreTube 同档),放弃 4K**——alpha.86 即 4K 终态。**待真机**:装 alpha.86 debug 包(需先卸载 alpha.85b debug,签名不同:alpha.85b 用 7565f89 之前随机 debug.keystore,alpha.86 用 7565f89 起固定 release keystore),验 ≤1080p 回归 403 已修、D2kXTmSPUJo/-GZatCruA8c 仍能播、jNl6YkkzKxw 仍 RELOAD(>1080p,与 LibreTube 同档,已知接受)。详见 `docs/youtube-hd-playback.md` alpha.85b/alpha.86 段 + memory `youtube-4k-two-paths-dead-accept-1080p`。 |
| P11-31 | YouTube 会员专属视频过滤(无法播放,展示去除) | 实施中(代码改完待云编译,运行时待真机) | 需求:YouTube 频道会员专属视频(members-only)非会员无法播放,feed 里展示出来点进去才失败,应在展示层去除。**参考实现**:NewPipe `YoutubeStreamInfoItemExtractor`(PR #1280,LibreTube 同栈)——检查视频 `badges[]` 数组里是否有 `metadataBadgeRenderer.style == "BADGE_STYLE_TYPE_MEMBERS_ONLY"`,命中即标记 `ContentAvailability.MEMBERSHIP` 供客户端过滤。**改动**:`YoutubeParsers` 加 `isMembersOnly(node)` 辅助(遍历 `badges[]` 查 `metadataBadgeRenderer.style == BADGE_STYLE_TYPE_MEMBERS_ONLY`),`parseVideoRenderer` + `parseLockupViewModel` 开头命中即 `return null` 过滤——两函数是全部 YouTube 入口(搜索/热门/频道/订阅流/相关视频)共用的解析函数,一处改全生效。**待真机**:订阅流/搜索/热门里会员专属视频不再出现;普通视频不受影响。 **移动端播放列表拖拽排序误触修复**：真机反馈拖一个视频时其它视频被误触乱动——根因 `PlaylistDetailScreen` 拖拽 `onDrag` 交换后只更新 `draggingIndex` 没同步修正 `draggingOffsetY`，交换后拖拽项基准 offset 变了但累计位移没减，`draggedCenter` 随列表位置漂移、比手指超前一个 item 高度，`target` 误判成更远视频致拖拽项连跳多个。修复：交换后 `draggingOffsetY -= (newBase - draggingInfo.offset)`（newBase 取交换后落点 offset），让拖拽项视觉始终跟手、只有手指真正越过下一视频中心才交换。改动 `MobileYoutubePlaylistPage.kt`。 |
| P11-33 | S905X5M 等 Amlogic 盒子解码器误判仅 H264(HEVC/AV1 选项缺失) | Done(云编译绿,运行时待真机) | 用户报告 X96M200(S905X5M 芯片,支持 HEVC/AV1 硬解)上「设置→解码器」固定 H264 无法切换,用 H264 看 4K 高码率很卡,隔壁 BV 可正常播 AV1。根因 `core/player/CodecCapabilityProbe.kt` 用 `MediaCodecList.REGULAR_CODECS` + `isHardwareAccelerated` 过滤硬件解码器——Amlogic 厂商 AV1/HEVC 硬解组件(`c2.aml.*`)常不置 `isHardwareAccelerated` 或被排除出 REGULAR 列表,`supportsAv1/supportsH265` 误判 false → TV 设置页 `playbackCodecOptions()`(SettingsRows.kt)只剩 `[Auto,H264]`,用户只能 Auto↔H264 循环(移动端设置页本就给全部 4 项,MobileSettingsScreen 不按探测过滤)。**改动**(commit `263c1eb`,对齐 BV `E:\GITHUB\bv\...\util\CodecUtil.kt` 的 ALL_CODECS + `!isSoftwareOnly` 判定):①`decoderMimeTypes()` `REGULAR_CODECS`→`ALL_CODECS` 防厂商解码器被排除;②`isHardwareDecoderInfo()` Q+ 分支 `isHardwareAccelerated` → `isHardwareAccelerated || !isSoftwareOnly`,真软解(dav1d/c2.android.*)`isSoftwareOnly=true` 仍排除无回归;预-Q 名称判定不变。该探测同时是播放全链路唯一依据(PlaybackRepository 的 `supports()`/`buildFnval()`/`isPlayable()`),修后 AV1/HEVC 选项与轨过滤同时生效。**待真机**:设置→解码器显示 Auto/AV1/H265/H264,切 AV1 4K 高码率不再卡;`BiliMT:Codec` 日志三标志全 true。若用户开着「流畅模式」(lowSpecMode=Smooth)仍会被独立强制 H264(刻意设计,非本修复范围)。 |
| P11-32 | YouTube 播放优先级设置(SABR 优先 / DASH 优先) | 实施中(代码改完待云编译,运行时待真机) | 背景:真机日志(2026-08-18 23:43,videoId=a0HOhKWNDkI)确认一次视频播放中断——SABR 首段在慢服务器 `rr5---sn-3pm76nes` 上 ~11s 才送达,而 stall 看门狗阈值 8s 无启动宽限,在首段刚到前 4s 误判 → 触发完整 teardown+重建会话(用户看到"卡住→重新加载视频"),与 `iptv-thumb-stall-watchdog-kills-slow-sources` 同一类"看门狗误杀慢源"。本次按用户要求加手动逃生通道:播放优先级设置,选「DASH 优先」先走 DASH 自合成兜底(NewPipe 已解密直链拼 MPD,实测能出 4K VP9),避开慢 SABR 首段被误杀;默认「SABR 优先」行为完全不变(非破坏)。**改动**:①新增枚举 `core/player/YoutubeDeliveryPriority.kt`(Sabr/Dash,`label` 中文,`fromKey` 模式);②`AppSettings` 加 `youtubeDeliveryPriority`(默认 Sabr);③`AppSettingsStore` 加 Key(`youtube_delivery_priority`)+读+`setYoutubeDeliveryPriority`;④`YoutubePlaybackResolver.resolve()` 顶部读 `dashFirst`,Piped 块条件加 `&& !dashFirst`,NewPipe-first 路径重排——`dashFirst` 先 `buildDashFallbackFromNewPipe` 成功即返(记 `DASH 优先` 日志),否则走 SABR(保留 reloadCount 死循环守卫),`!dashFirst` 才在 SABR 失败后 DASH 兜底;⑤TV `SettingsScreen` 加「播放优先级」`SettingsOptionRow`(content-region 同款循环切换,const=38,focusRequester/boundary/索引 when 全链更新,尾链 WebDav/Backup/Restore/Iptv/Logs/About/PlayerLogOverlay 索引 +1);⑥移动 `MobileSettingsScreen` 加 `MobileEnumPickerRow`;⑦strings.xml 加 title/description。**WebDav 备份不改**(主设置不走 WebDav)。**待真机**:TV/移动各切一次「DASH 优先」确认能存能读、播放走 `NewPipe-first(DASH 优先)`(日志),切回 SABR 优先确认 `source=NewPipe(primary)` 行为不变;慢源场景切 DASH 优先验证不再被 stall 看门狗误杀。 |
| P11-34 | YouTube 简介 Tab 点赞数显示 | 实施中(代码改完待云编译,运行时待真机) | 需求:补上 YouTube 视频点赞数显示(参考 LibreTube DescriptionLayout 的 `streams.likes`)。点赞数不在 `/player` 的 `videoDetails`,而在 `/next` 首屏 `contents.twoColumnWatchNextResults.results.results.contents[]`(带 `videoPrimaryInfoRenderer` 那项)的 `videoActions.menuRenderer.topLevelButtons`——对齐 NewPipe `YoutubeStreamExtractor.getLikeCount` 两策略:①新布局 `segmentedLikeDislikeButtonViewModel.likeButtonViewModel.likeButtonViewModel.toggleButtonViewModel.toggleButtonViewModel.defaultButtonViewModel.buttonViewModel.accessibilityText`;②旧布局 `segmentedLikeDislikeButtonRenderer.likeButton.toggleButtonRenderer` 的 `accessibilityData.accessibilityData.label`/`accessibility.label`/`defaultText.accessibility.accessibilityData.label`。**改动**:①`YoutubeModels.YoutubeVideoDetail` 加 `likeCount: Long?=null`;②`YoutubeParsers` 加 `parseLikeCount(root)`(走上述路径,取到 ≥0 即返)+ `parseCount` 增加 `likes/次點讚/次点赞/次赞` 后缀剥离(点赞 label 形如 "1,234 likes"/"1.2M likes"/"1.2萬次點讚");③`YoutubeRepository.getVideoDetail` 在 `/player` 后再发一次 `/next` 取点赞回写 `detail.copy(likeCount=...)`(失败保持 null,随 publishedAt NewPipe 兜底一起返回);④移动端 `MobileYoutubeIntroTab` 数据行加「点赞」项(`player_like_count_format` 字符串,formatCount 与观看一致,Long 收敛 Int)。**待真机**:YouTube 简介 Tab 显示「播放 X · 点赞 Y · 日期」;无点赞数(受限/解析失败)该行不出现;B 站播放不受影响。 |

**YouTube 频道页「最新 / 最热」排序(当前分支,未打 tag)**：对齐 B站 UP 空间,频道页视频 tab 支持最新/最热双档切换——`YoutubeConstants` 加 `ChannelPopularParams = "EgZwb3B1bGFy"`(解码 field1="popular",来源 rustypipe/invidious 文档)与 `ChannelVideoOrder` 枚举(Latest/Popular);`YoutubeRepository.getChannelVideos` 参数化 `params`(默认最新,翻页 continuation 与排序无关);TV `YoutubeChannelScreen` header 加最新/最热 sort chips(焦点聚焦即切换、`onMoveDown` 下移网格)、移动端 `MobileYoutubeChannelScreen` 加两个 OutlinedButton,均加 `loadedOrder` 守卫切排序强制重拉第一页。见 docs/youtube-api-notes.md §4.13。已补代码与文档,**待云编译 + 真机验证最热 params 是否按播放量排序**。 |

| P11-35 | SABR 升降档控制:起始挡 maxVideoSize + ceiling 降档滞回 | 部分(起始挡 Done,ceiling 待修) | 背景:不稳定网络反复升降档→看门狗整段重载黑屏。**起始挡**(首段钉档):seed(§6.22)真机失效(首段恒最高档,重新 resolve 出 4K 后首段直接 itag299 4K),改用 `DefaultTrackSelector.Parameters.setMaxVideoSize(Int.MAX_VALUE, startHeight)` 起播卡档+`onRenderedFirstFrame` 后 `clearVideoSizeConstraints` 松开(a2b38bf/d921ac6),公开 API 绕开 `AdaptiveTrackSelection.selectedIndex` private。**ceiling 降档滞回**(9977043/f373a2b):检测降档(`selectedIndex` 增大)把源档及以上升档候选 iterator 置 EMPTY,relax 窗口 bufferMaxMs/2=25s;但真机日志证实 `sel` 无视 ceiling 爬满 4K(ceiling=4 时 sel=0 itag299 被选中下载)——根因 `AdaptiveTrackSelection` 选档=`≤带宽估计×0.7 最高码率档` 不依赖 iterator,EMPTY 挡不了高档选择;待改 `trackSelection.excludeTrack(index, duration)` 把 ceiling 档及以上排除出选轨。详见 docs/youtube-hd-playback.md §6.23。
| P11-36 | 播放列表「下载」列表拖动排序 | 完成(云编译绿,运行时待真机) | 需求:播放列表页里的「下载」(YoutubePlaylistStore `DOWNLOAD_PLAYLIST_NAME`=下载)列表要能移动顺序。该列表由下载完成自动存档(`DownloadManager.finalizeGroup` 调 `playlistStore.addVideo`),此前在 `PlaylistDetailScreen`(MobileYoutubePlaylistPage.kt)标 `autoArchive` 只读——`pointerInput` 拖拽门控 `if (editMode || autoArchive) return@pointerInput` 禁掉长按拖动,且 autoArchive 恒不显示「编辑」按钮(编辑/移除本就不可用)。**改动**:拖拽门控去掉 `autoArchive` 分支,改为 `if (editMode) return@pointerInput`——因为 editMode 对「下载」恒为 false,长按拖动排序解锁,而批量编辑/移除仍禁用;排序经现有 `youtubePlaylistStore.replaceVideos(playlist.name, items)` 落盘(与普通播放列表同路径)。注意这是播放列表特性,非离线下载列表(离线下载顺序固定 Room `createdAtMs`,另开任务)。**待真机**:进入「播放列表→下载」长按卡片可拖动换序,松手后顺序持久化(重启/重进仍在);编辑按钮/批量移除对「下载」仍不出现。**崩溃修复**:误做离线排序的实验提交(84b389b)把 DownloadDatabase version 升到 2(加 sortOrder),设备库已 v2 后代码 revert 回 v1 → 启动撞「A migration from 2 to 1 was required but not found」。补 `DownloadDatabase.MIGRATION_2_1` 降级迁移(ALTER TABLE download DROP COLUMN sortOrder)并在 AppContainer `addMigrations` 注册,干净回 v1 保数据。
| P11-17 | YouTube 历史/简介频道头像恒空修复 | Done(云编译绿) | 根因:`/player` 接口不返回作者头像,`parseVideoDetail` 的 `channelAvatarUrl` 恒设为 `""` → 历史条目/简介 Tab 频道行头像空白(用户称 LibreTube 继续观看列表头像"就在时间下面"一直正常)。**修复(对齐 LibreTube 权威头像链路,同源同 API)**:①`YoutubeRepository.getVideoDetail` 复用同一次 NewPipe `StreamInfo.getInfo` 的 `uploaderAvatars.maxBy { it.height }.url` 填 `channelAvatarUrl`(需 `withContext(Dispatchers.IO)` —— `getInfo` 是同步阻塞网络,主线程抛 `NetworkOnMainThreadException` 致头像兜底恒失败);②移动端 `recordPlay` 对 YouTube 取权威 `youtubeDetail.channelAvatarUrl`(原取 `activeRequest.ownerFace`,因 `withResolvedMetadata` 对 YouTube `metadata=null` 恒空填不了);③`YoutubeHistoryStore` 新增 `updateChannel` 原地回填(channelAvatarUrl/channelId,不改列表位置),移动端历史页 `MobileHistoryPage` 后台按频道分组,每组只解析一次权威头像回填所有空白条目,不重播即愈合,已处理 videoId 记进集合防重复请求。LibreTube 用 Room `watchHistoryItem` 存 `uploaderAvatar`,起播即落库故永非空;我们用 DataStore 等价,新条目由 recordPlay 存,旧空条目由回填一次性迁移。详见 `docs/youtube-vs-libretube-comparison.md` §3.1.1 |
| P11-37 | 播放列表相关视频恒为列表后续 + seek 末端误触防护 | 实施中(代码改完待云编译) | 背景:从下载播放列表进播放器,「相关视频」原走在线 `/next` 优先、失败才回退 `playQueue`——在线通(debug)显示在线相关、在线断(release)碰巧等于列表,与用户自动连播 `computeNextRequest`(恒走 playQueue)不一致,行为随网络飘。同时真机 release 日志(LT4DZ8~O.LOG 15:34)实锤:缓存 360p 视频起播 0.9s 后 `playerState=4(pos=0)`(STATE_ENDED)→ 2ms 后自动切换下一集 RBJ(480p),全程无 seekTo 日志 = 误触进度条末端 seek 到末尾触发 ENDED → 自动连播,用户看到「点清晰度变 480p」(缓存时清晰度是静态文字贴进度条右侧,点清晰度落到进度条末端)。**改动**(MobilePlayerScreen.kt):①相关视频 `LaunchedEffect`——播放列表(playQueue.size>1 且当前项之后还有)相关 = `playQueue.drop(cur+1)` 直接返回,不试在线;历史/动态/搜索等单视频(playQueue 无后续)才走在线 `getYoutubeRelatedVideos`(YouTube)/`getRelatedVideos`(B站),播放列表与自动连播同源同序、行为统一;②`routeSeek` 加末端误触防护——目标落在末尾 3s 内钳到 `maxMs-3000`(≥0),seek 到末尾不再进 STATE_ENDED 不误连播,SABR 中段 seek 不受影响。**待真机**:播放列表里「相关视频=列表后续」且 debug/release 一致;误触进度条末端不再跳下一集;历史/搜索仍显示在线相关。 |
| P11-38 | 移动端播放器加全屏吞噬层——点缓存清晰度不再误开设置页(alpha.11) | Done(云编译绿,运行时待真机) | 需求延续 P11-37:用户真机仍反映点缓存视频「清晰度位置」进设置页。**根因**(alpha.10 诊断日志 `VRI[SettingsActivity]` 实锤):播放器全屏 Box 是 Scaffold 兄弟节点,`fillMaxSize().background(Black)` 已铺满全窗(含底栏区域),但 `background` 只画不吞指针事件——缓存视频「清晰度位置」是无 onClick 的静态文字,该处播放器无点击目标,点击穿透黑底命中底下 Scaffold 的「设置」tab 打开设置页(非全屏同构,非 debug/release 差异)。**改动**(MobileApp.kt):①播放器 Box 内新增一层 `fillMaxSize` 的 `consumeAllGestures()` 吞噬层(awaitPointerEventScope 死循环 `event.changes.forEach{it.consume()}`,`awaitPointerEventScope`/`awaitPointerEvent` 是 PointerInputScope 成员不需 import),把整个播放器区域所有指针事件吞掉、盖住并屏蔽其下任何层(底栏/页面/空间/未来新增);播放器控件叠于其上事件先被顶层控件消费不受影响,无点击目标处(缓存清晰度文案)指针被吞不落穿;②回退此前「播放时隐藏底栏」方案(commit ba7d9)——隐藏只处理已知底栏,覆盖才鲁棒。**待真机**:release 播缓存视频点「清晰度位置」不再打开设置页;全屏/非全屏都不再穿透;播放器暂停/播放/进度条/清晰度菜单(非缓存)交互正常。**曾踩编译坑**:`import awaitPointerEventScope` 是成员非顶层报 Unresolved,删 import 后编译绿。**云编译**:`32837437852`(fb9bea0)绿。 |
| P11-39 | 动态初始加载空修复 + 启动后台预加载(推荐+动态) | Done(云编译绿,运行时待真机) | 需求:TV 动态初始加载空、首页推荐 tab 需二次点击。**动态空根因**:首载时 `youtubeChannels` 还是 emptyList(store 未发),首载只拉 B 站并置 `loadedOnce=true`,频道发出来后 `loadedOnce` 挡住 YouTube 合并→初始空、手动刷新才出。**修复**(2889c10):`DynamicFeedUiState` 加 `youtubeMerged` 标志区分「频道首次就绪需补拉 YouTube」与「后续头像回填等频道变化(不应重载)」;TV `LaunchedEffect` key 改稳定 `channelId` 列表(头像回填不触发重启)+`youtubeMerged=false` 时 forceRefresh 补拉合并;移动端 key 加 channelId 列表重跑合并。**启动预加载**(93adcfd):AppShell 加 `LaunchedEffect(userSession.isLoggedIn)` 启动即拉首页推荐(`getHomeSectionVideos(Recommend)`)与动态(B站 `getDynamicFeed`+YouTube `youtubeSubscriptionsFeed` 合并)写入 state 对象,切到对应 tab 数据已就绪免二次点击;推荐免登录可拉、动态需登录(未登录跳过,登录后 effect 因 isLoggedIn 变化重跑补拉);取消必须向上抛(否则 runCatching 吞 CancellationException 误写 Empty 卡死推荐);只写「屏幕尚未自行加载」状态避免并发覆盖。**诊断日志**(bfdceba):`BiliMT:Preload` 标签打 start/成功/失败/跳过。**真机日志**:未登录用户预加载正常启动、推荐被屏幕抢先加载(数据本就就绪)、动态正确跳过;推荐二次点击问题疑为焦点/显示而非数据加载,待登录用户真机确认。 |
| P11-40 | 频道页播放列表卡缩略图/视频数失效修复 | Done(代码完成,待云编译+真机) | 用户报告:频道页"播放列表"tab 未点进播放列表时卡片没缩略图(只有"▶"占位块)。**取证(2026-08-27 curl 实测 InnerTube `/browse` 频道 Playlists tab,21/21 卡)**:①封面已迁移——`contentImage` 现在是 `collectionThumbnailViewModel` 包裹,真封面在 `primaryThumbnail.thumbnailViewModel.image.sources[]`,旧直连 `contentImage.thumbnailViewModel` 拿空 → 卡片无缩略图(alpha 期旧疾根因);②视频数已迁移——旧路径 `metadataRows` 现在只有 "Updated X ago"/"View full playlist",真视频数在缩略图角标 `thumbnailViewModel.overlays[].thumbnailOverlayBadgeViewModel.thumbnailBadges[].thumbnailBadgeViewModel.text`("120 videos"),旧解析取 metadataRows 首行会错显示更新时间文案;③onTap 仍空 → browseId 落 contentId(PL...),`normalizePlaylistBrowseId` 自动补 VL 前缀,详情页打开不受影响。**修复**(`YoutubeParsers.parseChannelPlaylists` lockup 分支,纯解析层、UI 零改动——ChannelPlaylistCard 本就渲染 thumbnail+videoCount):封面新路径优先、旧直连兜底;视频数 badge 优先、metadataRows 兜底。同构附带发现:搜索播放列表筛选 params=`EgIQAw%3D%3D`(type=3,实测 20/20 纯 lockup 播放列表卡+continuation),`Eg9QAQ%3D%3D` 被服务端无视返回普通视频;搜索卡片与频道页卡同构,未来做搜索播放列表类型可直接复用。已离线用真实响应验证新解析逻辑 21/21 全命中。文档:`docs/youtube-api-notes.md` §Playlists 卡补迁移实录。**待真机**:移动端频道页播放列表 tab 卡片显示封面+「N videos」角标视频数,点击照常进详情页。 |
| P11-41 | 播放列表详情作者/视频数恒 null 修复(中文 locale) + 简介结论确认 | Done(代码完成,待云编译+真机) | 续 P11-40 同批真机日志(10:00-10:01):缩略图修复生效后用户仍报"没有简介"。**日志取证**:desc 其实已解析成功(PLpr1zUR="最新上傳的精彩節目…"、PLxfczB3whC="这是一个对谈interview节目"),UI 会渲染在"播放全部"下方小灰字;三个列表全 `owner=null count=null`——这才是 header 行空白真因。**根因(curl hl=zh 复现 + curl 对比)**:`parsePlaylistHeader` 旧判定是语言硬编码——作者行 `startsWith("by ")` 只认英文(中文响应 avatarStack 文本是"创建者：xxx"/繁中"建立者：xxx"恒不匹配),视频数正则 `\d+\s*\S*video` 只认英文(中文是"141 个视频")。**修复**(YoutubeParsers.parsePlaylistHeader):改结构性判定——avatarStack part 即创建者行(剥 by/创建者：/建立者： 前缀留名字),视频数=含数字且含 video/视频/影片/動画 词的文本(排除"130,265次观看"纯播放量)。离线真实响应模拟验证两列表 owner/count 全命中。**简介结论**:①PLxfcznuBUN2ChRdODugrAthcWHS-PCRgi 在 YouTube 上**本身没简介**(`descriptionPreviewViewModel` 只有 truncationText"…更多"无 description.content),desc=null 正确;②另两个列表简介已渲染(小灰字),待用户确认是否可见性不足需调样式。文档:youtube-api-notes §播放列表详情头部补语言前缀坑。**待真机**:详情页 header 显示"作者名 · N 个视频";确认简介灰字是否可见,不可见再调样式。 |
| P11-42 | 播放列表详情页简介上移到播放全部按钮上方 | Done(代码完成,待云编译) | 用户看了当前 UI 样例定稿:简介从"播放全部"按钮下方挪到上方——顺序改为 封面→标题→作者·视频数→简介(可展开灰字)→播放全部→视频行列表。纯位置调整(MobileYoutubePlaylistDetailScreen.kt 头部 item 内 desc 块移到 OutlinedButton 前),样式不变(bodySmall 灰字、120 字截断点击展开)。前提:42e50dc 作者/视频数修复生效后 header 行已有作者名。 |
| P11-43 | 播放列表详情 owner 空串/count null 真因修复——viewModel text 节点须读 content 键 | Done(代码完成,待云编译+真机) | 42e50dc 修复后真机日志仍 `owner=`(空串)count=null;curl 全 locale+真实 clientVersion 复现均正常,按诊断套路加 header metadata dump(cc5949e)。**真机 dump 拿到实锤**:设备响应完全正常(avatarStack text.content="by FearNation 世界苦茶",设备 hl=en),问题在解析调用——`runsText` 只认 "runs" 键、`simpleText` 只认 "simpleText" 键(**不读 content**),而 viewModel 形状 text 节点是 `{"content":"...","commandRuns":[...]}`,两函数都返回空 → avatarText 空串(非 null)→ owner="",行文本同理恒空 → count=null。**修复**(parsePlaylistHeader):新增 viewModelText 先读 content 再回退 runs/simpleText。教训:42e50dc 的"离线验证"用 Python 模拟的是想要的逻辑,没对照 Kotlin 实际行为(simpleText 不读 content),验了个寂寞。诊断 dump 已按注释约定移除(既有 owner=/count= 日志行够验证)。文档:youtube-api-notes 同步补 content 键坑。**待真机**:详情页 header 显示"作者名 · N videos"(设备 hl=en 显示英文名+英文 N videos)。 |
| P11-44 | 移动端频道页主页 tab 对齐 B站官方(行列表+播放全部+排序入口)+ tab 改名主页 | Done(代码完成,待云编译+真机) | 用户提供 B站官方 app UP空间截图定稿:①tab「视频」改名「主页」(youtube_channel_tab_videos,zh 主页/en Home,仅移动频道页引用);②主页 tab 内容区从双列网格换**官方式纵向视频行**——左封面 128dp 16:9(右下角时长角标)+右标题(2行)/日期/播放量(YouTube 无弹幕不显示),行间细分割线,长按仍走视频长按菜单;③内容区头部行:**「▶ 播放全部」左**(primary 加粗,点它把已加载整份列表作为连播队列从第一条起播,MobileApp onPlayAll 接线 playQueue=queue+playbackRequest=first,镜像播放列表详情 onStartSelected 同款)+ **「≡ 最新发布/最热门」右对齐排序入口**(DropdownMenu 弹菜单选中打✓,替代原独立(最新)(最热)按钮行,已删);排序仅主页 tab 有,Shorts/直播 tab 保持网格不变。排序字符串 player_up_sort_latest 恰为「最新发布」与官方一致。**待真机**:主页 tab 显示纵向行列表+播放全部起播连播队列+右对齐排序切换生效;Shorts/直播/播放列表 tab 不受影响。 |
| P11-45 | B站移动端 UP 主空间页对齐官方(投稿行列表+播放全部+排序入口) | Done(代码完成,待云编译+真机) | 用户确认 YouTube 频道页改造没问题,要求 B站 UP 主页同样改造(MobileUserSpaceScreen):①投稿区从双列网格(MobileVideoCard)换**官方式纵向投稿行**——复用 YouTube 频道页 ChannelVideoRow(改 internal 跨文件共用,加 showDanmaku 参数):封面左 128dp+时长角标,右标题/日期/「▶ 播放量 · 💬 弹幕数」(B站有弹幕,官方样式),行间分割线,长按保留;②头部行「▶ 播放全部」左(已加载投稿作连播队列从第一条起播,MobileApp onPlayAll 接线 playQueue+playbackRequest=first,镜像 onVideoSelected 的 spacePlaybackBehind 处理)+「≡ 排序」右对齐 DropdownMenu(选中打✓),删除原独立(最新)(最热)按钮行;③共享字符串 player_up_sort_hot「最热门」→「最多播放」(B站官方措辞,TV 空间/YouTube 频道页同步生效);mobile_sort_latest/hot 字符串不再被本页引用。**待真机**:UP 空间投稿区显示纵向行列表(播放量+弹幕)、播放全部连播、排序菜单切换生效;TV 空间排序 label 变「最多播放」。 |
| P11-46 | TV 端 YouTube 频道页对齐移动端:内容 tab + 播放列表 tab + TV 播放列表详情页 | Done(代码完成,待云编译+真机) | 用户定稿:TV 同步移动端频道页能力,但**视频保留网格**。**YoutubeChannelScreen**:①头部加内容 tab 行(主页/Shorts/直播/播放列表,复用移动端字符串),D-pad 聚焦只高亮、OK 才切换(避免焦点扫过逐个重拉),焦点链:关注 chip↓→tab→排序(主页)/首卡;②排序行仅主页 tab 显示(label 已随 P11-45 变「最新发布/最多播放」);③数据层:tab 分发 getChannelVideos(主页用排序 params,Shorts/直播/播放列表优先服务端 tab params,来自 getChannelHeader 的 header.tabs,失败回退 resolveChannel+getChannelTabs,对齐移动端两段式)+ getChannelPlaylists;各 tab 独立 state(YoutubeChannelUiState 加 tab/shortsState/liveState/playlistState/serverTabParams/loadedTab 守卫);④播放列表 tab=新 ChannelPlaylistGrid(4 列焦点卡:封面+标题+视频数,聚焦近底自动翻页,返回焦点恢复)——视频 tab 按用户要求保持 TvVideoGrid 不变。**新文件 YoutubePlaylistDetailScreen(TV)**:镜像移动端详情数据流(getPlaylistVideos+header 元数据+continuation 翻页),D-pad 化:顶栏返回 chip+封面 320dp+标题/作者·视频数+可展开简介+「播放全部」+带序号视频行(聚焦近底翻页,聚焦边框高亮);TV 无连播队列,播放全部/点行均单视频起播。**AppShell**:加 youtubePlaylistRequest/playlistPlaybackBehind 覆盖层(镜像频道页门控),返回回频道页并 channelFocusRestoreRequestKey+1 恢复播放列表网格焦点;YoutubeChannelScreen 加 onOpenPlaylist 回调。**待真机**:TV 频道页 4 tab 切换(Shorts/直播网格、播放列表卡片网格)、点播放列表卡进详情页(封面/作者·视频数/简介/播放全部/视频行)、返回焦点恢复、播放起播正常。 |
| P11-47 | 移动端播放列表子 tab 返回键退出修复 | Done(代码完成,待云编译+真机) | 用户报告:移动端动态 tab →「播放列表」子 tab 点进某播放列表详情后按系统返回键**直接退出应用**。**根因**:`MobileYoutubePlaylistPage` 两层导航(列表→详情)用组件内部 `remember` 状态 `selectedName` 切换,详情层没有任何 BackHandler,返回事件一路冒泡无人拦截直落系统退出;对比频道页播放列表详情在 MobileApp.kt:525 有 BackHandler 兜底,唯独本子 tab 漏了。**修复**(MobileYoutubePlaylistPage.kt):①`PlaylistDetailScreen` 组合时注册 `BackHandler`——编辑模式先退编辑并清勾选(对齐相册多选惯例),否则 `onBack()` 回列表层;列表层(tab 根)不注册,返回退出应用保持预期;②`selectedName` `remember`→`rememberSaveable`:HorizontalPager 切走子 tab 再切回时 page 重组,原实现丢选中态掉回列表层,saveable 停在当前详情。**层级核对**:播放中返回先关播放器(MobileApp 播放器层 BackHandler 后注册优先级更高)、批量移除确认弹窗由 Dialog 自身拦截返回,均不误触本导航。**待真机**:播放列表详情按返回回列表(不再退出);编辑模式按返回先退编辑;播放中按返回回播放器,播放器关闭后回详情;切子 tab 再切回仍停在详情层。 |
| P11-48 | TV 端播放队列连播(对齐移动端 playQueue) | Done(代码完成,待云编译+真机) | 续 P11-46 遗留「TV 无连播队列,播放全部/点行均单视频起播」。对齐移动端已验证机制三文件改动:**①AppShell**:加 `playQueue: List<VideoSummary>` 状态(镜像 MobileApp.playQueue);TV YoutubePlaylistDetailScreen 起播回调改 `onStartSelected(video, queue)` 快照整份已加载列表,其它单视频入口(首页×2/动态/搜索/空间/频道页/PGC 选集)起播时清空防串队,退出播放器 onBack 也清空(对齐移动端),PlayerScreen 调用处透传。**②YoutubePlaylistDetailScreen(TV)**:回调签名 `onVideoSelected`→`onStartSelected(video, queue)`,点行从该视频起、播放全部从第一条起,均快照当前已加载 videos 为队列(翻页未加载部分不入队,与移动端同语义)。**③PlayerScreen(TV)**:加 `playQueue` 参数(默认空);**播完连播无条件执行**(对齐移动端——移动端 MobilePlayerScreen 无 autoPlayNextEpisode 等开关,队列连播是「播放全部」内在语义,放在所有开关分支之前):playQueue 按 bvid 定位当前视频取下一项,命中 toast+delay+startPlaybackRequest(clearMetadata=true 重拉元数据,分P场景才 false 复用);未命中再走既有 autoPlayNextEpisode(下一分P,PlayerNextEpisodeCompletion 包装类,注意与 PlaybackRequest 类型不同不能 Elvis 合并)→autoPlayRelatedVideo→回首页。**Related 面板队列化**(对齐移动端 P11-22 related source=playlist):playQueue 有后续时直接 drop(cur+1) 作面板列表不拉在线,队列末/单视频走在线相关(YouTube /next、B站 related);面板点项 startPlaybackRequest 就地切换,shell playQueue 保留继续连播。**待真机**:TV 播放列表详情点行/播放全部起播后播完自动连队列下一项;队列末播完按设置走相关视频/回首页;Related 面板显示队列后续且点项后继续连播;首页/搜索等单视频入口播完不受队列影响(设置开关照旧)。 |
| P11-49 | TV 播放列表详情视频行加观看进度条+已看完角标 | Done(代码完成,待云编译+真机) | 续 P11-48,TV YoutubePlaylistDetailScreen 视频行缩略图此前无任何观看状态。**数据源**=本地 YouTube 播放历史 `YoutubeHistoryStore`(positionMs/durationMs,播放器起播 recordPlay/暂停退出 updatePosition 写入)按 videoId 索引;不引入 WatchedStore(TV AppShell 无此参数,避免动 Activity 链路,TV 端自用历史已够)。**已看完判定**兼容两端写入语义:TV 播完 saveProgressNow 写 ≈duration(positionMs>=durationMs-2s 命中,CompletedThresholdMs 对齐播放器「距末尾 2s」裕量),移动端播完写 0(positionMs==0L 命中,store 注释「播完/接近播完时为 0」的既有语义);进度 ratio=completed?1f:positionMs/durationMs coerce 0..1。**UI**(YoutubePlaylistVideoRow 缩略图 Box 内):底部细进度条(轨道 BiliColors.ProgressTrack+粉色 BiliPink 填充,高 BiliSizing.VideoProgressBarHeight,样式对齐 TV VideoCard 私有 VideoWatchProgressBar 不跨文件复用)+右下角「已看完」角标(黑 60% 半透明圆角 pill+白字 10sp,样式对齐移动端 CompletedBadge;文案跟随本文件硬编码中文现状);collectAsState 持续订阅 history flow,播放中写入进度返回本页即刷新。AppShell 调用处透传 youtubeHistoryStore。**待真机**:看过一半的视频行缩略图底部显示半长进度条;看完整的显示满条+右下「已看完」;没看过的无条无角标;播放列表播放中退出返回详情页,当前视频进度条即时更新。 |
| P11-50 | TV 频道页排序 chip 按上丢焦点修复(致内容 tab 摸不到) | Done(代码完成,待云编译+真机) | 用户报「TV YouTube 频道顶部 tab 不能切换 只有主页」+「在最多/最新播放 tab 按上键会丢失焦点」。**TV 日志取证**(logs_live.log 19:10 会话):`getChannelVideos items=20` + `channel open header=ok tabs=featured,videos,shorts,streams,podcastp,playlists,store,search` 数据层全正常;焦点诊断 `GAINED [channel-grid]`→1.5s 后 `LOST [channel-grid] GAINED [root]`(焦点落空)→`GAINED [sidebar]`(逃逸到 AppShell 侧栏)→用户退出;另见 `FocusRequester is not initialized` 警告(首屏聚焦 runCatching 兜底,无害)。**根因**:tab 切换链路本身正常(移动端日志 tabClick Videos→Playlists 同构验证),断点在**焦点链**——用户从网格第一行按上经 `onMoveUpFromFirstRow` 落到排序 chip(最新发布/最多播放),而 `YoutubeChannelSortChip` 的 onPreviewKeyEvent 只处理 Down/Confirm **不处理 Up**,按上交给 Compose 默认焦点搜索后丢失(无法上移到内容 tab 行/关注 chip),用户焦点永远到不了 tab 行→「不能切换,只有主页」;焦点丢失后按左逃逸到侧栏。**修复**(YoutubeChannelScreen.kt):YoutubeChannelSortChip 加 `onMoveUp` 参数,Up 显式 `requestFocus` 到当前 tab 的 chip(tabFocusRequesters[tab],与 tab chip 的 Down→排序、follow chip 的 Down→tab 形成闭环);tab 间左右移动保持默认焦点搜索(与首页 BiliCapsuleTab 同模式,TV 长期验证可靠)。**待真机**:频道页网格按上→排序 chip→再按上→落内容 tab 行(高亮);tab 行 OK 切 Shorts/直播/播放列表内容正确;tab 行按上回关注 chip;焦点不再丢失/逃逸侧栏。 |
| P11-51 | TV 频道页内容 tab 行左右移动丢焦点修复 | Done(代码完成,待云编译+真机) | P11-50 上移修复生效后用户实测:上键能到「主页」tab,但 **tab 行内左右移动焦点消失**。修复假设「tab 间左右靠默认焦点搜索与首页 BiliCapsuleTab 同模式可靠」被真机证伪——频道页是 AppShell 覆盖层环境,默认焦点搜索落空(与 P11-50 排序 chip 同款病症)。**修复**(YoutubeChannelScreen.kt):tab 行 `entries.forEach`→`forEachIndexed`,YoutubeChannelTabChip 加 `onMoveLeft`/`onMoveRight` 参数,Left/Right 键显式 `requestFocus` 到相邻 tab chip(entries[index±1]);**行首/行尾消费按键不移动**(返回 true 不聚焦任何处),防止焦点逃逸出频道页覆盖层跳到 AppShell 侧栏(P11-50 日志已实证该逃逸路径)。YoutubeChannelFollowChip 的 Left 本就消费不移动(onMoveLeft={false})保持一致。Up/Down/Confirm 逻辑不变。**待真机**:主页 tab 左右键逐个聚焦 Shorts/直播/播放列表(高亮),行首行尾按左/右不丢焦点;OK 切 tab 内容正确;上/下键回关注 chip/排序行照旧。 |
| P11-52 | TV 播放列表详情页焦点高亮不可见修复(细边框→高亮底色) | Done(代码完成,待云编译+真机) | 用户报:播放列表详情页进入后看不到焦点位置,聚焦视频与其它视频无区别。根因:YoutubePlaylistVideoRow/YoutubePlaylistDetailScreen「播放全部」按钮聚焦高亮**只有 1px 粉色边框**(BiliFocus.BorderWidth),TV 观看距离几乎不可见。**修复**:复用 TV 播放器侧板视频行同款 `Modifier.playerFocusedLiquidGlassSurface(shape, focused)`(PlayerOverlay.kt internal,聚焦画 BiliColors.PlayerPanelFocused 高亮玻璃底色、未聚焦透明)——视频行(YoutubePlaylistVideoRow)与「播放全部」按钮聚焦时叠加高亮底色+保留粉边框,与 TV 行式列表焦点语言一致。返回 chip 不动(小组件白边框够用)。**待真机**:详情页视频行聚焦明显(高亮底+粉框),播放全部按钮聚焦有底色,行间移动焦点清晰可辨。 |
| P11-53 | TV 搜索结果页 chip 顺序对齐移动端(排序在前,类型在后) | Done(代码完成,待云编译+真机) | 用户要求 TV 搜索结果页 chip 行对齐移动端顺序「综合 最多播放 最新发布 最多弹幕 UP主」。两端排序数据/文案本就一致(BiliSearchSortOptions/YoutubeSearchSortOptions,strings 同源),差异仅在 chip 排列:TV 端类型 chip(视频/UP主)恒在最前、排序 chip 在后,移动端相反。**改动**(SearchScreen.kt SearchResultsHeader):①LazyRow 重排——排序 chip 在前(仅视频类型,UP主 类型仍隐藏排序只剩类型 chip),类型 chip 在后;②焦点链跟随调整——标题 Down 落**当前选中**排序 chip(非第一个:TV 端 SearchSortButton 是「焦点即选中」,聚焦未选中 chip 会触发 onSelected 重搜,落选中 chip 避免误切档;UP主 类型排序 chip 不在组合树,回退落「视频」类型 chip);行首 chip 的 Left→侧栏跟随行首移动(视频类型=第一个排序 chip,UP主 类型=第一个类型 chip),其余 chip Left 交给默认焦点搜索;showSort 计算提前到 Column 前(标题按键 handler 与 LazyRow 共用)。**待真机**:结果页 chip 顺序为 综合/最多播放/最新发布/最多弹幕/视频/UP主(YouTube 第 4 项为评分);焦点循环:标题↓→选中排序 chip、行首←→侧栏、UP主 类型下只剩 视频/UP主 且行首←可移侧栏。 |
| P11-54 | TV 频道页主页 tab 加「▶ 播放全部」对齐移动端 | Done(云编译绿,运行时待真机) | 用户要求频道页主页 tab 对齐移动端(「▶ 播放全部」+ 最新/最热切换)。两端排序切换本就同源(player_up_sort_latest「最新发布」/player_up_sort_hot「最多播放」,TV chip = 移动端下拉文案),缺的只有播放全部。**改动**:①`YoutubeChannelScreen` 加 `onPlayAll: (List<VideoSummary>) -> Unit` 参数;抽出 `displayItemsFor(state)`(原内容区内联的 channelId/owner 注入映射,播放全部与网格共用);`playAllCurrent()` 读当前 tab 视频态(仅主页 tab 有排序行,即 videoState)整份作队列。②`YoutubeChannelHeader` 排序行最前(排序 chip 前)加 `YoutubeChannelPlayAllChip`——粉色文字药丸(播放列表详情页「播放全部」同语言),**聚焦不触发任何选择**(区别于排序 chip 聚焦即切换),OK 起播;焦点链:Up→tab 行当前 tab,Down→网格首卡,行首 Left 消费不移动(防逃逸侧栏,P11-51 同款)。③AppShell 接线 `onPlayAll`(镜像移动端 MobileApp):`playQueue = queue; channelPlaybackBehind = false; playbackRequest = queue.first().toPlaybackRequest()`——复用播放器已有连播队列机制(PlayerScreen 对 playQueue 队列自动连播,与播放列表详情页同路径)。**待真机**:主页 tab 排序行显示 ▶播放全部(粉字)/最新发布/最多播放;OK 播放全部→第一条起播且播完自动连播下一条;Shorts/直播/播放列表 tab 无该 chip;tab 行↓仍落选中排序 chip(跳过播放全部)。 |
| P11-55 | TV UP空间/YouTube频道页主页排序改单按钮切换(右置)+播放全部左置,对齐移动端 | Done(云编译绿,运行时待真机) | 用户要求:主页 tab 最新/最热改一个按钮靠右切换,B站主页同改。**改动**:①新建 `ui/common/TvSortChips.kt` 共享两 chip——`TvPlayAllChip`(▶播放全部,粉字药丸,聚焦只高亮;行首 Left 默认消费防逃逸)与 `TvSortToggleChip`(「≡ 当前档位」单按钮,OK 在两档间翻转;**聚焦只高亮不切档**,切档重拉数据,聚焦扫过不该触发)——取代原「聚焦即切换」双排序 chip 模式;②`YoutubeChannelScreen` 排序行(仅主页 tab)改为 Row:播放全部居左+Spacer+切换按钮居右,`sortFocusRequesters` Map 换单 `sortFocusRequester`(tab 行↓、网格↑落点同步),删 YoutubeChannelSortChip/ YoutubeChannelPlayAllChip;③`UpSpaceScreen` 同改:原 LazyRow(排序 chip×2+关注)改 Row(播放全部+Spacer+切换+关注),删除 UpSpaceChip/UpSpaceOrders/UpSpaceOrder(不再使用),加 `onPlayAll` 参数+`playAllCurrent()`;④AppShell 两处接线 `onPlayAll`(镜像 MobileApp:playQueue=queue+playbackBehind=false+第一条起播)。焦点链:播放全部⇄切换按钮⇄关注 显式左右 requestFocus(默认焦点搜索在覆盖层不可靠,P11-51 教训),两者 Up→头像/tab 行、Down→网格首卡。**待真机**:两页内容行为「▶播放全部(左)……≡最新发布(右)+关注」;OK 切换按钮→档位翻转重拉;OK 播放全部→连播;左右键在 行首/切换/关注 间移动不丢焦点;YouTube 频道页仅主页 tab 显示该行。 |
| P11-56 | 移动端播放器 YouTube 简介频道行点头像/名进频道页(修复点击无反应) | Done(代码完成,待云编译+真机) | 用户报「YouTube 视频播放,点简介里的头像无法转到频道页,没反应」。根因:移动端 `MobileYoutubeIntroTab` 频道行(头像+名)是刻意纯展示(注释「仅展示,不进频道」),没挂任何点击;TV 端本就有「UP 主页」面板按钮走 `onOpenYoutubeChannel`(PlayerScreen UpFocusHome),坏的只有移动端。**改动**:①`MobilePlayerScreen` 加 `onOpenYoutubeChannel: (channelId, channelName) -> Unit` 参数(默认空实现),经 `MobilePlayerIntroCommentTabs`(两处调用:播放态/暂停态分栏)透传到 `MobileYoutubeIntroTab`;②简介频道行 Row 加 `clickable`——channelId 优先 /player videoDetails(`detail.channelId`),缺省回退卡片携带 `request.channelId`,名字同理回退 `request.ownerName`,两者皆非空才回调;③MobileApp 接线(镜像 `openOwner` 的 YouTube 分支):channelId 非空直接 `youtubeChannelRequest = YoutubeChannel(...)` + `channelPlaybackBehind = true`(垫在播放器后,返回播放器时露出,对齐 B 站 `onOpenUpSpace` 行为);channelId 缺失(搜索直进等存卡无 channelId)时 `youtubeRepository.resolveChannel(name)` 按频道名解析再进。**待真机**:YouTube 简介点头像/频道名 → 频道页覆盖播放器,Back 回播放器;B 站行为不变 |
| P11-57 | 移动端播放列表视频卡片加观看进度条+详情页补「已看完」角标 | Done(代码完成,待云编译+真机) | 用户要求播放列表视频卡片显示观看进度。现状:首页 MobileVideoCard 有 MobileWatchProgress 细条但吃 video.progress 字段,而播放列表(YoutubePlaylistStore DataStore)存的是加入时刻快照,progress 永远停在加入时值,照搬永远不显示;真实进度在 YoutubeHistoryStore(播放器起播/暂停/退出写 positionMs,仅最近 300 条(P11-58 扩))。**改动**:①MobileVideoCard.kt 加两个共享件——`rememberYoutubeWatchPositions(store)`(collect history 建 videoId->entry 映射)与 `YoutubeSnapshotWatchProgress(positionMs,durationMs)`(样式对齐 MobileWatchProgress:4dp 黑半透明轨道+primary 填充;位置/时长无效或 ≥99% 不画,播完语义让给「已看完」角标);②播放列表管理页(MobileYoutubePlaylistPage)第二层视频行:缩略图 Box 改外层 clip 圆角+fillMaxSize 图,未看完且历史有位置时底部画进度条(已看完仍右下角标);③频道播放列表详情页(MobileYoutubePlaylistDetailScreen)PlaylistVideoRow:同款底部进度条+补「已看完」角标(右上,右下让位给时长 pill);④两入口接线:MobileFeedScreen/MobileApp 传 youtubeHistoryStore。**待真机**:看一半退出的视频在播放列表管理页/频道播放列表详情页缩略图底部出进度条;看完的显示角标无进度条;不在最近 300 条历史的视频不画条 |
| P11-58 | YouTube 播放历史容量 50→300 条 | Done(代码完成,待云编译+真机) | 用户要求播放历史扩展到 300 条。播放列表进度条(P11-57)依赖 YoutubeHistoryStore 查真实播放位置,50 条上限导致稍早看过的视频在播放列表里查不到进度。**改动**:`YoutubeHistoryStore.MaxEntries` 50→300(对齐 WatchedStore 已有 300 上限);recordPlay/updatePosition 两处 take(MaxEntries) 自动生效,无迁移逻辑——旧数据本就存全量解码后重新 cap,首写时自然裁到 300。DataStore 体积:单条 JSON ~0.5KB,300 条 ~150KB,无压力。**待真机**:历史 tab 能翻到 300 条;看过多于 50 个视频后,第 51+ 早的视频在播放列表仍有进度条 |
| P11-59 | 移动端+TV 搜索类型 chip 改单开关按钮,去掉「视频」chip | Done(修复CI:单按钮需包 item{} 进 LazyListScope) | 用户要求搜索结果页去掉「视频」类型 chip(「不会有误解」——默认即视频,占位冗余),移动端+同步 TV(P11-53 刚对齐两端,单改一端又错位)。**改动**:①移动端 MobileSearchScreen:原 视频/UP主 两枚 FilterChip 换单开关按钮——恒标「UP主(B站)/频道(YouTube)」,选中态=UP主搜索,点一下在 视频⇄UP主 间翻转(selectType 已有幂等守卫);排序 chip 逻辑不变(B站 综合/最多播放/最新发布/最多弹幕,YouTube 综合最多播放/最新发布/评分),UP主/频道 类型时排序隐藏只剩该按钮;②TV SearchScreen:同改——typeOptions 两枚 SearchSortButton 换单枚,复用 SearchSortButton 渲染但加 `selectOnFocus=false`(聚焦只高亮,OK 才翻转,防 P11-53 焦点扫过误切重搜)+`consumeRight=true`(行尾 Right 消费防焦点逃逸,P11-51 教训);typeFocusRequesters Map(SearchTypeVideo/SearchTypeUser 两枚)换单个 typeToggleFocusRequester(标题↓回退落点、UserResultList↑回焦点同步);③Left 语义:视频模式按钮 Left 走默认焦点搜索回排序行,UP主 模式按钮行首 Left 移侧栏。**待真机**:移动 chip 行为 排序×4+「UP主」按钮,选中填充即UP主结果,再点切回视频;TV OK 翻转类型,聚焦不误切,焦点循环 标题↓→选中排序(UP主模时直接落按钮)↓→结果列表,行尾按右不逃逸 |
| P11-60 | WebDAV 备份/还原入口加快速连通校验 | Done(代码完成,待云编译+真机) | 备份/还原失败路径梳理发现:失败前要逐项走 30s 超时的下载客户端,连不通时用户干等(最坏 connect 30s/读 300s)。**改动**:①WebDavRepository 新增 probeClient(长超时客户端 newBuilder 派生,共享连接池,connect/read/write 各 8s),ping 切用它——保存配置校验与备份入口校验一并提速;②WebDavBackupService 三个入口 backup/restore/backupLogFile 在 runCatching 内首行加 ensureReachable:对配置根 URL 发短超时 GET,不通(网络异常或任何非 2xx 含 401/403)即抛 IOException("无法连接服务器(请检查网络、服务器地址或账号密码)")整体快速判失败;还原侧前置校验额外意义=降低「频道已 clear 后中断」的部分还原概率;备份侧=避免部分上传(日志仍保证全传成功才删本地)。**待真机**:停服务器/断网点备份,8s 内出「操作失败:无法连接服务器…」Toast 而非长时间转圈 |
| P11-61 | 「Auto 永不升过 1080p」根因定位 + SABR 硬解能力/落选诊断日志(Bug A 修复按用户指示暂缓) | Done(代码完成,待云编译+真机日志) | 真机日志定位:14 轨会话 DefaultTrackSelector 只喂 4 轨(303/299/302/298 全 60fps≤1080p),308/315 永不进自适应组 → up=null 恒;手动 1440 走单轨新会话路径所以能播。定位过程:media3 1.10.0 源码确认 ①DefaultTrackSelector(context,factory)=Parameters.getDefaults 无 viewport 约束;②ADAPTIVE 资格要求 isFormatSupported(allowExceeds=false)+bitrate 有值+min/max 约束内;③clearVideoSizeConstraints 只清 max 不清 min(media3-common:381)→ 起始档锁 min=720 泄漏(降档地板 720p,Bug A);④硬解判定链=MediaCodecVideoRenderer.supportsFormat→MediaCodecInfo.isFormatSupported(API29+ 先查 vendor PerformancePoint 覆盖 2560x1440@60/3840x2160@60,不覆盖即硬否;兜底 areSizeAndRateSupported),全 D 级日志真机收不到。**本次改动(仅诊断,按用户"bug A 先不管")**:①新建 SabrCodecDiagnostics:SABR Ready 时逐视频轨打硬解能力判定(itag/mime/wh/fps/bw/首选解码器/hw/FORMAT_HANDLED|EXCEEDS_CAPABILITIES),一次日志分辨性能点低报/sizeAndRate 否/带宽缺填三死因;②YtSabrChunk init 补 excluded=(TrackGroup 全量 itag-已吸收,分辨「在组没选」vs「没进组」);③YtSabrManifest 诊断补 @fps b=bitrate;④TV/移动 loadRequest 两处接线。**待真机**:1080p 视频自动播,看 YtSabrCodec codecSupport 行 308/315 的 support 值 + YtSabrChunk excluded 列表 |
| P11-62 | Firebase Crashlytics 集成 + 日志手动共享「上报」 | Done(代码完成,待云编译;待 google-services.json 就位激活) | 目标:远端设备日志无需连电脑即可回收。选型对比后用户定 Firebase Crashlytics(免费无上限、无需 Play Services、AOSP 盒子可上报,GreatFire 实测上报端点 firebase-settings.crashlytics.com 未被屏蔽;控制台境内需代理)。**机制**:Crashlytics 无直接传日志 API,用标准 workaround——`FirebaseLogSender.sendLogFile` 读日志尾部(`LogCatcherUtil.readLogContent`,LIVE 日志防 OOM 尾读)取结尾 1500 行逐行 `Crashlytics.log()`(内部环形缓冲 ~64KB 超限丢最旧),再 `recordException("Manual log share: <文件名>")` 触发非致命上报,控制台该 issue 下附全部日志 + custom keys(app_version/device_model/log_file/log_size)。**条件启用**:`google-services`/`crashlytics` gradle 插件仅 `app/google-services.json` 存在时 apply(plugins 块 apply false 声明),无 JSON 时 CI 行为与集成前完全一致;`FirebaseApp` 未初始化时 `FirebaseCrashlytics.getInstance()` 抛 IllegalStateException,全链 runCatching 兜底 toast「未配置 Firebase」不崩。**改动**:①`libs.versions.toml`(firebaseBom 33.7.0/googleServices 4.4.2/crashlyticsPlugin 3.0.2)+`app/build.gradle.kts` 条件插件+依赖;②新建 `core/util/FirebaseLogSender.kt`(install 设 custom keys/isAvailable/sendLogFile);③`BiliTvApplication.onCreate` install;④TV `SettingsLogPanel.LogContentPanel` 底部操作行加第 5 按钮「上报」(回调经 SettingsLogsColumn→SettingsScreen 两层→AppShell,AppShell 模仿 onBackupLog toast);⑤移动端 `MobileLogContentScreen` 顶行加「上报」TextButton;⑥strings 4 键(send/send_success/send_failed/send_unavailable)补 values/en/es/pt(zh-rHK/TW 无 logs 键组走默认回退)。**待办**:用户 console.firebase.google.com 建项目、**注册两个包名 com.kirin.mt + com.kirin.mt.debug**(debug 变体有 applicationIdSuffix,一个 client 数组合并),JSON 放 app/ 目录 commit 推送自动激活;真机点「上报」→ 控制台(需代理)看非致命 issue;debug 变体同样上报链路 |
| P11-63 | Crashlytics 集成收尾:JSON 就位激活 + 崩溃自动上报开关(默认关) | Done(手动上报真机验证通过:控制台问题列表「Manual log share」非致命 issue 可见日志尾部;剩余:开关行 UI 核对 + 开关开时崩溃自动报带日志待验证) | ①用户从 Firebase 控制台下载两包名 JSON,合并双 client 条目写入 `app/google-services.json`(6c8f0fa),google-services/crashlytics 插件按条件自动激活,CI 绿验证(33289721872);②新增设置「崩溃日志自动上报」默认关:`AppSettings.crashLogAutoReportEnabled`(DataStore key `crash_log_auto_report_enabled`),`FirebaseLogSender.bindAutoReport` 在 Application 启动后订阅设置流,同步 Crashlytics `isCrashlyticsCollectionEnabled`(关=崩溃数据不自动出网);`LogCatcherUtil` 未捕获处理器落盘崩溃日志后若开关开则把尾部 1500 行注入 Crashlytics 日志缓冲(`logLogcat` 改返回 File?),自动崩溃报告携带日志;③手动「上报」不受开关限制:日志详情页(TV 底部操作行/移动端顶栏)独立「上报」按钮保留,采集开关关时 recordException 后追加 `sendUnsentReports()` 立即放行;④开关行 TV 主设置列表新增(SettingsItemCrashLogAutoReport=38,焦点 5 处:常量/FocusRequesters/SettingsFocusableItems/toLazyIndex 44+extra/LazyColumn item)+移动端 MobileSettingsScreen 系统设置区 MobileSwitchRow;④strings 2 键(crash_auto_report_title/description)4 locale;⑤中途按用户反馈撤掉「分享选项弹窗」方案——列表「分享」恢复直接系统分享,上报只保留在详情页 |
| P11-61b | 「Auto 永不升过 1080p」真凶定位+修复:selectVideoTrack 隐性物理屏视口 | Done(代码完成,待云编译+真机) | 三轮日志递进:①第二轮 codecSupport 证明 308/315 FORMAT_HANDLED+qti 硬解+b/fps 正常(排除能力/带宽假说);②第三轮 YtSabrTracks(移动端补 READY dump)证明 14 轨全 sup=true 却恒 sel=false → 纯 DefaultTrackSelector 资格判定;③读 media3-common 1.10.0 Builder 默认块发现 **isViewportSizeLimitedByPhysicalDisplaySize=true**(默认)——selectVideoTrack 里 `isViewportSizeLimited&&context!=null ? Util.getCurrentDisplayModeSize(context) : null`,无视 viewportWidth=MAX 直接拿**物理屏尺寸**当视口传给 createForTrackGroup → 超屏分辨率(308=3.69MP/315=8.29MP > 屏显像素~2.1MP)isSuitableForViewport=false → 失去 ADAPTIVE 资格恒 FIXED(FixedTrackSelection 单轨兜底才能播→正是手动切 1440 能播的原因);1080p(2.07MP)压线全屏级进组,全部观测闭环。**修复**:TV/移动 trackSelector 参数链加 clearViewportSizeConstraints()(=setViewportSize(MAX,MAX,true),同时置物理屏限制开关 false),ABR 是否升档全交带宽实测(无 buggy 视口截断)。**Bug A(min=720 泄漏→降档地板 720p)按用户指示仍未修**,诊断证据已齐。**待真机**:4K 源自动播,bw 足时 selection 应能吸收 308/315(trackSelLen 6+、sel 含 1440/2160);手动 1440/2160 后 ABR 组仍单轨 |
| P11-64 | SABR Auto 不升 1440(手动切正常)修复:sustained 分母扣减需求驱动的停闸空闲 | Done(代码完成,待云编译+真机) | 用户报 Auto 停 1080p 不升 1440,手动切正常。日志取证(15:26-33):sel=2(303) 钉死,但 bw= 18-19M 已过 308 门①(13.4M×1.25)且无降档记录(门③豁免),卡在门②持续带宽;手动 1440 实测持续 20-23Mbps 证明管子够。**根因**:sustained=60s 墙钟交付÷全墙钟跨度,pinned 低档+满缓冲时停闸空窗全摊入 → sustained≈当前档消耗(8-10M<13.4M),从低档永远凑不出升档证据(§9 r1661 防突发假信号机制的定点死锁副作用)。**修复**(SabrMediaFetcher):recordFetchGap 滑行部分(coast=runway−10s)与 seek/手动 gap 记入 addSustainedGapSample,getSustainedBitrateEstimate 分母改为 rawSpan−sustainedGapMs(1-60s 钳位);GC/断流/pacing 期间 runway 低扣不掉,仍压低 sustained → 315 防卡意图不变。YtSabrAbr 诊断行加 sus=。**待真机**:1080p 满缓冲后 1-3min 内 Auto 升 1440(sus=≥13411K);慢网 4K 防卡死不回归 |
| P11-65 | Crashlytics 日志分享降噪+字节预算:修「注入 1500 行云端只剩 82 行 HWUI 噪音」 | Done(代码完成,待云编译+真机) | 日志中心实测(r1700,16:24 分享):776KB/1500 行注入进 Crashlytics 环形缓冲(~64KB 按字节计),云端只剩 82 行且整段截在 HWUI「Image decoding logging dropped」刷屏段,SABR/ABR 关键行一没带——瓶颈是字节不是行数(取结尾 N 行挡不住字节截断)。**修复**(FirebaseLogSender):`sendLogFile`/`attachCrashLog` 注入前先 `selectLinesForInjection` 降噪压缩——①连续重复行折叠(按剥离 MM-dd HH:mm:ss.SSS pid tid L 前缀的业务内容判重,HWUI/CCodec 刷屏一行顶几百);②已知噪音 tag 整类丢(HWUI/CCodecConfig/CCodecBuffers/CCodec/DisplayData);③按 56KB 字节预算(留 8KB 余量给报告头/customKeys)从尾往前装,高价值 tag(YtSabr*/YtResolver/MobilePlayer/FirebaseLogSender/LogCatcher/FATAL/ANR/AndroidRuntime)超预算可挤出空当(最多溢出单行);返回时间序(旧→新),最新行最后进缓冲在环形缓冲里最靠尾最稳。**离线仿真**(今天 776KB 真实日志同窗口):折叠+降噪 4000→2535 行,预算后 454 行含 183 行 SABR/播放关键行、噪音余 4 行(修复前 0 行关键行)。**已知遗留**:①空壳事件(注入成功云端 0 行,13 条里 5 条)疑似 SDK 上传侧问题,非本次范围;②部分会话「FirebaseCrashlytics component is not present」NPE 间歇复现(alpha.8 R8 装配问题,重启恢复),后续单独排查。**待真机**:播放后分享,云端事件日志应含 YtSabrAbr/YtSabr fetch 关键行(不再全 HWUI);行数 ~400+(预算内);日志头「Shared log」后紧跟有效行 |
